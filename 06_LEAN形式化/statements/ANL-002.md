# ANL-002

For all integers `p`, the scaled quadratic dual attains `p^2` at `x = p`:
`p p + p p - p p = p p`.

This matches the P0001 claim that `f(x)=x^2/2` attains its conjugate at `x=p`.

- Objects: `Int`
- Quantifiers: forall p
- Assumptions: none
- Conclusion: equality
