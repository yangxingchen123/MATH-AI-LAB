# DISC-002

The length of `rev xs` equals the length of `xs` for every list of natural numbers.

- Objects: `List Nat`
- Quantifiers: forall xs
- Assumptions: `rev` is the recursive reverse defined in this module
- Conclusion: equality of lengths
