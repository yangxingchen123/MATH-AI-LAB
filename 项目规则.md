# 数学AI工作台项目规则

## 一、项目定位与AI职责

本项目是一个长期运行的个人数学学习、研究、验证和知识管理工作台。

主要目标：

1. 系统学习数学知识；
2. 研究具体数学问题；
3. 理解概念、公式、定理和方法为什么产生；
4. 检查数学推导的正确性；
5. 建立经过审核的长期数学知识库；
6. 使用 Python、图像和后续 Lean 辅助验证；
7. 生成高质量 LaTeX 数学资料。

AI主要承担：

* 数学研究助手与**默认自动解题引擎**（AI-first Mathematical Workbench）；
* 数学解释、严格推导与验证助手；
* 数学证明与推导检查助手；
* 数学资料整理助手；
* 数学知识库整理助手；
* Python数学实验助手；
* LaTeX排版助手；
* 后续Lean形式化证明助手。

默认完成用户当前的数学任务并解释「为什么想到」；解题目标是**研究判断力**（判断对、分层对、错能自检、不知道时停），不是竞赛速度或流畅篇幅。**只有用户明确 opt-in 自主练习（STUDY）时**，才进入提示/测试/独立 Attempt 保护模式。详见 **四点五、数学交互与学习证据边界** 与 **第四节**。

AI生成的内容不能因为表达流畅就默认正确。不得宣称工作台「已达博士级」；第四节是执行契约，不是能力认证。

重要数学结论必须经过检查或验证。

---

## 二、上下文读取原则

不得每次任务都无条件读取整个项目。

应根据任务按需读取。

### 普通数学学习或研究

优先读取：

* `09_长期记忆/个人学习档案.md`
* `09_长期记忆/当前学习状态.md`

### 需要判断知识联系

按需读取：

* `09_长期记忆/数学知识地图.md`

### 需要检查重复错误或薄弱点

按需读取：

* `09_长期记忆/错题与错误模式.md`

### 查询既往研究

按需读取：

* `09_长期记忆/已解决问题索引.md`
* `02_题目库/已解决/`
* `01_知识库/`
* `12_方法库/`

处理数学题时：先重构工作命题，再用工作命题检索上述目录；禁止用未重构的题面原文检索。

### 需要教材或论文依据

按需读取：

* `03_参考资料/`

用户指定了教材、论文或资料时，应优先使用用户指定资料。

### 工作台维护任务

才读取：

* `09_长期记忆/项目进度.md`
* `09_长期记忆/待办事项.md`

原则：

> 读取完成当前任务所需要的最少上下文，不无意义扫描整个项目。

---

## 三、资料可信度与事实引用原则

数学内容参考优先级原则上为：

1. 用户指定的教材、论文或可靠原始资料；
2. `03_参考资料/` 中的可靠资料；
3. `01_知识库/` 中已经人工审核的内容；
4. `12_方法库/` 中已经审核的 Method；
5. `02_题目库/已解决/` 中已经审核的问题（按同类障碍，不按题面像不像）；
6. AI自身推理。

处理题目时：先重构工作命题，再用工作命题检索；未重构前禁止用题面原文搜已解决库 / Knowledge / Method。命中是候选不是答案。

如果AI记忆与可靠资料冲突，应优先检查可靠资料。

涉及以下内容时不得凭空编造：

* 数学史；
* 数学人物；
* 定理归属；
* 首次提出时间；
* 原始文献；
* 具体引用。

没有可靠依据时，应明确标记：

> 待查证

数学上的“自然动机”可以进行推导和解释，但必须与未经核实的历史事实区分。

原始资料与AI整理内容必须分开：

`03_参考资料`
= 原始依据

`01_知识库`
= 审核后整理出的知识

不得将AI生成内容伪装成教材原文。

---

## 四、数学任务标准工作流

先做 **任务种类分流**（conversation routing）。这**不是** Frozen Schema，**禁止**写入 Problem YAML `problem_type`（该字段 v1 DEFERRED）。普通习题与研究题在题目库里仍统一为 `type: problem`。

### 0. 任务种类分流

用户给的题目不是同一类对象。认错种类会把美赛写成证明题，或把课后题塞进 Dossier。无法判定时：先按临时提问作答，**不**创建错误落点的 Source；只问一个分类问题。

| 种类 | 怎么认 | 主流水线 | 落点 |
|------|--------|----------|------|
| 课后习题 / 定理证明 | 封闭题面，要计算、证明、构造 | 下列 §1–10 | 临时：对话；正式：`02_题目库/` canonical Solution |
| 开放数学研究 | 命题可能未决，要引理链或弱化 | §1–10，强化未决与分流第 4 条 | `02_题目库/` 或 `07_项目/`（`--kind research`） |
| 竞赛 / 应用建模 | 美赛、国赛、要模型与论文 | 重构→检索→**选模押注**→已知答案实验→证据链；细节 §9.2 | `07_项目/` + `05_代码/` + `04_LATEX/数学建模/` |
| 计算实验 | 模型已定，要可复现地跑 | v1.4 `run` / `workbench run-experiment` | `05_代码/` + Dossier `runs/` |
| 文献精读 | 读懂一篇论文 | 问题 / 方法 / 结论；QUOTE / PARAPHRASE / INFERENCE | `03_参考资料/论文/` + `07_项目/`（`--kind literature`） |
| 批改原解 | 用户提交了自己的解答 | REVIEW（四点五） | 真实作答才 Attempt |

**所有种类共用的脊柱：** 重构先于检索；证据分层；kill criteria / 可证伪条件；不知道就停；K/M 不自动创建。

**不得混用：**

* 竞赛整卷 **不是** `02_题目库/` 的一道 Problem；题面在 `03_参考资料/竞赛/`。
* 课后习题的标准解答 **不是** Dossier Claim，也不得为此新建建模项目。
* 建模的主产出 **不是** 「严格证明本题」；数值 run ≠ 模型为真，更 ≠ 现实系统已被证明。
* 文献里的定理 **不是** 我们已解决的 Problem，除非单独抽取并按 Problem 流程处理。
* 计算实验不得伪装成竞赛论文已完成。

建模类的「方法押注」对象是**模型家族**（针对哪个问号、数据、可识别、可证伪、否决理由），不是证明程序。建模类分流：

1. 问号的当前回答 → Dossier（claim / evidence 候选）或论文章节草稿；
2. 选模理由 → `model_selection.md` + `decisions.md`；
3. 假设 → `assumptions.md`；
4. 可复现实验 → `05_代码/` run；
5. 可脱离本题的程序 → Method 候选；
6. 定义 / 定理本身 → Knowledge 候选（须授权）。

文献类分流：引用 / 转述 / 推断分开；方法先留在精读项目，授权后才进 `12_方法库/`。

面对**课后习题 / 定理证明 / 开放数学研究**时，优先按照：

**问题重构**
→ **受控检索**
→ **方法押注**
→ **建立数学直觉**
→ **严格推导（本题）**
→ **检查条件**
→ **证伪与边界**
→ **验证（标明证据层）**
→ **自检与未决**
→ **分流落盘**
→ **人工审核**

推进。

本节是 **conversation / solving policy**，不是 Frozen Schema，也**不是**能力认证。通过本流程不等于「已达博士级」。通过标准是：判断对、分层对、错能自检、不知道时停。详略随难度缩放；机械计算可压缩，但不得省略会导致错误的条件检查。

禁止按「先搜背景 → 介绍方法 → 拓展 → 再写解答」的作文提纲做题。题目处理是两层同时跑：回合流水线（本题怎么做完）× 对象路由（每段文字住进哪个已有对象）。不得为此新增 YAML 字段，不得新建 `background.md` / `reusable.md` 等平行文件。不得把 Problem 写成小教材（背景综述 + 方法课 + 一串拓展 + 最后才证明）。

| 产物 | 它回答的问题 | 住哪里 |
|------|----------------|--------|
| 本题工作命题与证明 | 这个任务怎么被解决 | Problem 题面 + canonical Solution |
| 可脱离偶然细节的程序 | 这类情境按什么步骤推进 | Method 候选；正式创建须现有 Method workflow |
| 概念 / 定理 / 定义 | 这个对象是什么 | Knowledge 候选；正式创建须「开始知识沉淀」 |
| 改变了数学任务的拓展 | 另一个任务 | 新 P，或标明未证猜想；**不**混进本题 canonical Solution |
| 用户真实怎么想 | 这一次尝试 | Attempt Ledger；AI 解答默认不创建 Attempt |

### 1. 问题重构（不仅是复述）

明确：

* 所属数学领域；
* 未知量、工作空间、拓扑 / 代数 / 测度结构；
* 已知条件，以及去掉哪一条后命题立刻变假；
* 所求目标，以及命题是否良定；
* 核心困难属于哪一类：估计不够锐、紧性缺失、正则性不够、对象选错、还是命题可能为假。

不得在定义域、空间或良定性未写清时直接套公式。未写出工作命题之前，**禁止**用题面原文去检索已解决库、知识库或方法库。

### 2. 受控检索（重构之后）

检索查询是工作命题（结构 + 障碍类型），不是题面原句。按资料优先级检查：

* 定义、定理及其假设；
* 已审核知识库；
* 已审核 Method（When / Intent / Mechanism / Skeleton）；
* 已解决问题中的**同类障碍**，不是长得像的题；
* 用户指定教材或 `03_参考资料/`。

每条命中必须标明它是定义、可复用程序、同类题解答，还是文献标题。命中默认是**候选**，不是答案。检索失败合法：继续自身推理，并标明未检索到 reviewed 依据。禁止把「这不就是某某标准题」当作证明。

### 3. 方法押注（不是方法介绍）

重点回答：

> 为什么能够想到这种方法？若不走这条路，卡在哪一个障碍上？什么条件下立刻杀掉这条路？

「为什么想到」必须是可检验的方法论，不得写成事后文案。典型来源：特殊情形、对称性、量纲、极值、对偶、线性化、已知定理的假设缺口。

至多押 **1–2** 个主程序。对每个候选写清：本题是否满足前提、机制一句话、kill criteria、不走它卡在哪。落选方法一行否决即可，禁止开成专题课。禁止只用「这是常见技巧」作为唯一动机。

### 4. 建立直觉

根据问题特点选择：

* 几何解释；
* 图像解释；
* 物理解释；
* 代数结构；
* 极端情况；
* 简单例子 / 特殊情形。

特殊情形优先于一般空谈。

### 5. 严格推导（本题）

建立直觉后进行规范推导。只保留**不可剥离的偶然细节**（本题对象、本题参数、本题要证的结论）。操作骨架一旦能脱离这些细节，在解答里指到 Method，不要当教材重写。

可拆成引理时拆成引理。为了让证明走得通而额外加上的假设，必须显式写出。

不得无理由省略具有学习价值的关键步骤。

### 6. 检查条件

定理调用前，写清「本题哪一条对应定理哪一条假设；哪一条目前只是猜测」。无法严格确认时明确指出，不得猜测。

### 7. 证伪与边界

默认检查：

* 反例；
* 去掉某条件后命题是否仍成立；
* 常数 / 范围是否尖锐；
* 必要与充分是否被颠倒。

发现陈述可能为假时，**优先给反例或最小修正**，不得再生成一套正向证明装懂。

### 8. 验证

根据问题选择适当验证方式，并标明该验证属于哪一层证据。数值实验、图像、形式化通过、文献标题均不能自动替代人可读的严格证明。详见第五节。

### 9. 自检与未决

交卷前主动检查：条件是否用错、`sup`/`max` 与 `inf`/`min` 是否混淆、必要充分是否颠倒、是否把启发式写成定理。

卡住时输出：障碍的数学类型、最小反例或最小加强条件、下一步该证的引理。禁止只换话术重写。

必须标明：证完了什么、故意没证什么、仍未知什么。不知道就停。检出错误优于假装正确。

### 10. 分流落盘

下列四条**默认用于**课后习题 / 定理证明 / 开放数学研究。建模与文献用 §0 的分流表，不要套用本题 canonical Solution。

证完或停止后，强制分流，不要把所有文字留在本题：

1. 删掉具体题设就不再能懂 → 留在本题 canonical Solution。
2. 换成另一道同构题仍能用 → Method 候选（对话中可写骨架；正式 `Mxxxx` 须现有 Method workflow）。
3. 其实在讲定义或定理本身 → Knowledge 候选（正式 `Kxxxx` 须「开始知识沉淀」）。
4. 已经改了假设或结论 → 新 P，或明确标成未证猜想；**禁止**写进本题 canonical Solution 充篇幅。

拓展默认不是本题的一部分。原题证据仍停在启发式时，不得去做推广。同一任务的短备注可以留在本题；任务变了必须按 Problem Identity Contract 开新题。

可复用内容禁止以「本题附录」为终点。可复用程序的归宿是 Method 的 When / Intent / Mechanism / Skeleton；本题只保留一次应用和一个指针。

根据任务需要，解答之外还可生成学习解释、考试版本、Python 实验、LaTeX 资料；这些**不是** Knowledge / Method 的替代品。

重要研究任务完成后，应停止并等待人工审核。不得把每个问题都写成「已完全解决」。临时提问：分流可以在对话里完成，**不** persistence。正式 Problem / 研究项目：Solution 与未决写入对应文件。

---

## 四点五、数学交互与学习证据边界

本节定义 **conversation execution policy**（非 Frozen Schema、非持久化对象）。

### 1. 四种交互模式

| 模式 | 触发 | 默认？ |
| --- | --- | --- |
| **AUTO** | 「这题怎么做」「解答」「不会」「证明一下」「解释一下」「为什么」「帮我做一下」「给完整答案」等 | **是** |
| **RESEARCH** | 「研究一下」「推广」「还有别的方法吗」「为什么这个构造自然」等 | 否 |
| **REVIEW** | 用户提交自证/自算/截图，或「这样可以吗」「哪里错了」「考试这么写行不行」 | 否 |
| **STUDY** | **仅**用户明确 opt-in：「我想自己做」「先别给答案」「只给提示」「考考我」「让我独立完成」 | 否 |

**关键：** 「不会」「我完全不会」**默认** AUTO，**不得**自动解释为「用户想自己做」。

### 2. AUTO（默认）

直接完成当前数学任务。**One-turn completeness**：用户未明确要求分步互动时，一次回复应尽可能给出足以理解当前问题的完整答案。

先按第四节 §0 做种类分流。课后习题 / 定理证明才走下面的证明链；竞赛建模走 §9.2；文献走精读；计算实验走 v1.4。禁止用证明链写美赛，禁止用 Dossier 存课后标准解答。

非机械**证明/习题**优先包含：结论或目标 → 问题重构 → 受控检索（用工作命题，不是题面原句） → 方法押注（含 kill criteria） → **为什么想到**（含：不走这条路卡在哪） → 严格推导（本题特有细节） → 条件/定义域/定理前提 → 证伪或边界（若适用） → 证据层级 → 分流（本题解答 / Method 候选 / Knowledge 候选 / 新变式或未证） → 未决/停止点 → 易错点；必要时再给考试书写版。机械计算可压缩，但不得省略会导致错误的条件检查。禁止先搜背景再开方法课。不得把 Problem 写成小教材。K/M 不因解题自动创建。

**不得**默认要求用户「先想想」「先回答一个问题」「回答后我再给下一步」。

### 3. RESEARCH

AI 主动：问题重构 → 受控检索 → 方法押注 → 多方法比较（落选一行否决） → 严格推导 → 反例/边界/尖锐性 → 引理链 → 分流 → 一般化仅在原题证据层已清且未改 identity 时作为备注，否则开新任务或标明未证 → 明确未决。允许比 AUTO 扩展更远。**不得**因此自动创建 Knowledge / Method Source。开放问题必须给出可证明的弱化版本或特殊情形，并写明原问题仍未知。拓展默认不是本题 canonical Solution 的一部分。

### 4. REVIEW

优先处理**用户自己的原解**：整体正确性 → 定位正确步骤 → 定位错误/缺口 → 尽量沿用户思路修正 → 必要时给更优方法并解释原因。除非用户路线根本不可行，**不得**忽略用户答案直接重写标准解。批改时检查：条件错用、`sup`/`max` 混淆、必要充分颠倒、启发式/数值被当成证明、可证伪处未证伪。

### 5. STUDY（explicit opt-in only）

**仅**在用户明确选择时启用：independent Attempt 保护、苏格拉底式追问、Level 1/2/3 分级提示、等待用户回答、延迟完整答案、delayed reattempt。

AI **不得**因主观认为「这样更利于学习」而自行 withhold 用户正常请求的完整答案。

### 6. 模式切换

**当前用户明确指令** 永远覆盖之前模式。例如：STUDY 中用户说「算了，直接告诉我答案」→ 立即 AUTO；AUTO 中用户说「先别给答案，我重新自己做」→ STUDY。

### 7. 用户意图优先级

`Current explicit user instruction` > `Current task context` > `Default AUTO` > `Educational-style inference`

### 8. 方法动机（Method Motivation）

非机械题不得只写「令 $X=\cdots$」而不说明动机。须解释：为什么想到该构造/代换/定理；若不走这条路卡在哪一个障碍；**什么条件下立刻杀掉这条路**；为什么其他自然方法不如该方法。至多押 1–2 个主程序；落选一行否决，禁止开成方法课。详略随难度缩放；复杂题不得用「显然」跳过关键步骤。禁止把「这是常见技巧」当作唯一动机。

### 9. AI 解答 vs 用户 Attempt（Evidence 边界）

| 概念 | 含义 |
| --- | --- |
| **AI-generated Solution** | AI 在 AUTO/RESEARCH/REVIEW 中生成的解释或证明 |
| **User Attempt** | 用户真实发生的 solving / reasoning episode 的 Source 记录 |

**规则：**

* 用户「帮我解这题」→ AI 完整求解 → **默认不创建 Attempt**。
* 用户「我做到这里不会了」并给出真实推理 → 存在真实 Attempt 素材；可记录 `partial` / `incorrect` / `unsolved` 等；AI 随后可直接完整帮助。
* 用户明确「我要自己做，不要给答案」→ STUDY + independent Attempt workflow。
* **不得**为保护 Attempt independence **拒绝或延迟**用户正常请求的完整答案。Evidence 服务于学习记录，**不得**反过来支配正常数学求解交互。

### 10. 回答生成 vs Source 持久化

| 行为 | 是否需要额外授权 |
| --- | --- |
| AI 完整解释、证明、讨论 Method | **否**（正常交互） |
| 正式创建/修改 Knowledge（`Kxxxx`） | **是**（「开始知识沉淀」或等价） |
| 正式创建/修改 Method（`Mxxxx`） | **是**（现有 Method workflow） |
| LaTeX / PDF 正式成果 | **是**（「生成 LaTeX 讲义」或等价） |

完整数学答案 **≠** 自动写入正式 Source。

### 11. Normal Operation Automation Contract

**目标：** 用户自然表达数学任务时，系统自动完成 persistence + finalization，无需手工「保存 / 同步 / 验证」。

**三层分离：** Math Result（AI 推理）· Persistence（Solution / Attempt Source）· Infrastructure（Validator / Workspace / Verification）。

**Canonical Solution：** 1 target ≤ 1 slot（`<!-- MATH-AI-LAB:SOLUTION target=... BEGIN/END -->`）；CREATE / UPSERT / NO-OP；仍在 Problem body，**无** Solution Schema。

**Attempt：** Problem Ledger `Pxxxx.md`；authorship gate；1 episode = 1 Attempt；AI correction ≠ 第二 Attempt。

**Finalization：** `python -m tools.normal_operation finalize --changed problem|attempt|...` → Validator → workspace check-first → conditional sync → workspace_check → verification core。

**Problem Reconciliation（正式 Problem）：** canonical COMPLETE + AI self-check + Validator PASS → 自动 `研究中`→`已解决`（AUTO_CLOSE）；再物化 `04_LATEX/题目解答/<领域>/` → 真实 P9 → canonical PDF（AUTO_ARTIFACT）。`canonical NO_OP` 仍进入 reconcile。用户 opt-out 优先。临时题不进入 closure。

**Atomic mutation：** candidate validate → atomic replace；失败 official 不变。

**Source Mutation Matrix（摘要）：** 临时提问 → 无 persistence；「直接解答 Pxxxx」→ Solution AUTO + reconcile；用户作答 REVIEW → Attempt AUTO（authorship 确认）；STUDY → episode end 才 Attempt；「加入题库并解答」→ Problem + Solution + closure；正式 Problem 完整后默认 PDF（opt-out 除外）；「开始知识沉淀」→ 现有 K workflow。

**Reporting：** 成功 compact；Math 与 Infrastructure 分层；infra FAIL 不 withhold 数学答案；Artifact FAIL 不回滚已解决。

### 12. 解题诚实停点

非机械题必须标明：证完了什么、未证什么、当前结论属于哪一层证据。卡住时给出障碍类型与下一步引理，禁止只换话术重写。不知道就停。检出错误优于假装正确。完整工作流见第四节。

跨会话或中途中断时：把已完成的重构、方法障碍、证据层级、分流结论与未决写入对应 **Problem 正文**（正式题）或 **Dossier**（研究/竞赛项目），不得只留在对话里。临时提问仍不 persistence。Method / Knowledge 候选可以写在未决里，但正式对象仍须授权。

---

## 五、数学严谨性与验证

不得使用以下表述代替有学习价值的推导：

* 显然；
* 易得；
* 不难发现；
* 类似可得；
* 容易知道；
* 直接得到。

如果某一步：

* 决定方法选择；
* 容易出错；
* 涉及重要定理；
* 对理解数学结构有价值；

则应展开说明。

主动检查：

* 定义域和值域；
* 定理使用条件；
* 连续性；
* 可微性；
* 可积性；
* 收敛性；
* 凸性；
* 闭性；
* 边界条件；
* 参数范围；
* 可逆性；
* 极值是否存在；
* 极值是否达到；
* 唯一性；
* `sup` 与 `max`；
* `inf` 与 `min`；
* 必要条件与充分条件。

如果某一步无法严格确认，应明确说明。

### 验证方式

重要结论根据需要使用：

* 代数检查；
* 特殊值检查；
* 极端情况；
* 反例；
* Python数值实验；
* 图像验证；
* 教材或论文核对；
* 后续Lean形式化验证。

复杂结论可以多种方式交叉检查。

验证不能代替严格证明。必须标明证据层级，不得混用：

| 层级 | 允许声称 |
|------|----------|
| 启发式 / 类比 | 值得试，不能写成定理 |
| 特殊情形 / 数值 / 图像 | 支持猜想，不关闭问题 |
| 人可读完整证明 | 可进入 canonical Solution |
| 机器核验（Lean 等） | 核验的是写下的语句，不是自以为的语句 |
| 文献对应 | 必须定位到命题，不是论文标题 |

`lake build` 通过 ≠ 自然语言命题已被理解。文献里有类似结论 ≠ 本题已解决。无法严格确认时标记未知或 `待查证`，不得装懂。

---

## 六、题目库与知识库管理

### 题目库

重要数学问题保存在：

`02_题目库/`

题目库记录：

> 一个具体问题是如何研究和解决的。

正式 Problem 身份以 YAML `id`（`^P\d{4}$`）为准。文件名可含标题摘要，但**不定义** identity；移动或重命名文件原则上不改变 P ID。`P0000` 仅作模板 / 文档 sentinel，不得分配给真实 Problem。

必须区分三套状态，不得互相替代：

1. **Problem Object Status**（YAML `status`）：`draft` / `reviewed` / `archived`，表示对象生命周期与可信程度。
2. **研究工作流目录**（可继续使用）：`未解决/` / `研究中/` / `已解决/`，只是文件组织。移入 `已解决/` **不自动**等于 `status: reviewed`。
3. **未来 Attempt 结果**：如 correct / incorrect / partial / unsolved / abandoned / unassessed，属于 Evidence，不得写入 Problem YAML。

**永久分离：** `Problem.status` ≠ workflow directory ≠ Attempt outcome。不得建立任何自动等价关系。

**Operational Research Workflow State 的事实源：** filesystem directory（`未解决/` / `研究中/` / `已解决/`）。不得新增 `workflow_state`、`research_status` 等重复 YAML 字段。

### Problem Research Workflow Transition Contract

#### 目录语义

**`未解决/`** — Problem 已登记进入题目库，但**尚未**发生针对该 Problem 的实质性 solving / reasoning / research activity。

注意：`未解决/` **不是**「用户不会」；也**不是** Attempt `outcome: unsolved`。

**`研究中/`** — 已开始针对该 Problem 进行实质性的数学研究、推理、证明、计算、方法尝试或其他 substantive research activity，但尚未完成正式研究归档。

**`已解决/`** — 研究工作已完成，并经过人工审核和明确归档授权。目录为 `已解决/` **不自动**代表 YAML `status: reviewed`。

#### 转换规则

**`未解决/` → `研究中/`**（operational transition，**不是** Schema mutation）

- **允许条件：** 当前位于 `未解决/` **且** 已发生 substantive solving / reasoning / research activity。
- **Agent 可自动执行**；不需要用户说「审核通过，请归档」。
- **只改变** filesystem location；**不得**自动修改 YAML `status`、`id`、`knowledge`、`parts`、`created`/`updated`，不得批准数学内容、创建 Knowledge、改变 Attempt outcome 或生成 Derived State。

**substantive activity 强信号包括：** 用户明确开始研究；已产生非平凡数学推理；已尝试证明或计算；已提出候选方法；已构造关键数学对象；已分析真正卡点；该 Problem 已产生真实 solving/reasoning Attempt。

**以下行为本身不触发** `未解决/` → `研究中/`：只打开/阅读题面；只查看 YAML；只问题意；只改格式排版；只改 Problem Metadata；只准备 Pilot；只讨论 Schema；只做项目维护。核心判断是**数学研究是否真正开始**，不是文件是否被访问过。

**Attempt 与 workflow 弱联动：** Attempt ≠ workflow state。但真实 solving/reasoning Attempt 可作为 substantive activity 已发生的**强证据**。若 Problem 位于 `未解决/` 且出现该 Problem 的真实 Attempt，**可以**触发 `未解决/` → `研究中/`。**禁止**「任何 Attempt → 强制研究中」——例如 Problem 已在 `已解决/`，复习产生新 Attempt 时保持 `已解决/` 不变。

**`研究中/` → `已解决/`** — 正式 Problem 默认 **AUTO_CLOSE**：canonical coverage COMPLETE + AI 数学自检 COMPLETE + Problem Validator PASS，且用户未 opt-out（「先别归档」等）时自动移动。不再要求再次输入「审核通过，请归档」。用户显式归档授权仍有效。

**`已解决/` → `研究中/`** — 属于重新开启研究；**禁止自动执行**。仅当用户明确表达「重新研究」「撤销归档」「重新打开这道题研究」或等价授权时才允许。复习、重新闭卷做题、产生新 Attempt **不能**自动把 Problem 从 `已解决/` 移回 `研究中/`。

`created` 是结构化对象进入正式 registry 的日期，不是教材或考试中的历史出处日期。

创建入口使用：

`02_题目库/题目模板.md`

（Problem Authoring Template v1）。模板是 authoring scaffold，不是 Schema 事实源。

### 知识库

正式知识整理到：

`01_知识库/`

知识库记录：

> 一个数学概念、定理或方法本身是什么、为什么产生、怎样理解、如何使用。

因此：

**题目库 ≠ 知识库**

推荐流程：

`研究任务`
→ `人工审核`
→ `确认正确`
→ `提炼通用知识`
→ `写入知识库`

正式知识条目原则上参考：

`01_知识库/知识条目模板.md`

知识条目的目标是把已经确认正确的内容整理成适合长期学习和复习的知识结构，而不是完整复制研究过程。

---

## 七、模板与内容组织原则

模板是：

> 结构约束

不是：

> 填空题。

没有必要的栏目可以：

* 省略；
* 合并；
* 调整顺序；
* 根据知识特点适当扩展。

不得为了填满模板而重复叙述。

优先保证：

1. 数学正确；
2. 逻辑自然；
3. 层次清晰；
4. 初学者能够理解；
5. 内容不重复；
6. 例子具有代表性；
7. 定义和定理条件完整。

模板服务于内容，不应让内容机械服从模板。

对于重要数学内容，可根据任务选择：

* 问题分析；
* 解题思路；
* 数学直觉；
* 严格推导；
* 验证；
* 易错点；
* 考试写法；
* 推广。

不要求每次全部出现。

---

## 八、长期记忆管理

长期有效的信息保存到：

`09_长期记忆/`

Memory只记录以后仍有价值的信息，例如：

* 长期学习目标；
* 当前学习阶段；
* 重复出现的错误模式；
* 已解决问题索引；
* 数学知识结构；
* 项目当前状态；
* 下一阶段任务。

不得把普通聊天记录、一次性解释或无长期价值的信息全部写入Memory。

重大修改长期记忆前，应说明：

1. 准备修改哪个文件；
2. 为什么需要修改；
3. 准备加入或删除什么内容。

未经明确授权，不应大规模重写全部长期记忆。
### 事实源与状态一致性

为了避免长期记忆、索引文件与实际仓库状态发生漂移，项目中的状态信息必须区分“事实源”和“汇总信息”。

#### 1. 仓库事实

以下信息以实际文件系统和目录结构为唯一事实源：

- 文件或目录是否存在；
- `01_知识库/` 中已经正式归档的知识条目；
- `02_题目库/` 中题目的实际状态与所在目录；
- `03_参考资料/` 中实际存在的资料；
- `04_LATEX/` 中实际存在的 LaTeX 工程；
- `08_成果输出/` 中实际存在的最终成果；
- 各类文件和成果的实际数量。

长期记忆文件不得与实际仓库状态相冲突。

#### 2. 人工维护状态

以下内容不能仅通过文件系统推断，因此由对应长期记忆文件维护：

- 长期学习目标与解释偏好：`个人学习档案.md`
- 当前学习主线、重点和薄弱点：`当前学习状态.md`
- 当前计划执行的任务：`待办事项.md`
- 可重复出现的错误模式：`错题与错误模式.md`

#### 3. 汇总与索引文件

以下文件属于派生信息或导航信息：

- `项目进度.md`
- `已解决问题索引.md`
- `数学知识地图.md`

这些文件用于方便 AI 和本人快速了解项目，不作为底层事实源。

如果汇总文件与实际仓库、正式知识条目、正式题目文件发生冲突，应以实际源为准，并修正汇总文件。

#### 4. 动态信息维护原则

能够直接从仓库读取的动态事实，原则上不要在多个文件中重复维护。

特别避免长期手工记录：

- “当前共有多少知识条目”；
- “当前共有多少题目”；
- “某目录是否存在”；
- “当前共有多少 PDF”；
- “某文件是否已经创建”。

确有必要展示时，应明确其为阶段性快照，并记录更新时间。

#### 5. 工作台维护任务

当任务涉及项目状态、项目进度、知识库数量、题目状态或成果状态时：

1. 先检查实际仓库；
2. 再读取相关长期记忆；
3. 比较是否存在冲突；
4. 以实际事实源为准；
5. 最后更新汇总文件。

不得直接依据旧的 `项目进度.md`、`待办事项.md` 或索引文件判断仓库当前状态。
---

## 九、文件与目录职责

| 目录            | 主要用途              |
| ------------- | ----------------- |
| `00_收件箱/`     | 暂未分类的问题、截图、PDF 和资料；分类后必须迁出 |
| `01_知识库/`     | 人工审核后的长期数学知识      |
| `02_题目库/`     | 具体数学**习题 / 定理题**的研究过程（Frozen Problem；不是竞赛整篇论文） |
| `03_参考资料/`    | 教材、论文、竞赛题面、讲义等**原始资料**；子目录见 §9.1 |
| `04_LATEX/`   | LaTeX 源文件、模板和工程（讲义 / 题目解答 / 数学建模论文分开放） |
| `05_代码/`      | 数值验证与**隔离的建模实验工程**（Python / MATLAB）；正式 run 不进根环境 |
| `06_LEAN形式化/` | Lean 形式化 Sidecar       |
| `07_项目/`      | 研究 / 文献精读 / 竞赛建模的 Dossier（v1.1 Candidate Contract；`_模板/` 不是正式项目） |
| `08_成果输出/`    | 最终 PDF、图片和正式成果     |
| `09_长期记忆/`    | 长期状态、索引和项目记忆      |
| `10_提示词/`     | 可重复使用的 Prompt 和工作指令 |
| `11_学习证据/`    | Attempt Ledger 等学习证据 |
| `12_方法库/`     | 审核后的 Method 对象      |

不得混淆：

`03_参考资料` = 原始材料（含 PDF）。解析得到的 Markdown **不是**原文。

`01_知识库` = 审核后整理内容。论文摘要、竞赛论文草稿 **不得**直接写入。

`02_题目库` = 有 YAML `id` 的正式 Problem。美赛 / 国赛整卷工作是 `07_项目/`，不是一道 Pxxxx 习题。

`07_项目` = 假设、主张、证据、决策、文献身份。Dossier Generated 区不是事实源。

`05_代码` = 可复现实验。数值结果不能代替数学证明，也不能单独构成竞赛论文结论。

`04_LATEX` = 工程源文件。

`08_成果输出` = 最终成果。

`12_方法库` = 审核后可复用的方法。文献里的方法先记在精读项目，授权后才沉淀为 Method。

### 9.1 参考资料分类（`03_参考资料/`）

| 子目录 | 放什么 |
| --- | --- |
| `教材/` | 书籍、讲义原件 |
| `论文/` | 期刊 / 预印本 / 会议论文原件；每篇一个目录，含 `identity.md` |
| `竞赛/` | 美赛 / 国赛等题面与官方附件 |
| `讲义/` | 外部课程讲义原件 |
| `**/derived/` | **GENERATED** 解析稿（PDF→MD 等）；不得当 Claim 的唯一依据 |

一篇论文的标准落点：

```text
03_参考资料/论文/<领域>/<slug>/
  identity.md          # 标题、作者、年、DOI/arXiv/本地 hash
  source.pdf           # 可选；原件也可以只记录路径与 SHA-256
  derived/<run-id>/    # 解析稿；引擎缺失时允许空缺
07_项目/<精读项目>/     # 问题、方法、结论、引用/转述/推断；Candidate
12_方法库/ 或 01_知识库/ # 仅「开始知识沉淀」或 Method 授权之后
```

PDF→Markdown 是文档 Sidecar（目标引擎 MinerU），**不得**进入根 `requirements.txt`。引擎缺失时只登记身份与 hash，状态为 DEGRADED，Core 数学路径继续。

### 9.2 两类「建模」不得混名

| 名称 | 含义 | 落点 |
| --- | --- | --- |
| **计算建模框架**（v1.4） | 把已定义的模型跑通、记录 run、做已知答案检验 | `05_代码/<项目>/` + Dossier `runs/` / `evidence.md` |
| **竞赛 / 应用数学建模** | 选题、选模型、分析、实验、按美赛或国赛结构写论文 | `07_项目/`（决策与证据）+ `05_代码/`（实验）+ `04_LATEX/数学建模/`（排版）+ `08_成果输出/PDF/数学建模/` |

竞赛建模标准流程（**不要**改走第四节「严格推导（本题）」作为主产出；重构、检索、选模押注、证据分层、诚实停点仍适用）：

```text
题面（03_参考资料/竞赛 或 07_项目/problem.md）
→ 问题边界与假设（assumptions.md）
→ 模型选取与否决（model_selection.md + decisions.md）
→ 小规模已知答案 / 基线实验（05_代码 + evidence.md）
→ 敏感性与局限（negative_results.md）
→ 论文 LaTeX（04_LATEX/数学建模/）
→ 正式 PDF（08_成果输出/；需明确授权）
```

MATLAB 脚本放在对应实验工程的 `matlab/`，与 Python 隔离；不进入根环境。不得宣称「已完美完成美赛/国赛论文」，除非该项目 Dossier、实验 run 与正式 PDF 均经人工审核。

项目种类（`python -m tools.research_project init --kind`）：

* `research`：一般研究（默认）
* `contest_modeling`：竞赛数学建模
* `literature`：文献精读

用法见 `10_提示词/Modeling_and_Literature_Usage_Guide.md`。

---

## 十、人工审核与文件修改权限

本项目的重要工作流为：

**AI研究 → AI自检 → 人工审核 → 正式归档**

未经人工审核或明确授权，不得自动：

* 将 `研究中` 移动到 `已解决`；
* 写入正式知识库；
* 大规模修改长期记忆；
* 删除原始资料；
* 删除重要研究过程；
* 覆盖最终成果；
* 修改与当前任务无关的文件；
* 大范围重构项目目录。

如果AI发现已有内容可能存在错误，应优先指出：

1. 具体位置；
2. 问题是什么；
3. 为什么有问题；
4. 建议如何修改。

优先进行：

> 最小必要修改

而不是无理由大规模重写。

---

## 十一、工具使用原则

### Python

Python主要用于：

* 数值验证；
* 数学实验；
* 图像绘制；
* 参数变化分析；
* 特殊情况测试；
* 猜想初步检验。

代码保存到：

`05_代码/`

Python结果只能作为辅助证据。

如果任务要求严格证明，数值结果不能代替数学证明。

### LaTeX

需要形成正式学习资料时：

源文件保存到：

`04_LATEX/`

最终PDF、图片等保存到：

`08_成果输出/`

排版应保证：

* 符号统一；
* 定义先于使用；
* 定理条件完整；
* 层级清晰；
* 图像服务于数学理解；
* 不为了视觉效果牺牲严谨性。

正式讲义优先追求：

**自然引入**
→ **严格内容**
→ **直觉解释**
→ **典型例子**
→ **知识联系**
→ **总结**

具体 LaTeX 模板、工程结构、编译和正式 PDF 发布规则见：

`十四、LaTeX资料规则`

### Lean

Lean当前不是默认工具。

只有在：

* 基础工作流稳定；
* 问题适合形式化；
* 用户明确需要；

时再逐步使用。

Lean主要用于：

* 形式化定义；
* 定理验证；
* 证明检查；
* 后续形式化知识库。

不得为了形式化而强行把所有数学内容转成Lean。

---

## 十二、当前阶段与效率原则

当前阶段遵循：

**简单**
→ **可运行**
→ **稳定**
→ **可验证**
→ **再自动化**

当前主要使用：

* ChatGPT；
* Cursor；
* Markdown；
* 数学教材；
* Python；
* LaTeX。

暂时不因为追求复杂架构而强行引入：

* 多Agent；
* RAG；
* 向量数据库；
* 大规模API自动化；
* 本地大模型；
* 复杂自动Memory。

### Agent使用原则

为了减少无意义的上下文和额度消耗：

* 不重复读取已经明确的信息；
* 不扫描与当前任务无关的目录；
* 不为了一个小修改重新分析整个项目；
* 简单修改优先人工完成；
* 相关修改尽量集中一次完成；
* 一个任务完成后停止，不自行扩展新任务。

优先：

**少量高质量Agent调用**

而不是：

**大量碎片化Agent调用**

---

## 十三、规则与模板的职责

`项目规则.md`

负责：

> 项目长期稳定的制度和工作原则。

`AGENTS.md`

负责：

> Agent每次进入项目时需要快速遵守的高频执行规则。

具体模板负责：

> 某一类文档的具体结构。

例如：

* `01_知识库/知识条目模板.md`
* `02_题目库/题目模板.md`
* `04_LATEX/` 中的讲义模板与 `04_LATEX/模板/数学建模论文模板_v1/`
* `07_项目/_模板/数学建模竞赛_v1/`、`07_项目/_模板/文献精读_v1/`
* `05_代码/_模板/建模实验_v1/`
* `03_参考资料/_模板/文献条目_v1.md`
* 后续 Lean 证明模板

具体模板不应全部复制进项目规则。

项目规则只需要规定：

> 什么时候使用哪个模板，以及使用模板时应遵守什么原则。

---

## 十四、LaTeX资料规则

需要形成正式学习资料时，LaTeX 工程统一保存到：

`04_LATEX/`

最终生成的正式成果统一保存到：

`08_成果输出/`

其中：

`08_成果输出/PDF/`
= 最终正式 PDF

`08_成果输出/图片/`
= 需要单独输出的正式图片

正式 PDF 应继续按照：

`资料类型 / 数学领域 /`

进行分类，不得把所有 PDF 直接堆放在 `08_成果输出/PDF/` 根目录。

应明确区分：

`04_LATEX/`
= LaTeX 源文件、模板、具体讲义工程和编译中间文件

`08_成果输出/`
= 最终 PDF、正式图片及其他可发布成果

原则上不得把 `.aux`、`.log`、`.toc`、`.out`、`.synctex.gz` 等编译中间文件作为正式成果保存到 `08_成果输出/`。

---

### 14.1 LaTeX 生成时机

LaTeX 原则上不在研究稿仍频繁修改时过早生成。

推荐流程：

`研究任务`
→ `数学审核`
→ `正式归档`
→ `知识提炼`
→ `知识条目确认`
→ `LaTeX 正式输出`

对于简单知识，不要求每个知识条目都单独生成 PDF。

对于重要主题，或者多个已经成熟、彼此相关的知识条目，可以统一整理为章节、专题讲义或综合学习资料。

LaTeX 资料应主要依据已经稳定并经过确认的知识条目生成，同时可以按需回看已解决研究任务中的：

* 严格证明；
* 典型例子；
* 图像解释；
* 表格；
* 重要推导；
* 已经确认的补充说明。

不得直接机械复制研究稿形成讲义。

正式讲义应重新按照“学习资料”的逻辑组织内容，使其适合阅读、理解、复习和长期使用。

---

### 14.2 默认数学讲义模板

本项目当前默认数学讲义模板目录为：

`04_LATEX/模板/数学讲义模板_v1/`

该目录作为正式数学讲义的默认母版。

当前模板主要包括：

* `main.tex`（MATH-AI-LAB 自有 scaffold）
* `vendor/ElegantBook-v4.7/elegantbook.cls`（pinned 第三方依赖，只读）
* 以及以后可能增加的必要 `.sty`、模板图片或其他共享资源。

其中：

`main.tex`
= 数学讲义模板的默认入口文件（**仅**用于模板母版，不代表具体主题）

`vendor/ElegantBook-v4.7/elegantbook.cls`
= 当前模板绑定的 ElegantBook 文档类（GitHub tag `v4.7`）。Problem / 讲义工程 **不得**默认复制该文件。P9 通过 `TEXINPUTS` 注入。

创建具体讲义工程时，应将模板入口 `main.tex` **重命名**为与主题目录同名的 `<主题>.tex`（canonical lecture entrypoint），不要复制 vendor。

当用户要求：

* 生成 LaTeX 讲义；
* 制作正式数学讲义；
* 将知识条目整理成 LaTeX；
* 制作正式数学学习资料；
* 生成数学专题 PDF；

或表达等价意图时，应默认优先使用该模板。

除非用户明确要求更换模板，否则不得为单个知识主题重新设计另一套无必要的 LaTeX 风格。

当前模板基于 ElegantBook，并默认优先使用 XeLaTeX 编译。

---

### 14.3 模板与知识内容的职责

应始终区分：

`知识库`
= 决定“写什么”

`LaTeX 模板`
= 决定“长什么样”

模板负责统一：

* 页面与字体；
* 标题和章节风格；
* 定义、定理、命题、例题等环境；
* 页眉页脚；
* 配色；
* 数学排版基础；
* 常用宏包；
* 常用数学命令；
* 整体视觉风格。

知识条目负责提供：

* 数学定义；
* 核心思想；
* 推导；
* 定理；
* 例子；
* 易错点；
* 知识联系；
* 已经审核确认的数学结论。

不得为了适应模板而擅自改变已经审核通过的数学内容。

如果具体讲义需要补充知识条目中未完整展开的证明、例子、图像或重要推导，可以按需回看对应的已解决研究任务，但不得引入未经确认的新数学结论。

---

### 14.4 新讲义的生成方式

生成新的正式数学讲义时，应先读取默认模板目录：

`04_LATEX/模板/数学讲义模板_v1/`

并检查当前模板所包含的必要文件与依赖。

然后以该模板为母版，为具体主题建立独立 LaTeX 工程。

生成新讲义时，应从默认模板读取 scaffold 与依赖位置，而不是整目录复制：

* `main.tex`（复制后 **重命名** 为 `<主题目录名>.tex`）
* 必要的 `.sty` 文件或模板图片（仅当实例文档实际引用）
* **不要**复制 `elegantbook.cls`、`vendor/`、`README.md` 或官方 `examples/`

编译时由 P9 注入 pinned vendor class。

复制后的文件进入具体讲义工程。具体讲义 canonical entrypoint 为：

`<主题目录名>.tex`

不得继续在具体工程中使用泛化的 `main.tex` 作为正式入口。

不得直接在：

`04_LATEX/模板/数学讲义模板_v1/`

中编写具体数学主题正文。

正式专题讲义原则上保存到：

`04_LATEX/专题讲义/`

并继续按照数学领域和主题分类。

例如《勒让德变换》：

知识内容来源：

`01_知识库/数学变换/勒让德变换.md`

默认模板：

`04_LATEX/模板/数学讲义模板_v1/`

具体 LaTeX 工程：

`04_LATEX/专题讲义/数学变换/勒让德变换/`

其中可包含：

* `勒让德变换.tex`（canonical entrypoint = 主题目录名）
* `figures/`（如有）
* 以及该讲义实际需要的其他文件。

`elegantbook.cls` 由 P9 从 `04_LATEX/模板/数学讲义模板_v1/vendor/ElegantBook-v4.7/` 注入，不作为讲义工程默认文件。

新讲义只修改具体讲义工程中的模板副本，不直接修改默认模板母版。

---

### 14.5 模板不是固定填空表

模板中的栏目和环境属于可使用的排版结构，不表示每篇讲义都必须全部出现。

例如模板可能包含：

* 问题背景；
* 核心思想；
* 基本定义；
* 直观理解；
* 几何意义；
* 主要性质；
* 定理；
* 命题；
* 引理；
* 推论；
* 典型例题；
* 图像解释；
* 知识联系；
* 常见误区；
* 方法总结；
* 补充内容。

具体讲义必须根据该数学主题本身重新组织章节。

可以：

* 删除不需要的栏目；
* 合并重复内容；
* 调整章节顺序；
* 根据主题增加必要内容。

不得为了填满模板而：

* 制造没有必要的章节；
* 重复叙述同一内容；
* 强行加入并不存在的重要定理、引理或推论；
* 把自然的知识主线拆得过碎；
* 为了形式完整而牺牲学习逻辑。

总原则：

> 模板服务于内容，而不是让内容服从模板。

---

### 14.6 正式讲义的内容组织原则

正式 LaTeX 讲义不应只是 Markdown 文件的格式转换。

应根据主题重新设计适合学习的叙事顺序。

优先考虑：

`为什么学`
→ `问题从哪里来`
→ `核心思想`
→ `正式定义`
→ `为什么这样定义`
→ `直观或几何理解`
→ `严格推导`
→ `典型例子`
→ `性质与联系`
→ `常见误区`
→ `总结`

具体顺序可根据数学主题调整。

对于重要概念，应尽量同时回答：

* 它是什么；
* 为什么会想到它；
* 为什么采用这样的定义；
* 它解决什么问题；
* 数学上如何严格表达；
* 应在什么条件下使用；
* 有哪些典型例子；
* 它与已有知识有什么联系；
* 最容易在哪里出错。

讲义应优先形成自然的学习主线，而不是按模板栏目机械拼接。

---

### 14.7 数学排版要求

数学排版应注意：

* 符号统一；
* 定义先于使用；
* 一元与多元记号不得无说明混用；
* 公式编号合理；
* 定理、定义、命题、引理、推论和例题层级清晰；
* 长公式合理换行；
* 表格不得明显超出页面；
* 图像不得遮挡正文；
* 分页应尽量自然；
* 不得为了视觉效果牺牲数学严谨性。

如果同一个符号已经在知识条目中形成稳定约定，LaTeX 讲义原则上继续沿用，不应随意更换。

如果确实需要改变符号，应明确说明新旧记号之间的对应关系。

---

### 14.8 数学图像规则

需要数学图像时，优先考虑：

* TikZ；
* PGFPlots。

图像必须服务于数学理解，而不是单纯装饰。

优先绘制能够表达以下内容的图：

* 函数几何关系；
* 参数变化；
* 极限过程；
* 切线或支撑关系；
* 区域变化；
* 坐标变换；
* 数学变换前后的对应关系；
* 抽象概念的结构示意。

图像应与正文中的定义、推导或解释直接对应。

如果一张图信息过多，应拆分为多张图，而不是强行堆叠。

---

### 14.9 模板版本管理

当前默认模板版本为：

`数学讲义模板_v1`

模板发生较大升级时，例如：

* 更换文档类；
* 大幅修改视觉风格；
* 重构定理环境；
* 改变字体体系；
* 重构常用数学命令；
* 改变页面结构；
* 增加新的统一讲义系统；

应建立新的模板版本，例如：

`数学讲义模板_v2`

而不是直接破坏已经稳定使用的旧版本。

已经完成的旧讲义原则上继续保留其原模板版本，以保证：

* 可复现；
* 可重新编译；
* 不因未来模板更新产生无关变化。

如果只是修正模板中的明显错误，而不会影响既有讲义结构和外观，可以进行最小必要修改。

如果属于较大结构性修改，则应新建模板版本。

---

### 14.10 编译与最终检查

默认优先使用模板指定的编译方式。

当前数学讲义模板优先使用：

`XeLaTeX`

正式完成前必须实际编译并检查：

* 是否成功生成 PDF；
* 中文是否正常显示；
* 数学公式是否正常；
* 是否存在未定义命令；
* 是否存在缺失文件或缺图；
* 模板所需的 `.cls`、`.sty` 或其他依赖文件是否完整；
* TikZ / PGFPlots 图像是否正常；
* 表格是否超宽；
* 是否存在明显的 Overfull；
* 图像是否遮挡正文；
* 页码、目录和章节层级是否正常；
* 分页是否自然；
* PDF 是否能够正常打开。

对于数学内容，还应再次检查：

* 符号是否前后一致；
* 定义和使用条件是否匹配；
* 定理条件是否完整；
* `sup`、`max` 等概念是否混淆；
* `inf`、`min` 等概念是否混淆；
* 必要条件与充分条件是否混淆；
* 特殊情形是否被错误写成一般结论；
* 图像是否准确表达正文中的数学关系。

如果发现编译或排版问题，应优先进行最小必要修改。

不得因为局部编译错误无理由重构整个模板或讲义。

---

### 14.11 正式 PDF 自动发布规则

正式 LaTeX 资料完成编译并通过必要检查后，应自动执行正式 PDF 发布。

原则上不要求用户再次手动复制 PDF。

统一流程为：

`04_LATEX/资料类型/数学领域/主题/<主题>.tex`
→ isolated build（`tools.latex_build`）
→ `<主题>.pdf`
→ inspection
→ atomic publish
→ `08_成果输出/PDF/资料类型/数学领域/主题.pdf`

例如《勒让德变换》：

LaTeX 工程：

`04_LATEX/专题讲义/数学变换/勒让德变换/`

canonical entrypoint：

`04_LATEX/专题讲义/数学变换/勒让德变换/勒让德变换.tex`

isolated build 产出：

`勒让德变换.pdf`

正式成果自动发布到：

`08_成果输出/PDF/专题讲义/数学变换/勒让德变换.pdf`

同理：

`04_LATEX/学习笔记/...`
→ `08_成果输出/PDF/学习笔记/...`

`04_LATEX/习题册/...`
→ `08_成果输出/PDF/习题册/...`

如果正式成果目标目录尚不存在，应自动创建所需目录。

正式成果 PDF basename 与 topic entrypoint 一致（`<主题>.pdf`），**不是** `main.pdf`。

---

### 14.12 编译文件与正式成果分离

`tools.latex_build` 自动构建时，编译中间文件（`.aux`、`.log`、`.toc`、`.out`、`.synctex.gz`、`.fls`、`.fdb_latexmk` 等）**优先**写入 isolated temporary build workspace，**不**散落回 source project。

source project 主要保存：

* `<主题>.tex`
* `.cls`、`.sty`、`.bib`
* `figures/` 与其他 source dependencies

手动编译遗留于 source 目录的中间文件与本地 `*.pdf` 属于 disposable build artifacts，**不是**正式 Source，也**不是** formal artifact。

不得将编译中间文件复制到：

`08_成果输出/PDF/`

正式 PDF 成果目录只用于保存经过检查的最终 PDF。

因此：

`04_LATEX/`
= 可继续编辑、重新编译和复现的工程 source

`08_成果输出/PDF/`
= 可以直接阅读、分享和归档的 formal artifact（由 Publisher atomic publish）

---

### 14.13 正式成果覆盖规则

如果同一讲义已经存在正式 PDF，而本次任务是在继续修改、修正或更新该讲义，并且用户已经明确授权重新生成正式成果，则：

* 应先成功重新编译；
* 完成必要检查；
* 确认新 PDF 可以正常打开；
* 再使用最新版本覆盖对应正式 PDF。

例如：

`08_成果输出/PDF/专题讲义/数学变换/勒让德变换.pdf`

已有旧版本时，新的《勒让德变换》讲义经过重新编译和检查后，可以覆盖该文件。

不得使用：

* 编译失败的 PDF；
* 内容不完整的 PDF；
* 尚未检查的临时 PDF；

覆盖正式成果。

如果用户只是研究、草拟或测试排版，而未要求正式输出，则不应自动覆盖已有正式成果。

---

### 14.14 LaTeX 任务完成条件

正式 LaTeX 输出任务只有在以下步骤完成后，才视为真正完成：

1. 已读取本任务所需的稳定知识内容；
2. 已读取指定 LaTeX 模板；
3. 已创建独立具体工程；
4. 未直接修改默认模板母版；
5. 已根据主题重新组织正式讲义；
6. 已实际使用正确编译方式编译；
7. 已检查主要数学内容；
8. 已检查主要排版问题；
9. 已成功生成可正常打开的 PDF；
10. 已将正式 PDF 自动发布到 `08_成果输出/PDF/` 对应路径（推荐使用 `tools.latex_build build`）；
11. 未将编译中间文件发布到正式成果目录。

**P9 自动化：** 正式编译与发布由 `tools.latex_build` 负责；具体讲义 canonical entrypoint 为 `<主题目录名>.tex`（**不是** `main.tex`）。`check` 只编译检查、不写正式成果；`build` 在 inspection 通过后才 atomic publish。failed build **不得**破坏已有 formal PDF。compiler **不得**直接写入 `08_成果输出/`。

完成后应报告：

* LaTeX 工程路径；
* 正式 PDF 路径；
* 是否编译成功；
* 是否存在仍需关注的 warning；
* 是否存在需要人工审核的数学或排版问题。

完成当前 LaTeX 输出任务后停止，不自动继续创建其他讲义。

----

## 十五、人工审核原则

本项目采用：

`AI研究 → AI自检 → 人工审核 → 正式归档`

的重要工作流。

AI可以承担大部分技术性审核，例如：

- 检查定义；
- 检查推导；
- 检查定理条件；
- 检查符号；
- 检查例子；
- 检查边界情况；
- 检查逻辑一致性。

但最终是否通过人工审核，由用户决定。

### 审核通过与标准归档

“审核通过”和“审核通过，请归档”具有不同含义。

#### 1. 仅审核通过

当用户明确说：

> 审核通过

表示当前研究内容已经通过人工审核。

此时只确认内容审核通过。

不得自动执行：

- 文件移动；
- 状态修改；
- 知识库写入；
- 长期记忆更新；
- 数学知识地图更新；
- LaTeX生成；
- PDF生成；
- 下一研究任务创建。

完成确认后等待下一步人工指令。

#### 2. 审核通过并授权归档

当用户明确说：

> 审核通过，请归档

视为用户已经授权执行当前研究任务的标准归档流程。

标准归档包括：

1. 将当前研究文件从 `02_题目库/研究中/` 移动到 `02_题目库/已解决/`（这是 **legacy / operational 工作流组织**，不等于自动把 YAML `status` 改为 `reviewed`）；
2. 更新研究过程记录中的工作流说明（例如正文中的「已解决」属于研究进度，不是 Problem Object Status）；
3. 同步修改研究过程记录中的路径说明（如有）；
4. 在研究日志中记录人工审核通过、归档日期以及进入知识沉淀阶段；
5. 不修改已经审核通过的数学正文；
6. 仅当本次授权同时要求按 Frozen Problem Schema 提升对象可信状态，且对象已满足 reviewed 条件时，才把 YAML `status` 改为 `reviewed`（含显式 `knowledge` 列表，允许 `[]`）；否则保持原 YAML `status`；
7. 完成以上操作后停止，等待下一步人工指令。

“审核通过，请归档”只授权上述标准归档操作。

它不代表授权：

- 创建或修改正式知识库条目；
- 更新长期记忆；
- 更新数学知识地图；
- 创建下一研究任务；
- 创建 LaTeX 工程；
- 生成 PDF；
- 修改其他无关文件。

以上操作仍需后续单独授权。

### 正式知识沉淀

研究任务完成归档后，如果用户明确要求：

> 开始知识沉淀

才允许根据已经审核通过的研究材料创建或更新正式知识库条目。

知识沉淀时：

1. 不直接复制完整研究稿；
2. 应提炼真正需要长期掌握的数学知识；
3. 应区分研究过程与正式知识；
4. 重要定义、条件和公式不得因压缩而失真；
5. 知识条目完成后应再次等待人工检查。

未经“开始知识沉淀”或其他含义等价的明确授权，不得自动创建正式知识库条目。

### LaTeX正式输出

只有在用户明确要求：

> 生成 LaTeX 讲义

或给出其他含义明确的 LaTeX / PDF 输出指令后，才进入正式排版阶段。

审核通过或完成知识沉淀本身，不自动代表授权生成 LaTeX 或 PDF。

### 未经人工审核不得执行的操作

未经人工审核，不得自动：

- 将研究任务移动到“已解决”；
- 写入正式知识库；
- 删除原始文件；
- 覆盖最终成果；
- 大规模修改长期记忆；
- 将未经验证的数学结论标记为正确。

如果AI发现已有内容可能存在错误，应指出：

1. 具体位置；
2. 为什么有问题；
3. 建议如何修改。

不得直接大规模覆盖原内容而不说明。

---

## 十六、当前阶段原则

当前阶段优先保证：

`简单 → 可运行 → 稳定 → 可验证 → 再自动化`

当前主要使用：

- ChatGPT；
- Cursor；
- Markdown；
- 数学教材；
- Python；
- LaTeX。

暂时不因为追求复杂架构而强行引入：

- 多Agent；
- RAG；
- 向量数据库；
- 大规模API自动化；
- 本地大模型；
- 复杂自动Memory。

Lean形式验证将在基础工作流稳定后逐步加入。

---

## 十七、结构化对象与自动化治理原则

Knowledge、Problem、Attempt、Method、Error Mode 等结构化对象的字段、类型、ID、lifecycle、relation、required / optional、Schema version，以 `元数据规范.md` 为**技术事实源**。本文件不重复维护具体 Schema 表。

`Schema` ≠ `Template` ≠ `Validator` ≠ `Indexer` ≠ `Workflow` ≠ 项目规则 ≠ `AGENTS.md`。

`tools.research_project`（v1.1 + v1.3 文献/审稿扩展）、`tools.modeling`（v1.4）、`tools.figure`（v1.5）、`tools.lean_formalization`（v1.6）、`tools.review`（v1.7）、`tools.artifact_consistency`（v1.8）、`tools.retrieval`（v2.0 Hybrid RRF，无向量库）与 `tools.collaboration`（v2.1 Candidate 多角色）是 Candidate Contract，**不是** Frozen Schema，不得改 Knowledge / Problem / Attempt / Method Schema，不得伪造 Attempt，不得在未说「开始知识沉淀」时创建 Knowledge。Agent 输出一律是 Candidate，不得直接改 Frozen Source。使用指南：`10_提示词/Research_Project_Usage_Guide.md`。PDF/MinerU 未实现。Lean Sidecar 失败不得阻断普通数学路径。

冲突时：Frozen Schema 优先；修下游（模板、Validator、AGENTS、执行说明），不得为了实现方便反向改 Frozen Schema。Schema 一旦 Frozen，普通实现任务不得改 Frozen fields、类型、必填规则、ID 格式、lifecycle、identity contract、关系语义或 sentinel。确需变更须经过 Schema Change Review 与版本演进。

同一结构化事实原则上只人工维护一次。对象 Markdown / YAML 是对象事实源。索引、反向关系、统计、聚合、cache、生成报告、学习派生状态属于 derived data，不得反向成为对象事实源。对象之间优先用稳定 ID 建立正式关系，禁止用文件路径或自然语言标题代替可机读 ID。

**Validate Before Derive：** 存在依赖时按顺序运行，例如 Knowledge Validator → Problem Validator → Attempt Validator → Method Validator → Workspace Indexer / Check。Structured Object 自动化顺序：**Schema Freeze → Validator → Validated Registry → Indexer → Derived State**。禁止未验证 source 直接进入 Derived production pipeline。上游 ERROR 时，下游不得基于不可信 registry 生成看似正常的正式 derived result。自动化模块之间优先通过 Python public API 与 ValidationResult 传递已验证数据，避免 subprocess 串联内部工具，以及重复 discovery / parse / validation。

**Continuous Verification：** 本地 `python -m tools.verification` 是 verification semantics authority；GitHub Actions 只调用同一 contract，且不得自动修复 Source / Generated state（禁止 CI `workspace_indexer sync/rebuild` 与 `latex_build build`）。Weekly scheduled all-verification：DEFERRED。

### Generated Data Contract

能从正式对象、实际文件系统或已验证 registry **重新计算**得到的信息属于 **Derived / Generated Data**，**不是**底层事实源。

Generated 文件必须：**可删除** · **可重建** · **deterministic** · **不得人工维护业务事实** · **不得反向修改 Source**。

人工不应长期手工维护：知识条目数量、Problem 数量、reviewed 数量、各 operational directory 数量、PDF 数量、LaTeX 工程数量、具体文件是否存在、索引列表。这些应由机器生成（如 `python -m tools.workspace_indexer rebuild`）。

`09_长期记忆/自动索引/**` 属于 **generated derived views**，不是 formal source of truth；可删除并完全重建。

### Derived State Rebuildability Contract

Derived Learning State 的正式治理合同（Phase 3E authority：`派生学习状态架构.md`）：

1. Derived / generated state **永远不是** Source of Truth。
2. 必须 **只消费** validated registries 或 validated WorkspaceSnapshot。
3. 同一 validated input + 同一 derivation rule version → **deterministic** 输出。
4. Derived result 必须 **可删除并重建**；无法重建即含 hidden Source State，属架构违规。
5. Derived pipeline **不得反写** Frozen Source（K / P / A / M / 不得创建 Error Mode）。
6. invalid upstream validator ERROR → **abort derived publish → 0 writes**。
7. Generated files（含 future learning-state views）→ **no manual editing**；修正须改 source 或 derivation policy，再 validate → recompute。

### Source Mutation Authority

| 组件 | 权限 |
|------|------|
| Validator / Checker | **read-only** |
| Indexer | 只允许覆盖自己拥有的 GENERATED 输出 |
| Workflow / Creator / Migration Tool | 在明确授权和 contract 下才允许修改 Source |

禁止：Validator auto-fix、Indexer auto-fix、Checker 自动分配 ID、Indexer 自动移动 Problem、Indexer 自动 promotion。普通维护任务不允许隐藏 Source mutation。

Validator、Checker、Auditor 以及 Indexer 的 validation 阶段默认 **read-only**：不得隐式 auto-fix、ID allocation、status promotion、Knowledge creation、Schema mutation 或数学正确性批准。自动写文件只能进入职责明确的 Workflow / Creator / Migration Tool，或用户明确授权的写入任务；禁止把写入藏在 Validator、Indexer 或 Template Contract Test 内部。

**职责：**

* Template：authoring scaffold。与 Frozen Schema 冲突时修模板。
* Formal Validator：Frozen Schema 的机械执行层。不判断证明正确性、mastery、用户是否做对、题目是否「已解决」。
* Indexer：只生成 derived view，不得修改 source Markdown / YAML。
* Workflow：orchestration（在明确设计范围内组织 validate / derive / publish / promote），不得绕过 Validator。未来 Workflow 不得靠读取题目模板并字符串替换来实现 Schema；应依 Frozen Contract 构造 Metadata，再写 Markdown，再跑 Formal Validator。
* 数学内容与对象审核仍须遵守第十五节人工审核授权边界。

### Candidate / Pilot Governance

对于尚未 Frozen 的新对象类型，应区分：**Semantic Contract** → **Candidate Schema** → **Real Pilot** → **Final Review** → **Frozen Schema**。

- **Candidate ≠ Frozen Schema。** Candidate 阶段不得被 Validator、Indexer、Workflow 或其他工具提前硬编码成正式 Frozen Contract。
- **Real Pilot 必须来自真实发生的对象或学习事件。** 不得为了测试 Schema、覆盖场景、补齐统计、维持测试覆盖或推进 Phase 而伪造事实。

### Learning Evidence Governance

Attempt 是 **raw Learning Evidence**，不是 Derived Learning State。

* 真实 Attempt 必须来自**用户**真实学习事件（solving / reasoning Evidence），**不是** AI 代为生成解法。
* **AI-generated Solution ≠ User Attempt。** AI 在 AUTO/RESEARCH 中完整解题 **默认不创建** Attempt。
* 一个 Attempt 只对应 **一个** evaluation target（`problem` + optional `part`）。
* **Reattempt** 创建新 Evidence identity（新 A ID）；**不得**覆盖历史 Attempt 的 outcome。
* **Correction** 保留原 Attempt identity，并保留 auditable correction history（`corrections` append）。
* **Recording** 与 **Assessment** 分离；Recorder **不得**凭空猜测 correctness / outcome。
* **不得**为保护 independent Attempt 而拒绝或延迟用户正常请求的完整答案（STUDY 模式除外）。
* Attempt statistics（correct rate、error rate、mastery、weakness、trend、recommendation 等）全部属于 **Derived**；**不得**反写 source Attempt。
* Attempt Schema v1 正式技术事实源：`元数据规范.md` §8。设计历史见 `学习证据架构.md` §19–§23。
* **Physical storage（Attempt Storage Contract v1）：** 一次真实 Attempt → 分配全局 A ID → **append** 到对应 `11_学习证据/尝试记录/Pxxxx.md` Ledger；**不得**创建 legacy `Axxxxxx.md`。

### Method Governance

Method 是 **Static Structured Object**；Source root：`12_方法库/`。

* Method Schema v1 正式技术事实源：`元数据规范.md` §9（**Frozen**，2026-08-20）。设计历史见 `Method与ErrorMode架构.md` Phase 3D-1–3D-7M。
* Method Validator v1 **已实现**；Validated Method Registry **在 project PASS 时可用**；Workspace integration **COMPLETE**（Workspace Maintenance v1.4 — Method + Descriptive + Knowledge Associated Evidence）。
* 创建 / 修改 Method source 须遵循 Frozen Schema v1；Validator PASS ≠ Content Review / `status: reviewed`。
* Method Object `status: draft` **不**表示 Schema 未 Frozen；Schema maturity 与 Object lifecycle 分离。

### Schema Freeze Propagation Contract

适用于 Attempt、Method、Error Mode 及未来 Structured Object 的长期 Freeze 流程：

```
Candidate Final Review
        ↓
Schema Freeze（迁入 元数据规范.md）
        ↓
Authority Transition（设计文档降级为 history / rationale）
        ↓
Governance Propagation（项目规则.md）
        ↓
Agent Protocol Propagation（AGENTS.md）
        ↓
Milestone Update（项目进度.md）
        ↓
Validator Implementation
        ↓
Validated Registry
        ↓
Derived / Indexer Integration
```

规则：

1. Candidate Freeze 后，Frozen contract **必须**迁入 `元数据规范.md`。
2. 原架构 / 设计文档降级为 design-history / rationale authority，**不再**承担正式字段 technical authority。
3. `AGENTS.md` 只保存高频执行规则 + authority pointer，**不**复制完整 Schema。
4. `项目规则.md` 保存长期治理制度，**不**复制完整字段表。
5. `项目进度.md` 只记录 milestone / phase / next-step，**不是** Schema authority。
6. Validator 完成前，Indexer / Workspace Maintenance **不得**提前把新 Frozen Object 纳入正式 derived pipeline。
7. 正确自动化顺序：**Freeze → Validator → Validated Registry → Indexer → Derived State**。

### Validated Source Integration Contract

一个 Structured Object 类型只有同时满足：

1. Schema Frozen
2. Formal Validator Complete
3. Validated Registry Available

之后，才允许进入正式 **Workspace Indexer**、**Derived Pipeline**、**Generated Statistics**。

正式依赖链：

```
Source
→ Formal Validator
→ Validated Registry
→ Indexer
→ Generated / Derived
```

禁止：

* Raw YAML → Indexer 自己验证
* Indexer → 修复 Source
* Derived Result → 反写 Source

**Safe descriptive derived（v1.4）：** counts、identity navigation、target mapping、raw enum distribution、Descriptive Evidence State v1、Knowledge Associated Evidence Projection v1。

**Interpretive derived（当前禁止进入 Workspace v1.4）：** mastery、weakness、success interpretation、recommendation、review priority、correct rate 等 Learning State 指标。

此合同复用于 Method、Error Mode 及未来 Frozen Object。

### Priority Contract

通用优先级定义（**静态制度**）。**Current Priority / 具体任务排期** 见 `09_长期记忆/项目进度.md`，不在此维护动态任务列表。

| 级别 | 含义 |
| --- | --- |
| **P0** | integrity / blocker / architecture failure |
| **P1** | current critical-path architecture work |
| **P2** | next planned capability |
| **P3** | convenience automation |
| **P4** | experimental / advanced infrastructure |

**说明：** P 级只表达优先级 **类型**，不代替 Phase 授权。进入新 Phase 子步骤仍须明确授权（与 `AGENTS.md`、Phase 制度一致）。

