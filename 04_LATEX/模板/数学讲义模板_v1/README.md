# MATH-AI-LAB 数学讲义模板 v1

Template:
MATH-AI-LAB 数学讲义模板 v1

Compiler:
XeLaTeX

Base class:
ElegantBook v4.7 (GitHub tag `v4.7`, commit `0ff65a8`)

Upstream dependency:
`vendor/ElegantBook-v4.7/elegantbook.cls`

Dependency policy:
Pinned / read-only / no automatic update

Problem projects:
Do NOT contain `elegantbook.cls`

Build dependency:
Injected by P9 via `TEXINPUTS`

Main entry:
- Template master: `main.tex` (this directory only)
- Concrete project: `<project_dir>/<project_dir.name>.tex`

Template version (`v1`) and ElegantBook version (`v4.7`) are independent.
Do not copy `vendor/` into Problem or lecture instance projects.
