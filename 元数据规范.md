# MATH-AI-LAB 元数据规范

**Schema 版本：** v1（对象级进度不一）  
**规范状态：** draft（整体 Metadata Schema 仍在渐进设计中）  
**对象级状态：**  
- Knowledge Schema v1 — **Frozen**（2026-08-19）  
- Problem Schema v1 — **Frozen**（2026-08-19）  
- Attempt Schema v1 — **Frozen**（2026-08-19）  
- Method Schema v1 — **Frozen**（2026-08-20）  
- Error Mode Schema — 待设计  

**文件职责：** 定义结构化数学对象的 Metadata 格式、对象类型、ID 体系、字段规范、合法值、对象引用、验证要求与版本迁移规则  
**最后更新：** 2026-08-20

---

## 1. 设计目标

建立一套可人工维护、可机器验证、可长期演进的 Metadata Schema，使知识条目、数学题目、数学方法与错误模式能够：

* 被稳定引用；
* 被统一索引；
* 被统计与检查；
* 在不破坏正文数学内容的前提下逐步结构化。

Schema v1 的目标是逐步建立四类数学对象的通用结构规则与对象级 Schema，并通过小规模真实对象试点验证设计，不进行未经验证的全库批量迁移。不宣称 Schema v1 已完成；也不立即批量改造现有 Markdown 文件。

---

## 2. 适用范围

本规范适用于 MATH-AI-LAB 中计划结构化的数学对象 Metadata。

### 2.1 当前适用对象

| ID 前缀 | 中文类型 | 英文类型 | `type` 字段值 |
| ----- | ---- | ---------- | --- |
| `K` | 知识条目 | Knowledge | `knowledge` |
| `P` | 数学题目 | Problem | `problem` |
| `A` | 学习尝试记录 | Attempt | `attempt` |
| `M` | 数学方法 | Method | `method` |
| `EM` | 错误模式 | Error Mode | `error_mode` |

### 2.2 当前不适用

以下对象暂不纳入 Schema v1：

* Course
* Theorem（作为独立对象）
* Paper / Book / Person / Source
* Lean 证明对象
* RAG Chunk
* Tag 独立实体

如未来确有需要，通过 Schema 版本升级引入，不在本文件旁平行创建 `_v2` / `_最终版` 等副本。

### 2.3 与其他项目文件的职责边界

| 文件 / 目录 | 职责 |
| --- | --- |
| `项目规则.md` | 项目总体制度 |
| `元数据规范.md` | Metadata 的具体技术规范 |
| `AGENTS.md` | Agent 高频执行规则 |
| `01_知识库/` | 正式知识内容 |
| `02_题目库/` | 正式题目研究内容 |
| `12_方法库/` | 正式 Method 源对象（Frozen Method Schema v1） |
| `09_长期记忆/` | 学习状态、项目阶段与长期记忆 |

本文件不放入 `09_长期记忆/`。

---

## 3. Schema 设计原则

### 3.1 最小必要原则

只加入具有明确长期价值，并确定会被 AI、Python、索引或统计使用的字段。

不为“未来可能用到”提前设计大量字段。

### 3.2 单一事实原则

同一结构化事实原则上只维护一次。

避免同时存在多个含义重复的字段。

### 3.3 稳定引用原则

对象之间优先通过稳定 ID 引用，而不是文件路径或自然语言名称。

推荐：

```yaml
knowledge:
  - K0000
```

不推荐：

```yaml
knowledge:
  - 勒让德变换
```

（上例中的 `K0000` 为 Schema 保留示例编号，不可分配给真实对象；真实引用应使用已正式分配的 ID。）
### 3.4 人工可维护原则

元数据必须可以直接在 Markdown 文件中人工编辑。

Schema v1 不依赖数据库。

### 3.5 可验证原则

字段应尽可能具有：

* 明确类型；
* 明确格式；
* 明确合法值；
* 明确范围。

避免程序无法稳定解释的模糊字段。

### 3.6 向后兼容原则

正式分配的 ID 原则上永久稳定。

改标题、移动文件、修改目录时，不得随意修改已正式分配的 ID。

---

## 4. Metadata 基本格式

### 4.1 YAML Front Matter

结构化对象文件顶部采用 YAML Front Matter：

```yaml
---
schema_version: 1
id: K0000
type: knowledge
title: 示例知识条目
status: draft
created: 2026-08-19
updated: 2026-08-19
---
```

所有采用 Metadata Schema v1 的结构化对象都必须声明 `schema_version: 1`。Schema v1 使用整数 `1`，不使用 `v1`、`"1"`、`"1.0"` 或 `1.0`。

本节示例只是通用结构示意。`K0000` 为 Schema 保留示例编号，不可分配给真实对象。本节不列出对象专属字段。

YAML 之后继续正常 Markdown 正文。

### 4.2 两层分离

* **YAML Metadata**：结构层（身份、状态、关系、可机读属性）
* **Markdown 正文**：数学知识内容层

Metadata **不替代**正文中的：

* 数学直觉；
* 为什么想到；
* 图像解释；
* 定义；
* 定理；
* 推导；
* 证明；
* 例题；
* 易错点；
* 推广。

---

## 5. 对象类型

Schema v1 定义五类结构化对象（含 Learning Evidence 类型 Attempt）。

| `type` | 含义 | 主存放位置（规划） | 当前状态 |
| --- | --- | --- | --- |
| `knowledge` | 正式知识条目 | `01_知识库/` | Knowledge Schema v1 — Frozen（2026-08-19） |
| `problem` | 数学题目 / 研究任务 | `02_题目库/` | Problem Schema v1 — Frozen（2026-08-19） |
| `attempt` | 学习尝试记录（Evidence） | `11_学习证据/尝试记录/` | Attempt Schema v1 — Frozen（2026-08-19） |
| `method` | 可复用数学方法 | 待定（后续设计） | Schema 待设计 |
| `error_mode` | 可重复错误模式 | 待定（后续设计） | Schema 待设计 |

说明：

* 存放位置属于实施规划，不改变当前目录职责；
* 本轮不新建目录体系；
* 本轮不为现有文件批量添加 YAML。

---

## 6. Knowledge Schema

**Knowledge Schema 版本：** v1  
**状态：** Frozen（已冻结）  
**冻结日期：** 2026-08-19  
**对象类型：** `type: knowledge`  
**对象 `schema_version`：** 整数 `1`（对应本冻结版）  
**ID 格式：** `K` + 4 位数字（匹配 `^K\d{4}$`；真实对象从 `K0001` 起，`K0000` 为规范保留编号）

### 冻结含义

「冻结」表示：

1. Knowledge Schema v1 已通过真实对象验证；
2. 当前字段结构与验证语义视为稳定基线；
3. 后续普通内容录入不得随意修改 v1 Schema；
4. 模板、validator、索引等实施工作应基于该冻结版本；
5. 新需求应先作为 Future Enhancement 评估，而不是直接改写 v1。

冻结后允许：错别字修正、不改变语义的表达澄清、示例与实施说明补充。

冻结后不得在仍称为 Knowledge Schema v1 的情况下随意：删除/改语义/改类型字段、改 reviewed 必填规则、改 ID 规则、改关系方向、改 target status 约束、改 DAG 语义。若需语义变更，应进入新的 Schema 变更流程。

---

### 6.1 定位

Knowledge 对象用于表示具有独立长期学习价值的数学知识单元，例如：

* 数学概念；
* 定义；
* 定理；
* 数学变换；
* 理论框架；
* 数学专题。

Schema v1 **不**将 `theorem` / `definition` / `concept` / `transform` / `formula` 拆成独立对象类型。上述内容统一使用：

```yaml
type: knowledge
```

Knowledge Metadata 主要回答：

1. 这个知识对象是什么？
2. 它与其他知识对象有什么结构化关系？

Knowledge Metadata **不负责**记录：

* 用户掌握程度、复习计划、错题次数、学习优先级；
* 完整数学证明、数学直觉、为什么想到、例题详细解答（这些属于 Markdown 正文）。

---

### 6.2 字段体系总览

采用三层字段体系。

#### A. 始终必填字段

```yaml
schema_version:
id:
type:
title:
status:
created:
updated:
```

#### B. 推荐结构字段

```yaml
aliases:
domain:
prerequisites:
related:
```

说明：

* `domain`：`draft` 可省略；`reviewed` 必须存在且为**非空字符串**（见第 6.10 节）。Schema v1 **不**强制受控词表。
* `aliases` / `prerequisites` / `related`：
  * `draft` 可以完全省略；若写出，则必须为合法 YAML 数组，无内容时写 `[]`，不得写成空键；
  * `reviewed` 时这三个字段全部必须存在；无内容时必须显式写 `[]`。
* `archived` 保留原对象必要结构与历史引用，不因归档自动删除这些字段。

#### C. Schema v1 明确不加入的字段

不要加入：

```text
difficulty
mastery
review_priority
last_reviewed
mistake_count
source_problems
used_in_problems
methods
tags
keywords
related_domains
secondary_domains
citations
references
lean_status
rag_id
embedding_id
vector_id
```

---

### 6.3 `schema_version`

```yaml
schema_version: 1
```

规则：

* Schema v1 固定使用整数 `1`；
* 不使用 `v1`、`"1.0"`、字符串 `"1"`；
* 用于未来 Schema 升级兼容。

---

### 6.4 `id`

Knowledge ID 形如真实对象编号：

```text
K0001
```

必须匹配：

```text
^K\d{4}$
```

规则：

* 全项目唯一；
* 正式分配后原则上永久不变；
* 改标题、改文件名、移动目录均不改 ID；
* `archived` 后不回收旧编号；
* ID 表示对象身份；文件名和路径不表示永久身份；
* `K0000` 为规范保留编号，格式合法但不得正式分配；真实对象从 `K0001` 起。
---

### 6.5 `type`

Knowledge 固定：

```yaml
type: knowledge
```

不得使用：

```yaml
type: theorem
type: definition
type: concept
type: transform
type: 知识
```

---

### 6.6 `title`

示例：

```yaml
title: 勒让德变换
```

规则：

* 必填、非空；
* 使用自然、正式的人类可读名称；
* 标题不承担唯一身份职责；唯一身份由 `id` 提供。

---

### 6.7 `aliases`

示例：

```yaml
aliases:
  - Legendre transform
```

用途：英文名、常用异名、稳定缩写、常见替代名称。

没有别名时：

```yaml
aliases: []
```

规则：

* 不重复 `title`；
* 不允许重复元素；
* 不使用 aliases 堆砌关键词；
* 不放完整自然语言解释。

---

### 6.8 `status`

Knowledge Schema v1 **只允许**：

```text
draft
reviewed
archived
```

不使用一套全局枚举中的其他状态；本类型合法状态仅以上三者。

#### draft

* 内容仍可继续整理；
* Metadata 允许尚未完全完善；
* 某些关系可以暂为空；
* 不作为完全可靠的正式知识来源。

#### reviewed

* 正文已经完成人工审核；
* Metadata 满足正式要求；
* 可以作为稳定知识对象；
* 可以被其他对象正式引用；
* 可以进入正式索引。

#### archived

* 对象继续保留；
* 不再作为当前推荐知识对象；
* 原 ID 永久保留；
* 历史引用仍然有效。

推荐生命周期：

```text
draft → reviewed → archived
```

不要加入：`working` / `researching` / `reviewing` / `complete` / `finished` / `published` / `stable`。

---

### 6.9 `created` 与 `updated`

```yaml
created: 2026-08-19
updated: 2026-08-19
```

格式统一为 `YYYY-MM-DD`。

#### created

对象第一次正式建立的日期；原则上创建后不修改。

#### updated

以下情况更新：

* 数学正文发生实质变化；
* Metadata 关系发生实质变化；
* 重要错误被修正；
* `status` 发生变化。

仅空格、排版、无语义格式修正时，不要求强制修改 `updated`。

必须满足：

```text
updated >= created
```

---

### 6.10 `domain`

使用单一主领域，类型为 **string**：

```yaml
domain: 凸分析
```

不要使用：`primary_domain` / `related_domains` / `secondary_domains`。

#### Schema v1 可验证规则

* `draft`：允许省略 `domain`；不要写空键 `domain:`。
* `reviewed`：`domain` 必须存在；类型必须为 string；`trim` 后不得为空字符串。
* 非法示例：`domain:`（空键）、`domain: ""`。
* Schema v1 **不**强制 `domain` 来自受控词表；validator **不**检查词表成员资格。

#### Future Enhancement

`domain` 受控词表（controlled vocabulary）属于 Future Schema Enhancement，可在 v1.1 或后续版本设计。Schema v1 冻结**不**依赖完整领域词表。

跨领域关系不通过多个 domain 字段扩张；其他关联通过 `related` 或正文表达。

---

### 6.11 `prerequisites`

可解析 YAML 字段格式示意（编号仅为格式演示，不表示仓库实际对象）：

```yaml
prerequisites:
  - K0002
  - K0017
```

含义：理解当前知识之前真正需要掌握的**直接**前置 Knowledge。

规则：

* 只允许 Knowledge ID，且符合 `^K\d{4}$`；
* 数字部分不得为 `0000`（保留编号不可作为真实引用目标）；
* 不允许自身 ID；
* 不允许重复元素；
* 不允许与 `related` 重复；
* 引用目标的状态约束见第 6.14–6.15 节（source / target）。

#### 只记录直接 prerequisite

若依赖链为（抽象关系说明，不表示仓库实际对象状态）：

```text
Knowledge A → Knowledge B → Knowledge C
```

且 A 直接依赖 B、B 直接依赖 C，则 A 原则上只记录对 B 的直接引用，结构示意：

```yaml
prerequisites:
  - K0002
```

不要为“完整”再重复加入 C 对应的间接前置。间接 prerequisite 由未来知识图谱程序根据传递关系推导。

#### prerequisite 图必须无环（DAG）

`prerequisites` 表示学习依赖关系。全体 Knowledge 的 prerequisite 引用边必须构成**有向无环图（DAG）**：

* 禁止任意长度的依赖环，例如 `K0001 → K0002 → K0001`，或更长环；
* 原因：若 A 理解前必须先理解 B，而 B 经若干节点又要求先理解 A，则学习前置逻辑矛盾；
* 自引用（指向自身 ID）亦禁止，见上文；自引用是环的一种特例。

未来 validator 应检查：`prerequisite_cycle == false`。本规范只规定语义约束，不规定具体算法（DFS / 拓扑排序等属 Post-Freeze Implementation）。

无前置时：

```yaml
prerequisites: []
```

---

### 6.12 `related`

可解析 YAML 字段格式示意（编号仅为格式演示，不表示仓库实际对象）：

```yaml
related:
  - K0012
```

含义：与当前 Knowledge 有明确学习价值的**强关联**，但不是直接 prerequisite。

规则：

* 只允许 Knowledge ID；
* 数字部分不得为 `0000`；
* 不允许自身 ID；
* 不允许重复元素；
* 不与 `prerequisites` 重复；
* 只记录强关联，不堆砌可能相关的知识；
* 引用目标的状态约束见第 6.14–6.15 节（与 `prerequisites` 相同）。

`related` 在语义上视为对称关系。人工只维护一侧；不要求对侧再手工回写。未来自动索引程序将其视为双向关系。

#### related 允许成环

`related` **不受** prerequisite 无环规则约束：

* 关联网络可以成环（例如 A related B、B related C、C related A 合法）；
* cycle detection **只针对** `prerequisites`，**不针对** `related`；
* 但仍禁止自引用（`related` 不得包含自身 ID）。

无关联时：

```yaml
related: []
```

`related: []` 与 `prerequisites: []` 均为合法；reviewed 要求字段存在，**不**要求非空。

---

### 6.13 权威引用原则（Knowledge）

人工只维护权威方向；能够确定推导出的反向关系由未来程序生成。

Knowledge 自己维护：

```text
domain
prerequisites
related
```

Problem 未来自己维护：

```text
knowledge
```

因此 Problem → Knowledge 的引用由 Problem 负责。Knowledge **不**手工维护：

```yaml
source_problems:
used_in_problems:
```

未来程序通过扫描 Problem 的 `knowledge:` 自动计算哪些 Problem 引用了某个 Knowledge。

原则：对象自身固有关系由对象维护；可从另一侧确定推导的反向关系不人工重复维护。

---

### 6.14 引用目标状态约束（source / target）

术语：

* **source object**：当前正在验证的 Knowledge（写出 `prerequisites` / `related` 的一方）；
* **target object**：`prerequisites` / `related` 中被引用的 Knowledge。

例如：

```yaml
# K0001
prerequisites:
  - K0002
```

则 `K0001` 为 source，`K0002` 为 target。

#### draft source

draft Knowledge 的 `prerequisites` / `related` 允许引用 status 为 `draft` 或 `reviewed` 的 Knowledge。

* target 为 `reviewed`：通过；
* target 为 `draft`：允许，但未来验证程序应给出 **warning**（关系可在构建期提前建立，目标尚未稳定）；
* target 不存在：按悬空引用规则处理（一般为 **error**，除非另有可容忍策略）。

示意：`A(draft) → B(draft)` 允许，但 warning。

#### reviewed source

Knowledge 进入 `status: reviewed` 时，其 `prerequisites` / `related` 中每一个 target 必须同时满足：

1. 真实存在；
2. `type == knowledge`；
3. `status == reviewed`。

reviewed Knowledge **不能**正式依赖 draft Knowledge。若任一 target 仍为 `draft`（或为 `archived`），则 reviewed 验证失败。

#### archived

* 保留原 ID 与已有历史引用；
* 不因归档自动删除关系；
* 仍可被程序解析。

Schema v1 不建议新的 reviewed Knowledge 将 archived Knowledge 作为当前 `prerequisites` / `related` 目标；reviewed 晋升验证要求 target `status == reviewed`。本轮不设计 deprecated / replacement 机制。

---

### 6.15 状态感知验证（status-aware validation）

Knowledge Schema v1 采用 **status-aware validation**：不同状态的验证严格程度不同。

#### Draft 最小要求

draft 必须具有：

```text
schema_version
id
type
title
status
created
updated
```

draft 可以：

* 暂无 `domain`；
* 完全省略 `aliases` / `prerequisites` / `related`；
* 若写出上述列表字段，则必须为合法数组（无内容时写 `[]`）；
* 某些引用关系尚未建立。

若 draft source 写出关系字段：

* target `status == reviewed` → 正常；
* target `status == draft` → **warning**；
* target 不存在 → **error**（或沿用悬空引用策略）。

未来验证程序对 draft 的缺失推荐字段可以给出 `warning`，而不必全部判为 `error`。

#### Reviewed 正式要求

进入 `reviewed` 前必须满足：

* `schema_version == 1`；
* `id` 格式合法；
* `type` 固定为 `knowledge`；
* `title` 非空；
* `status` 合法；
* `created` / `updated` 合法，且 `updated >= created`；
* `domain` 已确定：必须存在、类型为 string、`trim` 后非空（Schema v1 不验证受控词表）；
* `aliases` / `prerequisites` / `related` 全部必须存在且为数组（无内容时写 `[]`；**允许空数组**，不要求非空关系）；
* `aliases` / `prerequisites` / `related` 无重复；
* `prerequisites` 与 `related` 不含自身、无交集；
* 所有 `prerequisites` / `related` 引用真实存在，且每个 target 满足 `type == knowledge` 且 `status == reviewed`；
* 全体 Knowledge 的 `prerequisites` 图无环（DAG）；
* 正文已经完成人工审核。

只有满足 reviewed 要求的对象才能作为正式稳定知识源。

#### Archived

* 保留原 ID 与必要 Metadata（含已有列表字段与历史引用）；
* 不因归档自动删除 `aliases` / `prerequisites` / `related` 等字段；
* 不回收编号；
* 仍允许历史对象引用；
* 仍可被程序解析。

---

### 6.16 列表字段规则

Knowledge 列表字段：`aliases`、`prerequisites`、`related`。

#### draft

可以完全省略上述字段。若写出，则：

* 必须为合法 YAML 数组；
* 无内容时必须写 `[]`；
* 不允许写成空键：

```yaml
aliases:
prerequisites:
related:
```

#### reviewed

上述三个字段全部必须存在。无内容时必须明确写：

```yaml
aliases: []
prerequisites: []
related: []
```

允许空数组：基础 Knowledge 可以没有前置；没有合适 related 时 `related: []` 合法。**不**要求非空关系。

#### archived

保留原对象必要结构与历史引用，不因归档自动删除这些字段。

另：不允许重复、不允许空字符串；ID 数组元素必须符合对应 ID 正则。

---

### 6.17 推荐字段顺序

为便于人工阅读、编辑、Git diff 与未来自动格式化，推荐统一顺序：

```yaml
---
schema_version: 1

id: K0000
type: knowledge
title: 示例知识条目
aliases: []

status: draft
created: 2026-08-19
updated: 2026-08-19

domain: 示例领域

prerequisites: []
related: []
---
```

以上为 Schema 字段顺序与结构示意。`K0000` 为规范保留示例编号，不可分配给真实对象。

---

### 6.18 最小 Draft 示例

```yaml
---
schema_version: 1

id: K0000
type: knowledge
title: 示例知识条目
aliases: []

status: draft
created: 2026-08-19
updated: 2026-08-19

prerequisites: []
related: []
---
```

> `K0000` 为 Schema 保留示例编号，不可分配给真实对象。

---

### 6.19 Reviewed 示例

```yaml
---
schema_version: 1

id: K0000
type: knowledge
title: 示例知识条目
aliases:
  - Example knowledge

status: reviewed
created: 2026-08-18
updated: 2026-08-19

domain: 示例领域

prerequisites: []
related: []
---
```

> 这是 Schema 格式展示，不代表任何真实 Knowledge 对象的当前状态。真实对象 `K0001`（勒让德变换）仍以实际知识文件中的 Metadata 为准。
---

### 6.20 明确排除的内容类别

#### 个人学习数据

```text
mastery
review_priority
last_reviewed
mistake_count
```

属于未来学习系统，不属于 Knowledge Metadata v1。

#### Problem 反向关系

```text
source_problems
used_in_problems
```

由 Problem → Knowledge 引用自动推导，不在 Knowledge 上手写。

#### 数学正文内容

```text
为什么想到
数学直觉
完整证明
详细推导
例题分析
易错点
推广
```

放在 Markdown 正文。

#### 尚未成熟的系统字段

```text
embedding_id
rag_id
vector_id
lean_status
```

Schema v1 不加入。

---

### 6.21 机器验证摘要

未来程序至少应检查：

```text
schema_version == 1

id matches ^K\d{4}$

type == knowledge

title 非空

status ∈ { draft, reviewed, archived }

created 合法
updated 合法
updated >= created

aliases 无重复

prerequisites:
  仅含 K-ID
  无重复
  不含自身

related:
  仅含 K-ID
  无重复
  不含自身

prerequisites ∩ related = ∅

prerequisite graph acyclic (DAG)
  （仅 prerequisites；related 允许成环）
```

引用目标状态（source / target）：

```text
For source.status == draft:
  target.status == reviewed → pass
  target.status == draft → warning
  target 不存在 → error（或悬空引用策略）

For source.status == reviewed:
  all prerequisite targets:
    type == knowledge
    status == reviewed
  all related targets:
    type == knowledge
    status == reviewed
```

当 `status == reviewed` 时额外检查：

```text
domain 必须存在
domain 为 string
trim(domain) != ""
（v1 不检查 domain ∈ 受控词表）

aliases 必须存在且为数组
prerequisites 必须存在且为数组（可为 []）
related 必须存在且为数组（可为 []）
所有 prerequisites 引用必须存在
所有 related 引用必须存在
所有 relationship targets 的 status == reviewed
prerequisite_cycle == false
```

---

### 6.22 Knowledge Schema 核心原则

> **ID 管身份。**

> **Metadata 管结构。**

> **Markdown 管数学内容。**

> **Knowledge 管知识本身，不管理个人学习状态。**

> **人工只维护权威引用，反向关系由程序生成。**

> **只记录直接 prerequisite，不人工维护可推导出的传递关系。**

> **prerequisites 构成 DAG（无环）；related 允许成环；二者均禁止自引用。**

---

## 7. Problem Schema

**Problem Schema 版本：** v1  
**状态：** Frozen（已冻结）  
**冻结日期：** 2026-08-19  
**对象类型：** `type: problem`  
**对象 `schema_version`：** 整数 `1`（对应本冻结版）  
**ID 格式：** `P` + 4 位数字（匹配 `^P\d{4}$`；真实对象从 `P0001` 起，`P0000` 为规范保留编号）

**已分配真实 ID：** `P0001`、`P0002`。

### 冻结含义

「冻结」表示：

1. Problem Schema v1 已通过 Architecture Review、双真实 Pilot、受控迁移、Candidate Gate 与 Final Review；
2. 当前字段结构与验证语义视为稳定基线；
3. 后续普通内容录入不得随意修改 v1 Schema；
4. 模板、validator、索引等实施工作应基于该冻结版本；
5. 新需求应先作为 Future Enhancement / 后续 Schema 版本评估，而不是直接改写 v1。

冻结后允许：错别字修正、不改变语义的表达澄清、示例与实施说明补充、implementation note、Validator 实现细节、非语义文档格式优化。

冻结后不得在仍称为 Problem Schema v1 的情况下随意：新增/删除 Frozen 字段、改字段类型、改 required/optional 语义、改 status 枚举、改 Identity Contract、改 `knowledge` 关系语义、改 reviewed `knowledge: []` 语义、改 `parts` 核心语义、改 ID 格式、改 `P0000` sentinel 规则。若需语义变更，应进入 Schema Change Review，并进入 Problem Schema v1.1 / v2 或项目现有版本治理。

### Frozen Baseline（2026-08-19）

* Schema：Problem Schema v1；`schema_version` 整数 `1`
* Frozen 字段（9）：`schema_version`、`id`、`type`、`title`、`status`、`created`、`updated`、`knowledge`、`parts`
* `status` enum：`draft` / `reviewed` / `archived`
* ID：`^P\d{4}$`；sentinel `P0000`
* 关系：Problem → directly engaged Knowledge（人工 Source of Truth；反向派生）
* multipart：optional 稳定 local `parts`
* Research / Ordinary 统一 `type: problem`

Hard Freeze Blockers：**0**（Resolved Before Freeze）。

---

### 7.1 定位

Problem 是 **Static Mathematical Object**：一个具有稳定数学语义身份的数学任务。

可以是普通习题、证明题、计算题、构造题、开放问题、研究任务、数学探索任务。

Research Problem 与 Ordinary Exercise **统一**为 `type: problem`。v1 不创建 `exercise` / `research_problem`。两真实 Pilot（`P0001` / `P0002`）构成最小稳定 v1 基础；未覆盖题型可通过后续版本扩展。

Problem 回答：**这个数学任务是什么？**

Problem **不是**：一次作答、做题记录、用户成绩、mastery、错误次数、复习状态、个人难度、recommendation。这些属于 Attempt 或 Derived Learning State（见 `学习证据架构.md`）。

YAML 只承担 Identity + Lifecycle + 可机读关系 + 稳定 multipart anchors。长数学内容留在 Markdown。Attempt 是学习证据。Derived State 是学习分析。禁止混层。

---

### 7.2 Problem Identity Contract

`id` 表示一个稳定数学任务身份。权威来源是 YAML Metadata，**不是**文件名、**不是**目录。

**最高判断标准：** 修改之后，历史 Attempt 是否仍然是在回答同一个数学任务？

是 → 保留原 P ID。  
否 → 创建新 P ID（新对象），而不是只改 `updated`。

**禁止**把 Problem identity 设计成题面文本 hash。数学上等价的排版、符号、表达，不因文字不同自动变成新 Problem。

#### 可保留原 P ID（不改变数学任务语义）

修正 typo / LaTeX / 排版；补充说明、解释、参考资料、标准解答；修正已有解答中的非题面错误；改进证明表达；增加图示或数学背景；术语统一；把原本隐含且不改变任务语义的语境写清楚（例如仅澄清公共题设的作用空间表述，且不改变原题实际数学任务）。

#### 必须创建新 P ID（改变数学任务本身）

改关键假设、核心条件、目标、要求证明的结论；改定义域且影响问题性质；改参数范围并改变数学性质；从「证明凸」改为「证明严格凸」；扩展成数学意义不同的问题；创建真正意义不同的变式题；把「无公共特征值」改成实质不同的「无公共实特征值」等改变任务语义的条件。

#### 等价表述

目标改成数学上可能等价的另一种表达（例如有限维同维下「单射」与「可逆」）时，**不能只按文本不同机械拆分**。仍以「历史 Attempt 是否在回答同一个数学任务」为最高判断。

#### 路径与文件名

`研究中/` → `已解决/` 只是文件组织 / 工作流变化，**不更换 P ID**。

文件名可以含标题摘要，但不能与 YAML `id` 形成两个独立事实源。`P0001` 当前文件名仍为 `P001_...`：**Legacy Filename Cleanup** = NON-BLOCKING FUTURE WORK。YAML `id` 为权威。

#### multipart 某一小问实质改变

因 v1 无 child P ID：若已有历史 Attempt，且某一 part 的数学任务发生实质变化，应倾向 **创建新 P ID**，而不是偷偷改原对象。最高原则仍是：历史 Attempt 是否仍然准确代表对当前任务的作答。

---

### 7.3 三种状态必须分离

| 层次 | 含义 | 权威 |
| --- | --- | --- |
| Problem Object Status | 对象生命周期与可信状态 | YAML `status`：`draft` / `reviewed` / `archived` |
| Problem Solving / Research Workflow State | 历史求解进度 | 当前目录 `未解决/` / `研究中/` / `已解决/`（Legacy，**不等于** Object Status） |
| Attempt Result | 某一次作答结果 | 未来 Attempt（本轮不设计字段/枚举） |

例如：`status: reviewed` 的 Problem 可以从未被 Attempt，也可以第一次 Attempt 为 incorrect。`reviewed` **不等于**「用户做对」，也 **不等于**「研究已经解决」。

Pilot A 目录「已解决」与正文「状态：已解决」是 **Legacy Workflow State**，不是 Object Status。Pilot B 即使用户当前还不会做，也可以是 `reviewed` Problem。

Attempt 结果（correct / incorrect / partial / hint-assisted 等）**绝不能**放入 `Problem.status`，也不得写成 `part_a_result` 等 Problem 字段。Attempt 枚举不属于 Problem Schema v1。

---

### 7.4 生命周期（Frozen）

#### draft

数学身份、题面、Metadata、Knowledge mapping 仍可能调整。允许继续编辑。

#### reviewed

作为数学对象已经过审核：任务身份可信、**数学任务陈述足够明确且经人工/AI 数学审核**、结构化 Metadata 已审核、Knowledge mapping 已审核到当前可接受程度。若正文已有解答，已有解答应当可信。

**不要求**所有 Problem 必须存在完整标准答案。Research / Open Problem 可以没有传统标准答案；Ordinary Problem 也可以先 reviewed 再补解答。Phase 3B-2 双 Pilot **无反例**。

`reviewed` 不因 Attempt 对错而自动变为 `draft` 或 `archived`。Problem 生命周期与学习表现解耦。`P0002` 的 YAML `status: draft` 是因为 Content Review Pending，**不是**「用户没做出来」；**operational workflow 目录**（`未解决/` / `研究中/` / `已解决/`）由 filesystem 决定，与 YAML `status` 独立。Problem Schema Frozen 时 `P0002` 可以仍为 `draft`。

未来 Problem Validator **只能**验证 Metadata，**不能**自动证明题面数学正确；题面可信依赖人工/AI 数学审核。

#### created

`created` 表示 **当前结构化 Problem 对象进入正式 registry 的创建日期**。

* 新对象：就是结构化对象创建日期。
* legacy 对象：若无法可靠恢复结构化对象创建日期，使用 **structured registration date**（`P0001`：`2026-08-19`）。此为 Frozen v1 legacy migration policy。
* **不是**题目第一次在教材/考试中出现的日期，也不是 source chronology。例如 2019 年考题 ≠ `created: 2019-...`。
* 与 Knowledge `created`（「对象第一次正式建立的日期」）**兼容**，无 SEMANTIC CONFLICT。不修改 Frozen Knowledge Schema。

历史来源日期 / 归档日期保留于正文或未来 `source`，不伪造成 `created`。

#### archived

历史身份保留；不再作为常规活跃 Problem；P ID 不回收。不等于「用户已经做完」或「研究已经解决」。

本轮不增加 `replacement` / `deprecated_by` / `superseded_by`。复杂归档迁移 DEFER。

---

### 7.5 字段归属（Frozen v1）

| 等级 | 含义 |
| --- | --- |
| FROZEN V1 | Problem Schema v1 正式字段 |
| DEFERRED | 不属于 Problem v1；可通过后续 Schema 版本重新评估（≠ 永久禁止） |
| EXCLUDED FROM V1 | 禁止进入 Problem v1（部分属于 Attempt / Derived State / 其他对象） |

---

### 7.6 Frozen 字段表

| 字段 | Frozen | 类型 | required | draft | reviewed | 语义 |
| --- | --- | --- | --- | --- | --- | --- |
| `schema_version` | Frozen | integer `1` | required | 必填 | 必填 | 对象 Schema 版本 |
| `id` | Frozen | string `^P\d{4}$` | required | 必填 | 必填 | 稳定身份；真实对象不得 `P0000`；不因移动/重命名/作答/工作流改变 |
| `type` | Frozen | `problem` | required | 必填 | 必填 | 固定；Research / Ordinary 共用 |
| `title` | Frozen | 非空 string（trim） | required | 必填 | 必填 | 人类识别；完整题面不放 title |
| `status` | Frozen | `draft`/`reviewed`/`archived` | required | 必填 | 必填 | Object Status，非 Attempt result，非工作流目录 |
| `created` | Frozen | `YYYY-MM-DD` | required | 必填 | 必填 | 结构化对象进入正式 registry 的日期；不是 source chronology |
| `updated` | Frozen | `YYYY-MM-DD` | required | 必填 | 必填 | 实质性对象修改；须 `updated >= created`。identity 改变须新 P ID |
| `knowledge` | Frozen | list of `^K\d{4}$` | lifecycle-dependent | optional；若存在须合法 list | **required**；`[]` 合法（Frozen v1 rule） | directly engaged Knowledge |
| `parts` | Frozen | list of string | optional | 可省略；若存在至少 2 项 | 同左 | Problem-local stable multipart anchors |

archived：对象仍须可解析；P ID 保留、不回收。不增加 `replacement` / `deprecated_by` / `superseded_by`。

未知 YAML 字段 **不属于** Problem Schema v1。工具可 WARNING（Implementation Policy）；不得把 unknown field 自动视为正式 v1 字段。

**Attempt Compatibility：** 同一 Problem 的多次 Attempt **不得**要求修改 Problem 动态状态。Attempt 依赖 **Problem ID + optional part anchor**（Frozen Attempt Schema v1，§8）。

---

### 7.7 `knowledge`（Frozen v1）

`knowledge` 是 Frozen v1 的 Problem → Knowledge 机器关系，类型 `list[str]`（每项 `^K\d{4}$`）。

**reviewed `knowledge: []`：** Frozen v1 rule。mapping 已审核，但当前正式 reviewed Knowledge inventory 没有合适的 directly engaged target。**不是** mapping 尚未完成。

表示该 Problem **directly engaged Knowledge**：直接涉及、直接考察、直接调用、或直接要求理解的 Knowledge。不要写成「所有相关 Knowledge」。

判定：该 Knowledge 在数学任务或高质量解答中被直接调用、识别、操作、证明、分析或推理。

**不记录 Knowledge 传递祖先。** Problem 不重复维护 transitive prerequisites；那些关系由 Knowledge prerequisite graph 表达。

不新增 `target_knowledge` / `supporting_knowledge` / `primary_knowledge` / `secondary_knowledge`。v1 保持单一 `knowledge` 关系。后续引入须 Schema 版本变更。

`Problem.knowledge` **只表示数学关系**，不等于 mastery 证据权重。不能由 `knowledge: [K0001, K0002]` 直接推出各 Knowledge 的 mastery 增量。Evidence 强度由未来 Attempt + Learning State Engine 判断。

#### draft

`knowledge` 可以省略。若存在：必须为合法 YAML 数组。

#### reviewed

`knowledge` **必须显式存在**为 list。**`knowledge: []` 合法**（Frozen v1 rule）。

区分两种空数组：

| 概念 | 正确处理 |
| --- | --- |
| Mapping not done（还没做映射） | **不要**用 `[]`；保持 `draft`，**省略** `knowledge` |
| Mapping reviewed，但当前 Knowledge inventory 没有适合正式绑定的对象 | **允许** `[]` |

`reviewed` + `knowledge: []` 精确语义：Knowledge mapping **已经**人工/AI 审核，但当前正式 reviewed Knowledge inventory 没有适合的 directly engaged target。

**Phase 3B-3：** `P0002` 为 `draft` 且 **省略** `knowledge`（mapping 与 Content Review 均未完成）。不得用 `[]` 伪装完成。未来新增线性代数 Knowledge 后可更新 `P0002.knowledge` 并更新 `updated`，**不改变** P0002 identity。

#### 列表约束（字段存在时）

* 每项 string，匹配 `^K\d{4}$`
* 禁止 `K0000`
* 禁止重复
* target 必须真实存在
* target `type == knowledge`
* 不得指向 P / M / EM

#### target status（Frozen v1）

* Problem `reviewed` 且 `knowledge` 非空：每个 target 必须 `status == reviewed`。reviewed Problem 不应对未审核 Knowledge 建立正式机器关系。archived Knowledge **不是** reviewed target。
* Problem `draft`：target 可为 `reviewed` 或 `draft`。draft → draft 允许；正式 Validator 可考虑 WARNING。

#### 权威方向

Problem YAML 是 **Problem → Knowledge** 的人工 Source of Truth。Knowledge 侧「哪些 Problem 使用某 K」由未来派生系统生成。

**禁止**给 Frozen Knowledge Schema v1 增加 `used_in_problems` / `source_problems` / `problems` / `assessed_by`。

Problem → Knowledge **没有** Problem prerequisite DAG。不要复制 Knowledge 的 DAG 规则。

---

### 7.8 Deferred（不属于 Problem Schema v1）

Deferred **≠** 永久禁止。未来只能通过后续 Schema 版本重新评估，不能悄悄写入 Frozen v1 field set。

| 字段 | v1 | 原因 |
| --- | --- | --- |
| `domain` | DEFER | 可由 Problem → Knowledge → domain 派生；避免双事实源 |
| `source` | DEFER | 来源模型不成熟；不影响 identity；不是 source 年代塞进 `created` |
| `problem_type` | DEFER | 题型词表多重、歧义、未稳定 |
| `aliases` | DEFER | 无足够真实需求 |
| `methods` | DEFER | 等待 Method Schema |

---

### 7.9 Excluded from Problem v1

禁止进入 Problem v1（不等于整个项目永久禁止；部分可在 Attempt、Derived State 或其他对象/后续版本出现）：

`result`、`correct`、`incorrect`、`mastery`、`success_rate`、`mistake_count`、`last_attempt`、`last_reviewed`、`review_due`、`personal_difficulty`、`time_spent`、`hint_used`、`confidence`、`intrinsic_difficulty`、`tags`、`attempts`、`error_modes`

以及任何 `part_result` / `part_score` / `part_correct` / `parent_problem` / `subproblems` / `children` / `part_ids` / `child_ids` / `part_problem_ids`。

`parts` **不是** Excluded：它是 Frozen v1 optional 字段（仅 token list）。

* Attempt 类与 Derived State 类：见 `学习证据架构.md`。
* `intrinsic_difficulty`：当前无真实数据需求；personal_difficulty 明确为 Derived。
* `tags`：避免无治理垃圾桶。
* `error_modes`：Error Mode Schema 未设计；正文「常见陷阱」≠ Attempt occurrence。
* `attempts`：反向关系从 Attempt evidence 派生，禁止双向人工维护。

---

### 7.10 Markdown 职责

完整题面、背景、假设、条件、目标、思路、解答、证明、推导、验证、常见陷阱、变式、研究说明：留在 Markdown。不要放入 YAML 长字段。

推荐正文结构（**不是 Schema 强制章节**）：题目；条件与目标；思路；解答/推导；验证；数学联系；常见陷阱；变式。Research Problem 可以没有标准解答；普通计算题可以没有研究日志。Schema 管机器结构；正文模板管数学表达。

当前 P0001 中的「候选思路 / 最终思路 / 研究日志」属于研究工作过程 / 文档历史，**不能**当成用户 Attempt history。

题面中的 domain / codomain / scalar field 等数学陈述完整性问题，优先在 Markdown 中澄清；**不要**因此新增 `scalar_field` / `matrix_space` 等 YAML 字段。

---

### 7.11 Multipart 与 `parts`（Frozen v1；optional）

`parts` 是 Frozen v1 字段：optional Problem-local stable multipart anchors。用于在同一 Problem identity 内稳定引用多个数学子任务。

`P0002` 作为一个 composite Problem（`parts: [a, b, c]`）合法：共享公共题设。不拆成独立 P0002a/b/c。v1 **保持扁平**，不包含 `parent_problem` / `child_problem` / `children` / `subproblem_ids` / `part_problem_ids`。

`parts` 不是独立 P ID、不是 Attempt result、不是 hierarchy。单一 part 不使用 `parts` 字段。

#### 类型与约束

* 类型：**仅** `list[str]`。不把 int 加入字段类型。
* 每项 trim 后非空；当前 Problem 内唯一；stable；**history-sensitive**。
* 字段 **optional**。不存在合法。
* 若存在：至少 **2** 项（单一 part 无 multipart 价值）。无反例，保留。
* list **顺序** = 当前自然展示顺序。**identity 不由位置决定**：`a` 仍是 `a`，不会因显示位置变化变成 `b`。禁止 index-based identity。
* 数字型 anchor 在 YAML 中必须写成字符串，例如：

```yaml
parts:
  - "1"
  - "2"
  - "3"
```

不要依赖 YAML 把 `1` 解析成 integer（Gate 将非 string 判为 ERROR，与 v1 一致）。`a`/`b`/`c` 可直接作为 string。
* **不**存题面。子问正文只在 Markdown。
* 已有 Attempt 引用某 token 后，不能仅因排版把 `a` → `1`。

分层：Problem = 静态 multipart structure；Attempt = 动态 part evidence。不在本 Schema 设计 Attempt 字段。

概念检查（不改 Problem Metadata）：Attempt #1 a correct / b incorrect / c not attempted；#2 b hint-assisted correct / c correct；#3 delayed independent correct。**P ID + part anchor 已足够**作为未来 Attempt 静态引用基础。

---

### 7.12 Frozen YAML 示例（documentation only）

`P0000` 永久保留为 Schema / documentation sentinel，**永远不能真实分配**。不得把任何真实 Pilot 注册为 `P0000`。

draft（`knowledge` 可省略）：

```yaml
---
schema_version: 1
id: P0000
type: problem
title: 示例 Problem
status: draft
created: 2026-08-19
updated: 2026-08-19
---
```

reviewed（`knowledge: []` = mapping 已审核但 inventory 无合适 target；Frozen v1 rule）：

```yaml
---
schema_version: 1
id: P0000
type: problem
title: 示例 Problem
status: reviewed
created: 2026-08-19
updated: 2026-08-19
knowledge: []
---
```

multipart documentation example（`parts` optional Frozen v1）：

```yaml
---
schema_version: 1
id: P0000
type: problem
title: 示例 multipart Problem
status: draft
created: 2026-08-19
updated: 2026-08-19
parts:
  - a
  - b
---
```

numeric-like anchors（documentation snippet；必须是字符串）：

```yaml
parts:
  - "1"
  - "2"
```

### 7.13 Frozen 验证语义（规范级；正式 Validator 后实现）

#### 通用（draft / reviewed / archived）

* `schema_version == 1`（整数，不接受字符串 `"1"`）
* `id` 匹配 `^P\d{4}$`；真实对象禁止 `P0000`
* `type: problem`
* `title` trim 后非空
* `status` ∈ {`draft`, `reviewed`, `archived`}
* `created` / `updated` 为真实日历 `YYYY-MM-DD`；`updated >= created`

#### draft

基础字段必填。`knowledge` 可省略；若存在须为合法 list，元素满足 7.7 列表约束。

#### reviewed

基础字段必填。`knowledge` 必须显式存在为 list，**允许 `[]`（Frozen v1 rule）**。非空时每个 target：格式合法、非 `K0000`、不重复、存在、`type == knowledge`、`status == reviewed`。

若存在 `parts`：list[str]、trim 非空、唯一、至少 2 项。数字型 token 必须是 YAML 字符串。

#### archived

P ID 必须保留、不可回收；对象仍须可解析。不增加 replacement 类字段。

编号不要求连续。不设计自动 ID allocator。

---

### 7.14 Phase 3B-3 Controlled Pilot Migration

| Pilot | 正式对象 | 路径 | 状态 |
| --- | --- | --- | --- |
| **A** | `P0001` | `02_题目库/已解决/P001_为什么勒让德变换这样定义.md`（**未重命名**） | `reviewed`；YAML 权威 ID；Legacy Filename Cleanup Deferred |
| **B** | `P0002` | Problem `P0002`（physical path is operational; not part of Problem identity） | `draft`（Content Review Pending，**不是**用户不会做）；省略 `knowledge`；`parts: [a,b,c]` |

`P0001.created = 2026-08-19` 为 **structured registration date**（Frozen v1 legacy policy）。正文保留 Legacy Research Workflow：已解决；归档日期 2026-08-17。

`P0001.knowledge`：`K0001`（勒让德变换）为直接考察对象；`K0002`（凸函数）因正文直接分析凸/严格凸、凸共轭优化与凸一阶条件而列入，不是自动展开的 prerequisite 祖先。未写入任何不存在的 K ID。

P0002 Content Review Issue（**Pilot 正文**，非 Schema hard blocker）：domain / codomain / scalar field 未显式给出；实矩阵复特征值语境下 (a) 的谱需确认。正文含 candidate-only marker：`Candidate Content Review: PENDING`（不是 YAML 字段）。

#### Problem Candidate Gate v0.1（Freeze 后命运）

Implementation tool：`python -m tools.problem_candidate_gate`。Read-only。Schema Frozen 后进入 **Promotion / Replacement Review**。本轮 **不改名、不删除、不升 v1、不改代码**。README / `PCG-PART-004` 仍可能写 provisional：**tooling wording cleanup pending**。

正式 **Problem Validator v1** 应基于 Frozen Problem Schema v1，而非 Candidate assumptions。未知 YAML 字段 Gate WARNING（`PCG-FIELD-W001`）= Implementation Policy，不是 v1 字段。`READY_WITH_WARNINGS`（legacy filename、P0002 Content Review）**不阻塞** Schema Frozen。

---

### 7.15 Legacy 记录

| 项 | 说明 |
| --- | --- |
| Metadata 级 `P001` → `P0001` | **RESOLVED AT METADATA LEVEL**（YAML `id: P0001`）。文件名仍 `P001_...`：Legacy Filename Cleanup Deferred |
| structured registration date | **RESOLVED**：`created` = 结构化对象登记日；P0001 策略接受为 v1 legacy policy |
| Legacy template sentinel mismatch | `题目模板.md` 仍用 `P000`；规范 sentinel `P0000`。不改模板 |
| 目录 ≠ Object Status | `未解决/` / `研究中/` / `已解决/` 非 Object Status 权威。Gate 不据此 ERROR |

旧数据迁移困难 **不能**反过来决定新 Schema 删除 `created` / `updated`。

---

### 7.16 Resolved Before Freeze / Future Work

**Hard Freeze Blockers：None（0）。** Phase 3B-4 Final Review 项在冻结前已关闭。

**NON-BLOCKING FUTURE WORK**

* Formal Problem Validator v1（基于 Frozen Schema；因果：Frozen → Validator）
* Problem Candidate Gate v0.1 Promotion / Replacement Review
* Problem Template v1 Sync（`P000` vs `P0000`）
* P0001 legacy filename cleanup
* P0002 Content Review（Schema Frozen 时对象可仍为 `draft`）
* Problem Indexer / Problem Workflow
* Attempt Schema（Cross-Schema Compatibility Requirement → **Frozen**，见 §8）
* Method Schema / Error Mode Schema
* Deferred fields future review（`domain` / `source` / `problem_type` / `aliases` / `methods`）
* directory workflow strategy
* unknown field 正式 Validator 策略
* Candidate Content Review Markdown marker 清理

---

### 7.17 下一阶段：Phase 3B-6 Problem Validator v1

Problem Schema v1 **已 Frozen（2026-08-19）**。Phase 3B Problem Schema **DONE**。

下一阶段将 Candidate Gate v0.1 中已验证的机械规则迁移 / 正式化为生产级 Problem Validator v1。Attempt Schema v1 **已 Frozen**（§8）；Phase 3C-6 Attempt Validator v1 为 Attempt 侧下一阶段。**不**修改 P0001 / P0002 / A000001 / A000002 语义 Evidence。

---

## 8. Attempt Schema

**状态：Frozen（2026-08-19）**

**Formal technical authority：** 本文件 §8 为 Attempt Schema v1 唯一正式技术事实源。

**Design history / rationale / Pilot evidence：** `学习证据架构.md`（§19–§23；Candidate / Pilot / Final Review 历史保留，**不再**承担字段级 technical authority）。

### 8.1 设计目标

Attempt 记录 **真实发生** 的 target-scoped 学习尝试 Evidence，不是 Derived Learning State，也不是 static lifecycle 对象。

核心原则：

* **Recording ≠ Assessment**：Recorder 不得无依据猜测 `outcome`；Assessment 说明可写在 Markdown body，但 v1 **不**增加 assessment provenance Metadata 字段。
* **one Attempt → one evaluation target**：`problem` + optional `part`。
* **Correction ≠ Reattempt**：Correction 保留同一 `id` 并 append `corrections`；新的 solving/reasoning Attempt → 新 A ID。
* **Attempt 无 `status` lifecycle**：`unassessed` 是 `outcome` 枚举值，不是 lifecycle state。
* **YAML `id` 为 authoritative identity**；filename `<A-ID>.md` 仅为 operational convention。

### 8.2 Allowed Top-Level Fields（exactly 9）

除下列 9 个 top-level YAML 字段外，**任何**未知 top-level key 均为 **Schema violation**。

| Field | Required | Type | Constraint |
| --- | --- | --- | --- |
| `schema_version` | yes | integer | must equal `1` |
| `id` | yes | Attempt ID | Frozen Attempt Identity Contract（§8.3） |
| `type` | yes | literal string | must equal `attempt` |
| `problem` | yes | Problem ID | 必须引用已存在的 valid Problem（Frozen Problem Identity Contract） |
| `part` | no | part anchor | 若存在，必须引用对应 Problem 中 **exactly one** 已存在的 part |
| `outcome` | yes | enum | `correct` / `incorrect` / `partial` / `unsolved` / `abandoned` / `unassessed` |
| `assistance` | no | enum | `independent` / `assisted` |
| `attempted_at` | yes | temporal string | Frozen Temporal Contract（§8.10） |
| `corrections` | no | list | non-empty list of `{at, note}`（§8.11） |

**Required：** 6 个 — `schema_version`, `id`, `type`, `problem`, `outcome`, `attempted_at`  
**Optional：** 3 个 — `part`, `assistance`, `corrections`  
**Total allowed：** 9

### 8.3 Attempt Identity Contract

**Regex：** `^A\d{6}$`

**Reserved sentinel：** `A000000` — **不得**作为真实 Attempt record。

**First real Attempt：** `A000001`

**Identity authority：** YAML `id` 为 authoritative identity；filename 为 operational convention，**不是**第二份 identity authority。filename mismatch **不应**自动等同于 identity mismatch（吸取 P0001 legacy filename 经验）。

### 8.4 type Contract

**Required.** Literal string，must equal `attempt`。

与 Knowledge / Problem structured-object convention 一致。

### 8.5 Target Contract

**Required：** `problem: P0002`（示例；须为 valid Problem ID）

**Optional：** `part: a`

语义：

* `part` **omitted** → target = whole Problem
* `part` **present** → target = referenced Problem 中 **exactly one** part

约束：

* 必须复用 Frozen Problem Identity Contract；**不得**建立第二套 Problem ID 规则。
* `part` 必须引用对应 Problem 中 **实际存在** 的 Frozen part anchor。
* **禁止** `part: [a, b]`（list）
* **禁止** `problem: P0002#a`（compound anchor in problem field）
* **禁止** nested `target: { problem:, part: }`
* one Attempt → exactly one evaluation target

**Episode ≠ Attempt：** 一个 solving episode 可形成多个不同 target-scoped Attempt；一个 Attempt 只对应一个 target。

### 8.6 Outcome Contract

**Required.** Frozen enum：

| Value | 含义 |
| --- | --- |
| `correct` | target 已 **完整、可靠地正确完成** |
| `incorrect` | 形成了完整、可评价的答案 / 证明 / 结论，但存在 **决定性数学错误** |
| `partial` | target **未完整完成**，但已取得 **实质性、可确认正确** 的数学进展 |
| `unsolved` | 发生真实 solving/reasoning Attempt，但 **没有** 形成足够达到 `partial` 的正确成果 |
| `abandoned` | 在数学闭合前，由于主动停止、时间、优先级或其他 **过程原因** 结束 |
| `unassessed` | Attempt 确实发生，但 **没有可靠 Assessment basis**，因此无法可靠判断正确性 |

**必须明确：**

* `partial` **≠** `unsolved`
* `unassessed` **≠** `draft` **≠** missing outcome **≠** unknown **≠** lifecycle state
* 即使 `outcome: unassessed`，字段本身仍 **required**

### 8.7 Assistance Contract

**Optional.**

若存在，只允许：

| Value | 含义 |
| --- | --- |
| `independent` | 外部帮助 **没有实质改变** 当前 target 的 search space、key insight、next mathematical action。允许事后核对、非关键正确性确认、非实质性交流，仍可 classified `independent` |
| `assisted` | 外部帮助 **实质改变** search space、key insight 或 next mathematical action |

**正交维度：** `outcome` 与 `assistance` 独立。例如 `correct` + `assisted`、`partial` + `independent` 均合法。

### 8.8 Assistance Omission Semantics

若 `assistance` 字段 **不存在**：

语义 = **assistance information was not reliably recorded**

**不是** `independent`；也 **不是** enum `unknown` / `omitted` / `none`。

v1 **不**增加 `minimal` / `hinted` / `guided` / `heavy` / `partial_help` 等 taxonomy。

### 8.9 Excluded Fields（v1 禁止）

下列字段 **不属于** Attempt Schema v1，任何 top-level 出现均为 violation：

* lifecycle / workflow：`status`, `title`, `created`, `updated`, `workflow_state`, `research_status`
* assessment provenance：`assessment_basis`, `assessment_method`, `assessed_by`, `assessment_confidence`, `confidence`
* cross-object redundancy：`knowledge`, `method`, `error_mode`
* derived learning state：`mastery`, `weakness`, `success_rate`, `needs_review`, `recommended_next`, `confidence_score`, `recommendation`
* pedagogical metadata：`difficulty`, `hint_level`, `independent_progress`
* reattempt linkage：`reattempt_of`, `previous_attempt`

**Method / Error Mode：** DEFER → Phase 3D  
**Derived Learning State：** DEFER → Phase 3E

### 8.10 attempted_at Frozen Temporal Contract

**Required.** Canonical type：**temporal string scalar**（不依赖 YAML parser implicit date/timestamp typing）。

**Canonical YAML representation：** 必须使用 **quoted scalar**。

**语义：** 该 target-scoped Attempt Evidence **形成 / 结束时** 的最佳可靠时间记录。

**不是：** chat created time、Markdown file created time、filesystem mtime、Recorder 写文件时间、整个 Chat session 时间、整个 solving episode 任意结束时间。

**原则：** best available temporal record + **no fabricated precision**

#### Form A — minute + explicit offset

当 **分钟可靠已知**：

```
"YYYY-MM-DDTHH:MM±HH:MM"
```

示例：

```yaml
attempted_at: "2026-08-19T22:05+08:00"
```

必须包含：date、hour、minute、explicit UTC offset。**不要** seconds。

#### Form B — date-only

当 **只可靠知道 calendar date**：

```
"YYYY-MM-DD"
```

示例：

```yaml
attempted_at: "2026-08-19"
```

语义：只知道事件发生的可靠本地日历日期，**没有**可靠分钟信息。

#### False Precision Policy

宁可 **降低时间粒度**，也不得制造假精度。若不能可靠得到分钟，**不得**写 `2026-08-19T21:00+08:00`；仅能可靠确定日期时写 date-only。

v1 **不提供** approximate clock-time representation（例如「大约九点」不能自动编码成 `21:00`，除非该分钟本身被认为可靠）。否则降级到可靠 calendar date。

v1 **不**增加 `attempted_at_precision` 等额外字段。

### 8.11 corrections Contract

**Optional.** 若存在，必须是 **non-empty list**。

每个 correction item **只允许**：

```yaml
corrections:
  - at: "2026-08-20T10:20+08:00"
    note: "修正了……"
```

* `at`：**required**；复用 §8.10 Temporal Contract（quoted temporal string；Form A 或 Form B）
* `note`：**required**；non-empty human-readable string；描述此次 correction 内容

**禁止** item keys：`author`, `reason`, `source`, `diff`, `previous_value`, `correction_id`, `assessment`, `confidence`

#### Correction Semantics

* Correction → **same Attempt identity** → same A ID → **append** correction history
* Correction **不**表示一次新的 solving/reasoning Attempt
* 必须保留 auditable provenance；已有 correction history **不得**静默删除或覆盖后伪装从未发生
* append-only 首先是 **process/provenance contract**；snapshot Validator 未来可能无法完全验证 historical append-only

### 8.12 Reattempt Contract

新的真实 solving/reasoning Attempt → **new A ID**。

示例：先 `A000002` `P0002(a)` `partial`，以后独立重做 → `A000015` `P0002(a)` `correct`；**必须保留** `A000002`，**不得**把其 outcome 直接改成 `correct`。

v1 **不**增加 `reattempt_of` / `previous_attempt`。未来 Derived 层可通过 `problem`, `part`, `attempted_at`, `id` 推导 reattempt sequence。

### 8.13 Correction vs Reattempt

| | Correction | Reattempt |
| --- | --- | --- |
| Identity | same A ID | new A ID |
| 语义 | append `corrections` | new solving/reasoning Evidence |
| 不得混用 | ✓ | ✓ |

### 8.14 Recording vs Assessment

**Recording ≠ Assessment**

Recorder 不得凭空猜 `outcome`；`outcome` 必须存在可解释的 Assessment basis。必要 Assessment explanation **允许**放在 Markdown body。

**Assessment provenance Metadata：** v1 **KEEP EXCLUDED**（`assessment_basis`, `assessed_by`, `assessment_method`, `assessment_confidence` 等）。未来 Phase 3E / Validator v2+ 可重新评估。

### 8.15 File Convention

**Frozen Attempt Record Schema v1 不变。** 本节仅定义 **Attempt Storage Contract v1**（physical storage）。

* **Logical Evidence unit** = Attempt（A ID 全局唯一；`frontmatter.attempts[]` 中每条 record 仍须完整满足 §8.2 Frozen record）
* **Physical storage unit** = Problem-scoped Attempt Ledger（`11_学习证据/尝试记录/Pxxxx.md`）
* **one Problem → at most one Ledger file**；同一 Problem 的全部 Attempt 写入同一 Ledger
* **Operational filename convention：** `<Problem-ID>.md`（例如 `P0002.md`）；文件名 Problem ID = Ledger owner
* **禁止**正式 production Source 使用 legacy `<A-ID>.md` 单文件存储
* YAML `attempts[i].id` → authoritative logical identity；正文 `## Axxxxxx` 仅承载 human-readable narrative，**不是** metadata authority

**Ledger frontmatter（storage container，非新 Schema object）：**

```yaml
---
storage_format: problem_attempt_ledger_v1
problem: P0002
attempts:
  - schema_version: 1
    id: A000001
    type: attempt
    problem: P0002
    # ... 完整 Frozen Attempt record ...
---
```

正文按 `attempted_at` 升序（相同时 A ID 升序）排列 `## Axxxxxx` 分节；`attempts[]` 与正文 section 顺序须一致。

### 8.16 Markdown Body Convention

Markdown body **不是** Schema required structure。body 用于保存 natural historical evidence。

可使用 **optional** 轻量 convention，例如 `## 作答 / 推理`、`## Assessment`；Validator **不得**要求固定 heading。

v1 **不**创建正式 Attempt Template（Post-Freeze Implementation）。

### 8.17 Unknown-Field Policy

Frozen v1：任何不属于 §8.2 所列 9 个字段的 top-level YAML key → **Schema violation**。

不允许「先写着以后可能用」。

### 8.18 Validator-Enforceable vs Semantic/Process-Only

#### A. Validator future enforceable

* Markdown / frontmatter discovery
* YAML parse
* allowed top-level fields（exactly 9）
* required fields presence
* `schema_version == 1`
* `id` lexical shape + sentinel exclusion
* duplicate Attempt ID
* `type == attempt`
* referenced `problem` existence
* `part` existence when present
* whole target via `part` omission
* `outcome` enum
* `assistance` enum / omission allowed
* `attempted_at` lexical format（quoted temporal string；Form A or Form B）
* `corrections` list shape、allowed keys、`at` temporal validation、`note` non-empty
* Ledger container invariants（filename / problem / attempts non-empty / metadata-body parity / ordering / global A ID uniqueness）；每条 `attempts[i]` 仍按 Frozen Attempt record 校验

#### B. Semantic / process-only

Validator **不得**假装能够判断：

* outcome 数学判断是否真的正确
* assistance 是否真的属于 `assisted` / `independent`
* Recorder 是否真的有 Assessment basis
* Correction 是否真正 append-only
* Reattempt 是否真的来自新 solving event
* body 是否真实反映历史过程

### 8.19 Canonical Examples

#### Example A — Real Pilot（A000001）

```yaml
---
schema_version: 1
id: A000001
type: attempt
problem: P0002
part: b
outcome: correct
assistance: assisted
attempted_at: "2026-08-19T21:36+08:00"
---
```

#### Example B — Real Pilot（A000002）

```yaml
---
schema_version: 1
id: A000002
type: attempt
problem: P0002
part: a
outcome: partial
assistance: independent
attempted_at: "2026-08-19T22:05+08:00"
---
```

#### Whole Problem target

`part` omitted → target = whole Problem：

```yaml
---
schema_version: 1
id: A000099
type: attempt
problem: P0002
outcome: unsolved
attempted_at: "2026-08-19"
---
```

> **SCHEMATIC EXAMPLE — NOT A REAL ATTEMPT**（`A000099` 为示意 ID）

#### Date-only attempted_at

```yaml
attempted_at: "2026-08-19"
```

### 8.20 下一阶段：Phase 3C-6 Attempt Validator v1

Attempt Schema v1 **已 Frozen（2026-08-19）**。Phase 3C-5 **DONE**。

下一阶段：Attempt Validator v1（§8.18A 规则）。Validator **不得**评估 §8.18B 语义层。Validator 完成前，Indexer / Workspace Maintenance **不得**把 Attempt 纳入正式 derived pipeline。

正确顺序：**Freeze → Validator → Validated Registry → Indexer → Derived State**

---

## 9. Method Schema

**Method Schema 版本：** v1  
**状态：** Frozen（已冻结）  
**冻结日期：** 2026-08-20  
**对象类型：** `type: method`  
**对象 `schema_version`：** 整数 `1`（对应本冻结版）  
**ID 格式：** `M` + 4 位数字（匹配 `^M\d{4}$`；真实对象从 `M0001` 起，`M0000` 为规范保留编号）  
**Formal technical authority：** 本文件 §9 为 Method Schema v1 唯一正式技术事实源。  
**Design history / rationale / Pilot evidence：** `Method与ErrorMode架构.md`（Phase 3D-1–3D-6M；**不再**承担字段级 technical authority）。

**已分配真实 ID：** `M0001`、`M0002`。

**Source root：** `12_方法库/`（`<M-ID>.md`）。

### 冻结含义

「冻结」表示：

1. Method Schema v1 已通过 Semantic Contract、Relation Contract、Candidate Schema、Real Pilot 与 Final Review（3D-6M FREEZE-READY）；
2. 当前 6-field 结构与验证语义视为稳定基线；
3. 后续普通内容录入不得随意修改 v1 Schema；
4. 模板、validator、索引等实施工作应基于该冻结版本；
5. 新需求应先作为 Future Enhancement / 后续 Schema 版本评估，而不是直接改写 v1。

冻结后允许：错别字修正、不改变语义的表达澄清、示例与实施说明补充、implementation note、Validator 实现细节、非语义文档格式优化。

冻结后不得在仍称为 Method Schema v1 的情况下随意：新增/删除 Frozen 字段、改字段类型、改 required/optional 语义、改 status 枚举、改 Identity Contract、改 `knowledge` 关系语义、改 omission 语义、改 ID 格式、改 `M0000` sentinel 规则。若需语义变更，应进入 Schema Change Review，并进入 Method Schema v1.1 / v2 或项目现有版本治理。

**Schema maturity ≠ Object status：** Method Schema v1 **FROZEN** 不自动把 Method Object 晋升为 `reviewed`。`M0001` / `M0002` 当前 `status: draft` 仅表示对象内容尚未 Content Review，**不**表示 Schema 仍为 Candidate。

### Frozen Baseline（2026-08-20）

* Schema：Method Schema v1；`schema_version` 整数 `1`
* Frozen 字段（6）：`schema_version`、`id`、`type`、`title`、`status`、`knowledge`（optional）
* Required（5）：`schema_version`、`id`、`type`、`title`、`status`
* Optional（1）：`knowledge`
* `status` enum：`draft` / `reviewed` / `archived`（复用 Static Object lifecycle，与 Knowledge / Problem 一致）
* ID：`^M\d{4}$`；sentinel `M0000`
* 关系：Method → supporting Knowledge（optional；Method-side SoT；反向 Derived）
* **不含** `created` / `updated`（3D-6M CLOSED — DROP FROM v1）
* Real Pilots：`M0001`、`M0002`；Final Review FREEZE-READY；Hard Freeze Blockers = 0

---

### 9.1 定位

Method 是 **Static Mathematical Object**：具有 **稳定 Mathematical Intent** 与 **稳定 Reasoning / Operative Mechanism**、可以 **脱离单一 Problem 偶然细节** 而复用的 **solving / reasoning procedure**。

Method 回答：**面对某类数学情境，通常按什么可复用步骤/策略去推进？**

Method **不是**：Knowledge 本身、Problem 完整解答、Attempt 单次步骤、Recommendation / Derived 学习状态、单题临时操作。

详细语义边界见 `Method与ErrorMode架构.md` Phase 3D-1。**本 §9 只冻结 Validator / Agent / Source Author 需要的 contract。**

YAML 只承担 Identity + Lifecycle + optional Method→Knowledge relation。Intent · Mechanism · Procedure Skeleton · Applicability · Preconditions · Variants · Examples 留在 Markdown body。

---

### 9.2 Method Identity Contract

`id` 表示一个稳定 **procedure** 身份。权威来源是 YAML Metadata，**不是** filename、**不是** title、**不是** Knowledge 列表、**不是** Problem / Attempt 引用。

**语义 identity core（Content Review，非 Validator）：** Stable Mathematical Intent + Stable Reasoning / Operative Mechanism。

#### 可保留原 M ID

修正 title wording；补充或调整 `knowledge` 列表（Intent + Mechanism 未变）；更换 body 中的 example Problem；改进 body 表达；notation / dimension variant 写入 body。

#### 必须创建新 M ID

核心 mathematical mechanism 或 procedure skeleton **实质改变**；原对象实为多个独立 Method 的偶然串联且无法修正为单一 procedure。

#### 路径与文件名

**Operational filename convention：** `<M-ID>.md`（如 `M0001.md`）。

YAML `id` 为 **authoritative identity**；filename 为 operational convention，**不是**第二份 identity。filename 与 YAML `id` 不一致时的 Validator 严重级别 → **Phase 3D-8M Method Validator v1** 设计（本轮 Freeze 不额外发明新 identity rule）。

---

### 9.3 生命周期（Frozen）

复用 Static Object lifecycle（与 Knowledge / Problem 一致）：

#### draft

内容 / Metadata 可调整；非正式引用基线。

#### reviewed

Content Review 已确认：对象 **Method-worthy**；Metadata 满足 Frozen v1 规则；body 能独立表达 When / Intent / Mechanism / Skeleton。

**不要求** 已有跨题使用证据；**不** 由 Attempt outcome 自动晋升。

#### archived

历史身份保留；M ID 不回收；不再作为常规活跃 Method。

**不使用 Method-only enum**（如 `candidate` / `stable` / `verified` / `deprecated`）。

---

### 9.4 字段归属（Frozen v1）

| 等级 | 含义 |
| --- | --- |
| FROZEN V1 | Method Schema v1 正式字段 |
| BODY ONLY | 语义重要但不进入 YAML |
| DEFERRED | 不属于 Method v1；可通过后续 Schema 版本重新评估 |
| EXCLUDED FROM V1 | 禁止进入 Method v1（含 Derived / 其他对象关系） |

---

### 9.5 Frozen 字段表

| 字段 | Frozen | 类型 | required | 语义 |
| --- | --- | --- | --- | --- |
| `schema_version` | Frozen | integer `1` | required | 对象 Schema 版本 |
| `id` | Frozen | string `^M\d{4}$` | required | 稳定身份；真实对象不得 `M0000` |
| `type` | Frozen | literal `method` | required | 固定字面量 |
| `title` | Frozen | 非空 string（trim） | required | 人类导航标签；**≠ identity** |
| `status` | Frozen | `draft`/`reviewed`/`archived` | required | Object Status |
| `knowledge` | Frozen | list of `^K\d{4}$` | **optional** | Method → supporting Knowledge |

**Unknown top-level field：** 任何不属于上表 6 个字段的 top-level YAML key → **Schema violation**（含 `created`、`updated`、`aliases`、`intent`、`problems`、`attempts` 等）。

---

### 9.6 `schema_version`

```yaml
schema_version: 1
```

整数 `1`。**不是** `"1"`、**不是** `1.0`。与 Frozen K / P / A 一致。

---

### 9.7 `type`

```yaml
type: method
```

必须为字面量 `method`。不得使用 `Method`、`methods`、`method_object` 等。

---

### 9.8 `title`

* **required**；trim 后非空 string
* 人类可读短名；完整 mechanism 不放 title
* **≠ Method identity**；改 title wording 在 Intent + Mechanism 未变时 **不** 产生新 M ID
* Validator **不得**判断 title 是否数学准确

---

### 9.9 `knowledge`（Frozen v1）

`knowledge` 表示 Method 对 Knowledge 的 **formal intrinsic static relation**（procedure 所依赖的 supporting Knowledge）。

**与 Problem `knowledge` 的差异：** Problem `knowledge` = directly engaged Knowledge；Method `knowledge` = procedure 依赖的 supporting Knowledge。**同一字段名、同一 K ID 词法；语义 owner 不同。**

#### Optional · cardinality 0..n

| 写法 | 含义 |
| --- | --- |
| **省略 `knowledge`** | **canonical** — 当前 **未断言** Method→K source relation |
| `knowledge:` 含 ≥1 合法 K ID | 已断言 supporting Knowledge |
| `knowledge: []` | **invalid** |
| 空键 `knowledge:` / `null` | **invalid** |

**Omission 语义（Frozen）：** omit **不**表示 unknown、validator failure、missing metadata 或「以后必须补」。Canonical no-relation representation = **omit field**。

**draft / reviewed / archived** 均适用同一 omission 规则（**无** Problem 式 `reviewed` 必须显式 `knowledge` 规则）。

#### 列表约束（字段 present 时）

复用 Knowledge ID lexical contract（§6 / §11）：

* 类型 `list[str]`；每项匹配 `^K\d{4}$`
* 禁止 `K0000`
* 禁止重复
* target 必须真实存在
* target `type == knowledge`
* 不得指向 P / M / EM / A

target status：Formal Validator 可检查存在性与基本 registry 一致性；**不**在本 Freeze 阶段额外发明 Method-only target-status lifecycle 规则（具体 diagnostic 留给 3D-8M）。

#### Identity independence

`knowledge` 列表变化 **不自动** 改变 Method identity（Intent + Mechanism 未变 → same M ID）。

#### 权威方向

Method YAML 是 **Method → Knowledge** 的 Source of Truth。Knowledge 侧「哪些 Method 依赖某 K」**必须 Derived**（indexer / registry scan）。

**禁止**给 Frozen Knowledge Schema v1 增加 `methods` / `used_by_methods` 等 backlink 字段。

---

### 9.10 BODY ONLY（Frozen v1）

下列内容 **语义重要**，但 **不属于** Method YAML：

* Applicability（适用场景 / When）
* Mathematical Intent
* Reasoning / Operative Mechanism
* Procedure Skeleton
* Mathematical Preconditions
* Variants
* Examples（含 body 中对 P / A / K 的文档引用）

Body 中提及 `P0001` · `A000001` 等 = documentation / example，**≠** structured P↔M / A↔M relation。

**Fixed body headings：NOT REQUIRED。** 推荐 authoring headings（适用场景、数学意图、核心机制、操作骨架、数学前提、示例与边界）仅为 convention；Validator v1 **不得**因缺少某 heading 判 ERROR。

---

### 9.11 Deferred / Excluded from Method v1

#### DEFERRED（≠ 永久禁止）

| 主题 | v1 | 备注 |
| --- | --- | --- |
| `aliases` | DEFER | 无真实多名称需求 |
| P↔M · A↔M · M↔EM | DEFER | 见 `Method与ErrorMode架构.md` Phase 3D-3 |
| `problems` / `attempts` / `error_modes` / `used_in` / `applies_to` / `example_problems` / `common_errors` | DEFER | 关系层；不得进入 Method source YAML |
| filename == id Validator-required | DEFER | 3D-8M |
| Compound Method（parent/sub/composition） | DEFER | 3D-1 semantic only |

#### EXCLUDED FROM V1

`created` · `updated` · `tags` · `category` · `domain` · `difficulty` · `intent` · `mechanism` · `procedure` · `steps` · `variants` · `examples`（作为 YAML 字段）· `mastery` · `proficiency` · `success_rate` · `usage_count` · `last_used` · `weakness` · `review_priority` · `priority` · `recommendation` · `confidence` · `personal_score`

`created` / `updated`：**DROP FROM v1**（3D-6M CLOSED）；未来需求只能通过 Schema v2+ 重新评估，**不得**因 K/P 有该字段而在 v1 偷偷补回。

---

### 9.12 Validator vs Content Review（Frozen 边界）

#### 9.12A — 未来 Method Validator v1 **可**机械检查

* frontmatter parse
* exact allowed top-level fields（closed set of 6）
* required fields present
* `schema_version` integer `1`
* ID regex · sentinel · duplicate ID（registry 级）
* `type` literal `method`
* `title` non-empty（trim）
* `status` enum
* `knowledge` list shape · omission vs empty list · K ID lexical validity · K registry existence（当 present）
* unknown top-level field → ERROR

#### 9.12B — Validator **不得**检查（Content Review / 人工语义治理）

* Method 是否数学正确
* Method 是否可复用
* Intent / Mechanism 是否稳定或相同
* 两个 Method 是否数学同一
* Applicability 是否正确
* body 是否足够教学友好
* body 是否含推荐 headings

---

### 9.13 Validated Registry 与 Workspace（Frozen 治理顺序）

Method Source Object（Frozen Schema v1）**≠** Validated Method Registry entry。

正确顺序：

```
Method Source + Frozen Schema v1
        ↓
Formal Method Validator v1（Phase 3D-8M — 尚未实现）
        ↓
Validated Method Registry
        ↓
Workspace Indexer / Derived Integration（Phase 3D-9M）
```

Method Validator 完成前：Workspace Indexer / Workspace Check **不得**把 Method 纳入正式 derived pipeline；**不得**手写 Method derived index。

---

### 9.14 下一阶段：Phase 3D-8M Method Validator v1

Method Schema v1 **已 Frozen（2026-08-20）**。Phase 3D-7M **DONE**。

下一阶段：Method Validator v1（§9.12A 规则）。Validator **不得**评估 §9.12B 语义层。Validator 完成前，Indexer / Workspace Maintenance **不得**把 Method 纳入正式 derived pipeline。

正确顺序：**Freeze → Validator → Validated Registry → Indexer → Derived State**

---

## 10. Error Mode Schema

**状态：待设计**

### 10.1 设计目标（草案）

描述可重复出现的错误模式，供题目复盘与长期规避引用。

### 10.2 预留核心字段方向

* 身份：`id`, `type`, `title`
* 生命周期：`status`, `created`, `updated`
* 关系：待设计
* 其他字段：待设计

### 10.3 待定事项

* 推荐主位置（当前仅规划，不创建目录）：`09_长期记忆/错误模式/`
* 原 `09_长期记忆/错题与错误模式.md` 未来可作为错误模式索引与诊断摘要
* 现有 `09_长期记忆/错题与错误模式.md` 中的 `EM001`–`EM005` 与未来独立 Error Mode 对象（推荐 ID 形如 `EM0001`）如何衔接
* 是否拆成一模式一文件，或继续集中维护后再结构化
* 必填字段与合法值

---

## 11. ID 规则

### 11.1 推荐格式（Schema v1）

| 类型 | 推荐格式 | 真实对象编号起点 | 规范保留编号 |
| --- | --- | --- | --- |
| Knowledge | `K` + 4 位数字 | `K0001` | `K0000` |
| Problem | `P` + 4 位数字 | `P0001` | `P0000` |
| Attempt | `A` + 6 位数字 | `A000001` | `A000000` |
| Method | `M` + 4 位数字 | `M0001` | `M0000` |
| Error Mode | `EM` + 4 位数字 | `EM0001` | `EM0000` |

格式合法性正则（供未来验证程序使用）：

```text
^K\d{4}$
^P\d{4}$
^A\d{6}$
^M\d{4}$
^EM\d{4}$
```

### 11.2 规范保留编号

以下编号永久保留用于：

* Schema 文档示例；
* 格式演示；
* 测试说明；

不得分配给真实对象：

```text
K0000
P0000
A000000
M0000
EM0000
```

`0000` 是 documentation sentinel / schema example ID。不得创建真实对象如：

```yaml
id: K0000
```

真实对象编号从：

```text
K0001
P0001
A000001
M0001
EM0001
```

开始。

### 11.3 格式合法与可正式分配

格式合法并不代表可以正式分配。

* **format_valid**：ID 符合对应格式正则。
* **assignable / real_object_valid**：可正式分配给真实对象。

真实对象必须同时满足：

1. ID 符合对应格式正则；
2. 数字部分不得为 `0000`；
3. ID 当前没有被其他真实对象占用。

因此：

```text
K0000
```

格式上符合 `^K\d{4}$`，但**不得正式分配**。

未来验证程序应区分 `format_valid` 与 `assignable` / `real_object_valid`。本规范阶段不实现 Python 工具。

### 11.4 稳定性

* 正式分配后的 ID 原则上永久不变；
* 不得因改标题、移动目录、重写正文而更换 ID；
* 废弃对象应使用状态标记，而不是回收并重用 ID。

### 11.5 与现有仓库的关系

当前首个正式 Knowledge 对象已经分配：

```text
K0001 = 勒让德变换
```

其实际对象文件为：

`01_知识库/数学变换/勒让德变换.md`

这是已经发生且永久稳定的 ID 分配事实，不是示例、计划或推荐。实际知识文件中的 YAML `id` 仍是 ID 分配的事实源；本处仅记录已发生的稳定事实，不另建 ID 注册表。

当前仓库中另有非正式或不完全对齐的编号习惯，例如：

* 题目文件：`02_题目库/已解决/P001_为什么勒让德变换这样定义.md`
* 错误模式汇总：`09_长期记忆/错题与错误模式.md` 中的 `EM001`–`EM005`

Schema v1 **不自动**将上述历史编号重编号为 `P0001` / `EM0001` 等，不批量改文件名，不宣称上述历史编号已经完成正式 Schema 分配。

迁移策略属于后续人工决策事项。

---

## 12. 通用字段规则

以下规则对所有对象类型通用，除非某类型 Schema 明确覆盖。

### 12.1 日期

统一格式：

`YYYY-MM-DD`

示例：

```yaml
created: 2026-08-19
updated: 2026-08-19
```

### 12.2 布尔值

统一：

```yaml
true
false
```

不要使用：

* `是` / `否`
* `yes` / `no`
* `1` / `0`

### 12.3 空数组

统一：

```yaml
related: []
```

不要使用仅有键名而无值的写法：

```yaml
related:
```

### 12.4 字符串与列表

* 单值字符串：直接写标量；
* 多值引用：使用 YAML 数组，元素为对象 ID；
* 不要把多个 ID 塞进一个逗号分隔字符串，除非未来 Schema 明确允许。

### 12.5 通用基础字段

`schema_version`、`id`、`type`、`title`、`status`、`created`、`updated` 是四类对象（Knowledge / Problem / Method / Error Mode）Schema 设计时都必须处理的通用基础字段。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `schema_version` | integer | 对象采用的 Metadata Schema 版本；Schema v1 固定为 `1` |
| `id` | string | 稳定对象 ID |
| `type` | string | 对象类型，见第 5 节 |
| `title` | string | 人类可读标题 |
| `status` | string | 生命周期状态（合法值由各对象 Schema 各自定义） |
| `created` | date | 创建日期 |
| `updated` | date | 最近更新日期 |

`status` **不使用一套全局枚举**。各对象类型在自己的 Schema 中定义合法状态。Knowledge 与 Problem 各自使用 `draft` / `reviewed` / `archived`（语义不同，见第 6 节与第 7 节）。Method / Error Mode 的 `status` 枚举仍待各自 Schema。

---

## 13. 对象引用规则

### 13.1 引用方式

对象之间通过稳定 ID 建立引用。

示意（以下为 Schema 关系结构演示；`P0000` / `K0000` / `M0000` / `EM0000` 为规范保留示例编号，不可分配给真实对象）：

```text
P0000
└─ knowledge → K0000
   （methods / error_modes：Problem Schema v1 Frozen 明确 DEFER，不进入本版 YAML）
```

```text
Knowledge（当前对象）
├─ prerequisites → 其他 Knowledge ID
└─ related → 其他 Knowledge ID
```

真实 Knowledge 对象的 ID 分配事实以实际知识文件为准；当前首个正式对象见第 10.5 节（`K0001`）。### 12.2 规则

* 引用字段的值应为合法 ID，而不是标题或路径；
* 被引用对象在未来验证时应存在，或被明确标记为可容忍的悬空引用策略（策略待定）；
* 本轮只定义引用思想，不创建真实不存在的正式 K / P / M / EM 对象。

### 13.3 权威方向与反向引用

人工只维护权威方向的关系。能够从权威方向确定推导出的反向关系，由未来程序自动生成。不人工维护可确定推导出的重复关系。

#### Problem → Knowledge

由 Problem 对象人工维护，例如：

```yaml
knowledge:
  - K0000
```

（`K0000` 仅为字段格式演示；真实 Problem 应引用已正式分配的 Knowledge ID。）
Knowledge 不人工维护：

```yaml
source_problems:
used_in_problems:
```

未来程序通过扫描 Problem 的 `knowledge` 字段，自动计算 Knowledge 的反向题目索引。

#### Knowledge 自身关系

以下属于 Knowledge 自身固有关系，由 Knowledge 自己维护：

```text
prerequisites
related
```

各对象 Schema 定稿时，应明确哪些关系字段属于该对象的权威方向。Problem / Method / Error Mode 的其余权威字段在对应 Schema 中定义。

---

## 14. Metadata 与正文职责边界

| 层级 | 负责内容 | 不负责内容 |
| --- | --- | --- |
| Metadata | ID、类型、状态、日期、对象关系、可机读属性 | 证明细节、数学直觉、图像解释 |
| Markdown 正文 | 定义、定理、推导、例子、易错点、推广 | 充当数据库字段堆砌 |

规则：

* 正文可以叙述关系，但结构化关系以 Metadata 中的 ID 引用为准；
* 不得为了填 Metadata 而删掉正文中有学习价值的数学解释；
* 不得把大段证明塞进 YAML。

---

## 15. Schema 验证要求

本轮不实现 Python 工具，但 Schema 设计必须为未来自动检查预留能力。

未来程序至少应能检查：

1. YAML Front Matter 是否可解析；
2. `schema_version` 是否存在；
3. `schema_version` 是否属于当前支持版本；
4. Schema v1 对象是否满足 `schema_version == 1`；
5. `id` 格式是否合法（`format_valid`）；
6. `id` 是否可正式分配（`assignable`：数字部分非 `0000`，且未被其他真实对象占用）；
7. `id` 是否全局重复；
8. `type` 是否属于合法集合；
9. `status` 是否属于该类型允许的合法集合；
10. `created` / `updated` 是否符合 `YYYY-MM-DD`；
11. 必填字段是否缺失；
12. 引用字段中的 ID 是否指向存在对象，且引用目标本身可正式分配；
13. 引用目标对象的 `type` / `status` 是否满足该对象类型 Schema 的约束（对象专属细则见各对象 Schema；Knowledge 见第 6.14–6.15、6.21 节）；
14. 对象级 Schema 可以定义跨对象图约束（例如 Knowledge `prerequisites` 必须无环 / DAG）；对应 validator 应按对象 Schema 检查；
15. 数值字段是否超出规定范围（若未来引入数值字段）。

对象专属验证（例如 Knowledge 的 `prerequisites ∩ related = ∅`、`reviewed` 时 `domain` 为非空 string、reviewed source 要求 target 为 `reviewed`、`prerequisites` 图无环）写在对应对象 Schema 中，本节只规定通用验证框架。

因此：

* 不要设计无法稳定解析的自由格式“伪结构”；
* 枚举值应显式列出；
* 范围与单位应写清。

---

## 16. Schema 版本管理

### 16.1 文件名

永久保持：

`元数据规范.md`

不创建：

* `元数据规范_v1.md`
* `metadata-schema-final.md`
* `元数据规范最终版.md`
* `元数据规范_new.md`

### 16.2 版本记录

* 文件内部记录：`Schema 版本：v1`
* 历史版本由 Git 管理

### 16.3 版本号含义（草案）

* **整体 Metadata Schema：** 仍为 draft / progressive design。四种对象体系与通用规则并存，但**不等于**整体已完成或 stable。
* **对象级：**
  * Knowledge Schema v1 — **Frozen**（2026-08-19）；对象 `schema_version` 仍为整数 `1`；
  * Problem Schema v1 — **Frozen**（2026-08-19）；对象 `schema_version` 仍为整数 `1`；
  * Method Schema — 待设计；
  * Error Mode Schema — 待设计。
* 不宣称「Metadata Schema v1 整体完成」。
* 后续小改（字段补充、合法值澄清）：对已冻结对象原则上进入新变更流程或 v1.1；对未完成对象继续 Candidate 设计（升级规则见第 16 节）。
* 不兼容变更：提升主版本号。

---

## 17. Schema 变更原则

1. **先设计，后批量落地**：Schema 未相对稳定前，不批量改写现有知识库 / 题目库。
2. **正式 ID 不随意改**：已正式分配的 ID 原则上永久保留。
3. **兼容优先**：能通过新增可选字段解决的，不优先改名或删除旧字段。
4. **迁移可描述**：任何不兼容变更必须写明迁移规则。
5. **制度与规范分离**：总体制度仍写在 `项目规则.md`；Metadata 技术细节写在本文件。
6. **禁止平行终稿文件**：以本文件 + Git 历史为准。

---

## 18. 当前实施阶段

当前阶段定位：

> **Knowledge Schema v1 已正式冻结；Problem Schema v1 已正式冻结；Attempt Schema v1 已正式冻结（2026-08-19）；整体 Metadata Schema 仍为渐进设计；Method / Error Mode 待设计**

**Knowledge Schema v1 正式冻结日期：** 2026-08-19

**Freeze basis（摘要）：**

* `K0001` = 勒让德变换；`K0002` = 凸函数；二者均 `reviewed`
* `K0001.prerequisites → K0002`；真实生命周期与引用试点通过
* Candidate final review: PASS WITH FINAL FIXES；最终修复完成；无 Freeze Blocker

本阶段已完成（Knowledge）：

* Knowledge Schema v1 Candidate 设计、一致性收口与真实试点；
* domain 非空 string、prerequisite DAG、target 状态约束等最终规则；
* Knowledge Schema v1 **正式冻结**。

Knowledge 下一阶段（Implementation / Rollout，本轮不执行）：

1. 同步 `01_知识库/知识库模板.md`
2. 实现 Knowledge validator（含 prerequisite 环检测）
3. 自动索引 / 反向引用
4. 后续 Knowledge 按 v1 接入 Metadata

**Problem Schema v1 正式冻结日期：** 2026-08-19

**Problem Freeze basis（摘要）：**

* Identity Contract、lifecycle、`knowledge`、`parts`、legacy `created` policy 已冻结
* 双 Pilot：`P0001` reviewed、`P0002` draft（Content Review 非 Schema blocker）
* Final Review PASS；Hard Freeze Blockers = 0

整体 Metadata Schema 尚未完成：

* Problem Schema v1 **Frozen**（2026-08-19）；Phase 3B Problem Schema **DONE**；Phase 3B-6 Problem Validator v1 已实现
* Attempt Schema v1 **Frozen**（2026-08-19）；Phase 3C-5 **DONE**；下一阶段 Phase 3C-6 Attempt Validator v1
* Method Schema / Error Mode Schema 仍待设计。

说明：

* Schema 已冻结 ≠ Implementation 已完成；
* 模板、validator、索引属 Post-Freeze Implementation；
* domain 受控词表、aliases 术语增强等属 Future Enhancement。

本阶段明确不做（除非单独授权）：

* 批量给现有文件添加 YAML；
* 自动给现有文件正式编号或执行 `P001` → `P0001`、`EM001` → `EM0001` 迁移；
* 修改已有正式知识条目与正式题目正文；
* 未经授权修改知识模板；
* 创建 RAG、向量库、多 Agent。

**Attempt Schema v1 正式冻结日期：** 2026-08-19

**Attempt Freeze basis（摘要）：**

* §22 Final Candidate → §8 Frozen contract（含 `attempted_at` dual-format）
* Real Pilots：`A000001`、`A000002`；Final Review PASS；Hard Freeze Blockers = 0
* Formal technical authority：本文件 §8

---

## 19. 当前待设计事项

### 19.1 Knowledge Schema（已冻结）与后续对象

- [x] Knowledge Schema v1 Candidate 字段设计
- [x] Knowledge Schema 跨章节一致性收口
- [x] K0001 首个真实对象试点
- [x] K0002 从零创建试点
- [x] K0002 reviewed 生命周期试点
- [x] K0001 → K0002 prerequisite 真实引用试点
- [x] Knowledge 引用目标状态规则验证
- [x] K0001 数学内容 reviewed 审核
- [x] K0001 reviewed Metadata 完整化
- [x] Knowledge Schema v1 Candidate 最终验收
- [x] Knowledge Schema v1 冻结前最终修复
- [x] Knowledge Schema v1 正式冻结（2026-08-19）

说明：Knowledge **Schema** 已完成并冻结。以下是 Implementation / Rollout，不是「Schema 尚未完成」。

### 19.1b Knowledge Post-Freeze Implementation

- [ ] 同步知识库模板（`01_知识库/知识库模板.md`）
- [ ] 实现 Knowledge validator
- [ ] 自动索引 / 反向引用
- [ ] 后续 Knowledge Metadata 迁移 / 按 v1 接入

### 19.1c 其他对象 Schema

- [x] Phase 3B-1 Problem Schema v1 Candidate（身份契约 + 最小字段；未冻结）
- [x] Phase 3B-2 Dual Real Pilot Validation（Research Pilot A + Ordinary Multipart Pilot B）
- [x] Phase 3B-3 Controlled Pilot Migration + Problem Candidate Gate v0.1（`P0001`/`P0002`；未冻结）
- [x] Phase 3B-4 Candidate Final Review（Passed — Ready for Formal Freeze）
- [x] Phase 3B-5 Formal Problem Schema v1 Freeze（2026-08-19）
- [x] Phase 3B-6 Problem Validator v1
- [x] Phase 3C-1 Attempt Semantic Contract（§19 in `学习证据架构.md`）
- [x] Phase 3C-2 Attempt Schema v1 Candidate
- [x] Phase 3C-3 Real Attempt Pilots（A000001/A000002）
- [x] Phase 3C-4 Attempt Candidate Final Review
- [x] Phase 3C-5 Attempt Schema v1 Formal Freeze（2026-08-19）
- [ ] Phase 3C-6 Attempt Validator v1
- [x] Phase 3D Method Semantic / Relation / Candidate / Pilot / Final Review（`Method与ErrorMode架构.md`）
- [x] Phase 3D-7M Method Schema v1 Formal Freeze（2026-08-20）
- [ ] Phase 3D-8M Method Validator v1
- [ ] Error Mode Schema 字段设计

### 19.2 需要人工决定的问题

- [x] Legacy metadata：`P001` 正文旧编号已让位于 YAML `id: P0001`（文件名 cleanup deferred；未假定文件名必须立即改为 P0001）
- [ ] Legacy template sentinel mismatch：`02_题目库/题目模板.md` 使用 `P000`，规范 sentinel 为 `P0000`（本轮不改模板）
- [ ] 旧目录 `未解决/` / `研究中/` / `已解决/` 与 Problem Object Status（`draft`/`reviewed`/`archived`）如何并存或迁移
- [x] legacy structured registration date 策略（`created` = 结构化对象登记日；P0001 接受）
- [ ] P0002 Content Review（标量域 / domain / codomain；非 Schema Freeze Blocker）
- [ ] 现有仓库编号 `EM001`–`EM005`（位于长期记忆汇总文件）是否升级为独立 Error Mode 对象（推荐格式为 `EM0001`；迁移尚未执行）
- [ ] Method / Error Mode 推荐主位置：Method **`12_方法库/`** 已 Frozen；Error Mode 待 Schema 定稿
- [x] `status`：Knowledge、Problem、Method 均已定义 `draft`/`reviewed`/`archived`（各自 Frozen Schema）；Error Mode 待 Schema
- [x] 确立原则：人工维护权威方向；可推导反向关系由程序生成（各对象具体权威字段仍由各自 Schema 定义）
- [x] 首个正式 Knowledge 对象已分配：`K0001` = 勒让德变换
- [x] Knowledge `prerequisites` 首条真实引用已落地：`K0001 → K0002`（`related: []` 合法；非空 related 可待自然出现后再测）
- [x] domain v1 定为非空 string；受控词表延期为 Future Enhancement
- [x] prerequisites 图必须为 DAG；related 允许成环
- [ ] K0001 正文「来源研究」如何随 Problem Schema 迁移（不阻塞 Knowledge Schema v1；属 Problem 侧）

### 19.3 后续实施顺序（仅规划）

Knowledge（已冻结）→ Implementation：

1. 同步知识库模板；
2. 实现 Knowledge validator；
3. 自动索引与反向引用；
4. 后续 Knowledge 按 v1 接入。

其他对象仍按：Candidate → 一致性检查 → 真实试点 → 修正 → 冻结 → 模板 → 自动化。

说明：

* Knowledge 已独立完成 Candidate → 试点 → 冻结；
* Problem Schema v1 **Frozen**（2026-08-19）；Phase 3B Schema 工作 DONE；Problem Validator v1 已实现
* Attempt Schema v1 **Frozen**（2026-08-19）；Phase 3C-5 **DONE**；下一阶段 Phase 3C-6 Attempt Validator v1（不改 Frozen 字段集）
* Method Schema v1 **Frozen**（2026-08-20）；Phase 3D-7M **DONE**；下一阶段 Phase 3D-8M Method Validator v1
* Error Mode 仍待设计，后续采用同样流程；
* 不要求四种对象全部完成才能推进 Knowledge Implementation。

---

## 附录 A. 最小 Front Matter 示例（无扩展字段）

仅演示结构层最小形态，不代表各对象最终必填集。Knowledge / Problem / Method 的 `status: draft` 均为合法值之一；Error Mode 的 `status` 枚举仍待 Schema。

示例 ID 使用规范保留编号：

```text
K0000
P0000
A000000
M0000
EM0000
```

`0000` 为 Schema 保留示例编号，不可分配给真实对象。真实对象从 `K0001` / `P0001` / `M0001` / `EM0001` 起。Problem：`P0001`、`P0002` 已正式分配；文件名 `P001_...` 为 legacy path。Error Mode 等历史编号迁移尚未执行。

```yaml
---
schema_version: 1
id: K0000
type: knowledge
title: 示例知识条目
status: draft
created: 2026-08-19
updated: 2026-08-19
---
```

```yaml
---
schema_version: 1
id: P0000
type: problem
title: 示例数学题目
status: draft
created: 2026-08-19
updated: 2026-08-19
---
```

```yaml
---
schema_version: 1
id: M0000
type: method
title: 示例数学方法
status: draft
---
```

```yaml
---
schema_version: 1
id: EM0000
type: error_mode
title: 示例错误模式
status: draft
created: 2026-08-19
updated: 2026-08-19
---
```

---

## 附录 B. 本轮明确不做清单

* 不修改 `01_知识库/数学变换/勒让德变换.md`
* 不修改 `02_题目库/已解决/P001_为什么勒让德变换这样定义.md`
* 不修改 `AGENTS.md`、`项目规则.md`（本轮原则上不动）
* 不修改 LaTeX / PDF / Python
* 不创建自动化程序
* 不引入 RAG / 向量数据库 / Lean / 多 Agent。
