# MATH-AI-LAB 学习证据架构

**副标题：** Evidence-Driven Mathematics Learning Architecture  
**文档状态：** Phase 3 Architecture Baseline  
**阶段：** Phase 3C-6 — Attempt Validator v1（**DONE**）  
**最后更新：** 2026-08-19

本文件不是 Frozen Schema 技术事实源。Knowledge / Problem / **Attempt** 的正式 technical authority 均在 `元数据规范.md`（Attempt → §8）。本文件承载 **职责边界、Attempt 设计历史**（§19–§24）、**Semantic Contract / Candidate / Pilot / Final Review / Freeze / Validator 历史**。

> **Phase 3C-1** Semantic Contract · **§20** Candidate · **§21** Pilots · **§22** Final Candidate · **§23** Freeze · **§24** Validator · **Phase 3C COMPLETE**  
> Attempt Schema v1：**FROZEN**；Attempt Validator v1：**COMPLETE**（`tools/attempt_validator/`）。

---

## 1. 文档定位

`学习证据架构.md` 是 Phase 3 的架构级规范。

它 **是**：

* 静态数学对象、学习证据、派生学习状态、推荐输出的职责定义；
* Source of Truth 分层；
* 禁止重复维护的数据边界。

它 **不是**：

* Knowledge 内容；
* 长期记忆快照；
* Schema 字段表；
* 学习状态数据库；
* 自动生成索引；
* 算法实现。

与现有文件的职责：

| 文件 | 职责 |
| --- | --- |
| `项目规则.md` | 项目总体制度与工作台事实源 |
| `元数据规范.md` | 数学对象 Metadata 技术规范 |
| `AGENTS.md` | Agent 执行边界 |
| `学习证据架构.md` | Phase 3 学习证据分层与职责 |
| `派生学习状态架构.md` | Phase 3E Derived Learning State 语义 authority |
| `01_知识库/_索引/` | Knowledge 派生索引（Phase 2） |

本文件不修改 `元数据规范.md` 的 Frozen contract（Attempt Schema v1 已于 Phase 3C-5 迁入 `元数据规范.md` §8）。Knowledge Schema v1、Problem Schema v1、Attempt Schema v1 均已 Frozen。Method / Error Mode Schema 仍待设计。**Attempt 设计历史** §19–§23；**Attempt 正式字段 authority** → `元数据规范.md` §8。

**Derived Learning State（Phase 3E）** 的 boundary · provenance · descriptive/assessment/recommendation 分层 → **`派生学习状态架构.md`**（本文件 §4–§8 为 Phase 3A baseline；3E-0 起以新 authority 为准）。

---

## 2. Phase 3 核心目标

Phase 3 目标：建立 **基于学习证据的数学学习系统**（Evidence-Driven Mathematics Learning System）。

必须严格区分：

| 层次 | 回答的问题 |
| --- | --- |
| 数学对象本身 | 这是什么？ |
| 一次学习行为 | 这一次实际发生了什么？ |
| 当前掌握状态 | 根据现有证据，现在大概处于什么状态？ |
| 下一步建议 | 下一步应该做什么？ |

四者不互相替代，也不互相写回为对方的事实。

---

## 3. 为什么采用 Evidence-Driven

Phase 3 采用 **Evidence-Driven**，不采用 **Manual-State-Driven**。

系统不得主要依靠人工填写如下永久状态来判断学习：

* `mastery: 0.8`
* `difficulty: 7`
* `mistake_count: 4`
* `review_priority: high`

原因：

1. 这些值会随新的作答而过时；
2. 人工覆盖会丢失历史；
3. 无法解释“为什么现在认为已掌握”；
4. 与 Phase 2 已验证的治理模式冲突：权威事实只维护一次，可推导信息不人工重复维护。

学习状态应尽可能来自 **真实学习行为证据**。

Phase 2 已证明一种可复用哲学：

```text
Source Truth（Knowledge YAML）
  → Validator
  → Derived Index
  → Workflow
```

Phase 3 应复用同一哲学，而不是提前实现对应组件：

```text
Attempt Evidence
  → Future Validator
  → Learning State Engine
  → Recommendation
```

---

## 4. 四层架构

### 4.1 Layer 1 — Static Mathematical Objects

静态数学对象描述数学世界中相对稳定的对象。

包括：

* **Knowledge** — 概念、定理、变换、理论框架
* **Problem** — 一道具体数学题 / 研究任务
* **Method** — 可跨题复用的解题策略 / 推理方法
* **Error Mode** — 可重复出现的错误机制 / 失败模式

回答：**这是什么？**  
不回答：**我这次做得怎么样？**

### 4.2 Layer 2 — Learning Evidence

Phase 3 初期的核心证据对象：**Attempt**。

Attempt 表示：用户在某个时间点，针对某个 Problem 的一次真实作答 / 尝试。

同一道题的多次作答必须是多次 Attempt，不得覆盖为单一 `Problem.result`。

Attempt 是 **Learning Evidence Truth**。

### 4.3 Layer 3 — Derived Learning State

由 Static Objects + Learning Evidence **计算**得到。

例如（名称仅为概念，不是字段规范）：

* Knowledge / Method mastery
* Error recurrence
* Retention
* Weakness
* Review Due
* Personal Difficulty
* Success Rate
* Recent Performance

回答：**根据目前证据，我现在大概处于什么状态？**

原则：Derived，不是 Source of Truth。必须可删除、可重新计算。

### 4.4 Layer 4 — Recommendation / Action

根据 Derived Learning State 产生下一步建议。例如：

* 下一题练什么；
* 哪个 Knowledge 应复习；
* 哪个 Method 应专项练习；
* 哪个 Error Mode 最近反复出现；
* 哪道旧题需要重做；
* 是否增加间隔复习；
* 是否需要补前置知识。

回答：**下一步应该做什么？**

Recommendation 不是永久数学事实。默认视为 **derived / transient output**。

### 4.5 总数据流（Closed Learning Loop）

```text
Knowledge
Problem
Method
Error Mode
      │
      ▼
   Attempt
      │
      ▼
Derived Learning State
      │
      ▼
Recommendation
      │
      ▼
Next Learning Activity
      │
      ▼
New Attempt
      │
      └────────→ 循环
```

这是闭环学习回路。新 Attempt 只追加证据，不改写静态对象，也不把推荐结果写回为数学事实。

---

## 5. Source of Truth

三种权威层次。

### 5.1 Mathematical Object Truth

**Source of Truth：** Knowledge / Problem / Method / Error Mode

负责：数学对象本身的事实（定义、题面、可复用方法、可复用错误机制、对象间静态关系）。

Knowledge YAML 是 Knowledge 的权威事实源。未来 Problem / Method / Error Mode 各自 Schema 定稿后，对象 YAML 是对应对象的权威事实源。

### 5.2 Learning Evidence Truth

**Source of Truth：** Attempt

负责：真实发生过的学习行为。

### 5.3 Derived State

**不是**原始 Source of Truth。

包括但不限于：mastery、success_rate、mistake_count、review_due、weakness、personal difficulty。

应由 evidence 计算。即使未来落盘为 JSON / 汇总文件，也只是缓存。丢失后必须能从 Attempt 重建。

这与当前治理一致：

* Knowledge YAML → Source Truth
* `01_知识库/_索引/` → Derived

禁止 Derived State 与静态对象双向维护同一事实。

---

## 6. Static Object 职责

本轮只定职责，不定字段表。

`元数据规范.md` 已保留文档示例编号：`K0000` / `P0000` / `M0000` / `EM0000`。它们是 Schema documentation sentinel，永不分配给真实对象。本文件只引用，不改 ID 规则。

### 6.1 Knowledge

是什么：具有独立长期学习价值的数学知识单元。

不是什么：用户掌握程度、复习计划、错题次数。

与 Frozen Knowledge Schema v1 一致：Knowledge **不**保存 `mastery`、`review_priority`、`mistake_count`、`last_reviewed`、`success_rate`、`source_problems`、`used_in_problems`。Phase 3 不得回头给 Frozen Knowledge 增加这些学习状态字段。

### 6.2 Problem

是什么：题目 / 研究任务本身。

未来 Schema 可能描述（方向，非本轮字段）：题面、数学领域、关联 Knowledge、题目来源、数学类型、标准解答、可能适用的 Method、其他静态数学属性。

**不**保存用户不断变化的：

* result / current result
* current_mastery
* mistake_count
* last_attempt_result
* last_reviewed
* success_rate
* personal_difficulty
* review_priority

这些属于 Attempt 或 Derived State。

Problem Schema 字段设计属于 **Phase 3B**，本文件不开始。

### 6.3 Method

是什么：可跨题复用的数学策略 / 推理模式。

例如：构造辅助函数 + Rolle 定理；用 Hessian 判断凸性；通过特征值判断二次型；换元转化为标准积分；利用对称性降低计算复杂度。

不是什么：某一道题的完整解答。Problem 正文可以有具体解答；Method 保存可复用方法模式。

Method **不**把 `mastery` 作为人工 Source Truth。Method mastery 应由使用该 Method 的 Attempt 及其结果推导。

Method Schema 属于 **Phase 3D**。

### 6.4 Error Mode

是什么：可重复出现的失败机制。

例如：把充分条件当必要条件；忽略定义域变化；Hessian 半正定就误判严格凸；换元后忘记修改积分上下限；矩阵乘法次序错误；把局部结论误当全局结论。

不是什么：某道题做错这一次。

Error Mode **不**人工维护 `mistake_count`、`last_seen`、`frequency`、`recurrence_rate`。这些由 Attempt 聚合，属于 Derived State / Analytics。

Error Mode Schema 属于 **Phase 3D**。

---

## 7. Attempt 职责

> **Phase 3C-1 更新：** 正式语义见 **第 19 节**。下列小节保留 Phase 3A 职责摘要；若与 19 节冲突，以 19 节为准。

### 7.1 核心语义

Attempt = **Learning Evidence Truth**：针对一个确定数学评价 target 的可独立评价的原始学习证据（非当前 mastery）。

**正式定义（3C-1）：**

> Attempt 是在一次 solving/reasoning episode 的上下文中，针对一个确定的数学评价 target 所形成的一条可独立评价的原始学习证据。

Solving/reasoning **episode** 是过程上下文；**Attempt** 是 target-scoped evidence unit。**Episode ≠ Attempt**；一个 episode 可产生多条 Attempt records；一条 Attempt 只对应 **一个** evaluation target。

具体 YAML 字段、required 规则见 **§20**（Phase 3C-2 Candidate）。

### 7.2 为什么必须与 Problem 分离

错误设计：

```text
Problem X:
  result: correct
```

同一道题多次做后，`correct` 会覆盖“不会 / 错误 / 提示后正确”，学习历史消失。

正确设计：

```text
Problem X          ← 题目本身不变
Attempt #1         ← 第一次证据
Attempt #2         ← 第二次证据
Attempt #3         ← 第三次证据
```

### 7.3 历史原则：append-oriented

新的尝试：**新增**新的 Attempt。

不要用覆盖旧 Attempt 表示新的学习结果。

第一次错误，不能因为第二次做对就把第一次改成正确。历史证据必须保留。

### 7.4 纠正边界

Attempt **不是**绝对 immutable forever。

允许：因 **事实性录入错误** 纠正。

禁止：因为“后来掌握了”而重写历史 Attempt。

### 7.5 结果不等于错误模式出现

一次 Attempt 最终可能正确，过程中仍可能暴露 Error Mode（开始理解错、随后自行纠正）。

**Result** 与 **Error Mode occurrence** 不是同一个概念。

### 7.6 实际方法不等于推荐方法

Problem 未来可能标明适用 / 推荐 Method。用户 Attempt 可能使用另一种 Method 且做对。必须区分：

* Problem 的适用 / 推荐 Method（静态对象属性，待 Schema 决定）
* Attempt 实际使用的 Method（证据）

### 7.7 结果（outcome）语义

3C-0 曾列举多种结果概念且 **不冻结 enum**。Phase 3C-1 已冻结 **六种 outcome 语义值**（非 YAML 字段名）：`correct`、`incorrect`、`partial`、`unsolved`、`abandoned`、`unassessed`。定义与判定优先原则见 **§19.8–19.9**。

Assistance（`independent` / `assisted` / omitted）与 outcome **正交**；不要用 `hint_correct` 等合并枚举膨胀 result。

### 7.8 证据质量

独立正确优于提示后正确；闭卷重做优于刚看完答案立即复现；14 天后正确优于 5 分钟后重复正确。

Learning State Engine **未来可以利用** evidence quality。本轮 **不设计评分权重**。

### 7.9 Review Event 暂不单独建模

Phase 3 初期：重做旧题 = 新的 Attempt。由此已能获得 retention evidence。

不单独设计 ReviewEvent Schema。

知识阅读型复习（只复习笔记、没有做题）记为 Future Extension。

不提前建立万能 `LearningEvent` 继承体系。当前最关键证据是 Problem Attempt。

---

## 8. Derived State 职责

Derived Learning State 由引擎从 Static Objects + Attempt 计算。

包括（概念层）：

* Mastery — Derived。不假设必须是 `0.0–1.0` 单一永久数字。未来可综合 correctness、independence、hint usage、recency、spaced retention、repeated success、transfer、error recurrence。本轮不定义算法。
* Success rate — Attempt aggregation，不是 Knowledge / Problem 人工字段。
* Mistake count — Attempt count，不是 Error Mode 静态 Metadata。
* Review due / Weakness / Retention / Recent performance — Derived。
* Personal difficulty — 这道题对当前用户有多难，主要由 Attempt 推导，不是永久 Problem truth。

**Intrinsic difficulty**（题目一般难度）即使未来存在，也只是 Problem 静态属性 **候选**，不是本轮字段。它与 personal difficulty 必须分开。

删除重建原则：未来即使存在类似 `mastery.json` 的缓存，丢失后必须能通过 Attempt 重建。Derived State 不得成为唯一证据。

单向派生：

```text
Attempt
  → Learning State Engine
  → Derived Mastery
```

禁止：Attempt 更新 Knowledge.mastery，再用 Knowledge.mastery 作为 Attempt 判断事实（双向事实循环）。禁止 Derived State 写回静态对象 YAML。

任何“你已掌握某 Knowledge”的系统结论，原则上应能追溯所依据的 Attempt evidence（explainability / provenance）。本轮只记录原则。

---

## 9. Recommendation 职责

Recommendation 根据当前 Derived State **临时计算**。

“今天优先复习凸函数”不是永久 Source Truth。

可重新生成。若未来保留推荐历史，属于 analytics / audit，不属于数学事实层。

推荐应可解释，例如：最近相关 Attempt 中多次出现某 Error Mode；较长时间没有成功 recall；该 Knowledge 又是另一 Knowledge 的前置。本轮不实现算法，也不引入推荐 ML 模型。

---

## 10. 数据生命周期

| 层 | 生命周期 |
| --- | --- |
| Static Object | 创建 →（审核）→ 使用 → 归档；ID 不回收 |
| Attempt | 发生 → 记录 → 保留；原则上追加；仅纠录入错误 |
| Derived State | 计算 → 失效 → 重算 |
| Recommendation | 计算 → 使用或忽略 → 随状态更新 |

必须区分两类时间：

* 对象何时创建（静态对象生命周期）
* 学习行为何时发生（Attempt 发生时间）

例如：Problem 创建时间 ≠ Attempt 发生时间。本轮只记录该架构要求，不设计字段名。

---

## 11. 关键关系边界

本轮是 Responsibility Architecture，不是 Graph Schema。不定字段名、方向必填性、target status。

未来 **允许** 静态关系（语义）：

* Problem → Knowledge
* Problem → Method
* Method → Knowledge
* Error Mode → Knowledge

Evidence 到静态对象：Attempt 未来可能引用 Problem、Method、Error Mode；也可能经 Problem 关联 Knowledge。Attempt 是否直接重复记录 Knowledge，留给 Attempt Schema 根据完整性、查询能力、冗余风险决定。本轮不定。

---

## 12. 禁止的建模反模式

### 1. `Problem.result` 表示用户当前最终结果

禁止。结果属于 Attempt。

### 2. `Knowledge.mastery` 作为人工 Source Truth

禁止。

### 3. `Method.mastery` 作为人工 Source Truth

禁止。

### 4. `ErrorMode.mistake_count` 人工维护

禁止。

### 5. `Problem.success_rate` 人工维护

禁止。

### 6. 覆盖旧 Attempt

禁止用后来成功覆盖历史失败。

### 7. Derived State 写回静态对象

禁止。

### 8. Recommendation 作为永久数学事实

禁止。

---

## 13. 推荐的建模模式

| 对象 / 产物 | 角色 |
| --- | --- |
| Problem | 静态题目 |
| Attempt | 一次真实作答 |
| Method | 可复用策略 |
| Error Mode | 可重复错误机制 |
| Attempt history | 学习证据（追加） |
| Learning State | 从 evidence 派生 |
| Recommendation | 从 state 派生（可再生成） |

---

## 14. 完整学习证据示例

以下为 **架构示例**，不是当前算法，不创建真实对象，不占用真实 ID。

`P0000` / `M0000` / `EM0000` 仅为 `元数据规范.md` 已保留的 Schema documentation sentinel 的概念引用，**不是**本轮分配。Attempt 编号使用 `Attempt #1` 等概念伪代码，**不代表**未来 Attempt ID 方案。

```text
Problem X
  关联知识（概念）：凸函数、勒让德变换
  可能适用的方法（概念）：某可复用凸性判定策略
  题目本身不保存 result / mastery

Attempt #1
  → 不会
  → 使用提示
  → 暴露“忽略定义域”这类错误模式
  → 保留为历史证据

Attempt #2
  → 独立正确
  → 不修改 Attempt #1

（间隔约 14 天）

Attempt #3
  → 独立正确
  → 不修改 Attempt #1 / #2

Derived State（示例，非算法输出）
  凸函数：掌握证据增强
  该错误模式：近期未复发
  该题 personal difficulty：相对下降

Recommendation（示例，可再生成）
  暂不立即重复该题，转向迁移题
```

---

## 15. Phase 3 路线图

### Phase 3A — Learning Evidence Architecture

DONE。职责边界与四层架构已冻结。

### Phase 3B — Problem Static Object Foundation

DONE。Problem Schema v1 Frozen；Validator / Authoring Template 已稳定。Problem Indexer 与 Problem Workflow **Deferred Infrastructure**（等 Attempt 真实 pilots 后再评估 Index 粒度）。

### Phase 3C — Attempt / Learning Evidence Foundation

- **3C-0** Inventory + Boundary Review — DONE（§18）
- **3C-1** Identity + Evidence Contract — DONE（§19；**Semantic Contract Frozen**）
- **3C-2** Attempt Schema v1 Candidate — DONE（§20；**Candidate Baseline**；**historical — superseded by Frozen v1 §`元数据规范.md` §8**）
- **3C-3** Real Attempt Pilots — DONE（§21；A000001/A000002）
- **3C-4** Attempt Candidate Final Review — DONE（§22；**Final Candidate**；**Freeze Readiness: YES**）
- **3C-5** Schema Freeze — **DONE**（§23；Attempt Schema v1 **FROZEN** → `元数据规范.md` §8）
- **3C-6** Attempt Validator v1 — **DONE**（`tools/attempt_validator/`；read-only；Problem dependency-aware）

**Phase 3C — COMPLETE**（Attempt / Learning Evidence Foundation）

### Phase 3D — Method + Error Mode Schema

定义可复用方法与可复用错误模式。可先 Method 后 Error Mode，或分别设计。不要求一次同时冻结。

### Phase 3E — Learning State Engine

从 Attempt 推导 Knowledge State、Method State、Error Statistics、Retention、Weakness、Review Due。

### Phase 3F — Practice / Review Engine

根据 state 选择下一步练习与复习。

### Phase 3G — Adaptive Learning Loop

学习 → Attempt → State → Recommendation → 新学习 → 新 Attempt。

---

## 16. Future Extensions

以下 **不是** Phase 3A blocker，本轮不实现、不设计字段：

* Knowledge Creator
* ID Allocator
* Watch Mode
* Git Hook / CI
* RAG / Embedding / Multi-Agent
* universal LearningEvent
* ReviewEvent / LearningSession
* 知识阅读型复习事件
* sophisticated mastery algorithm
* adaptive difficulty model
* graph centrality
* recommendation ML model
* 评分权重 / review interval / confidence 公式

---

## 17. Phase 3A 决策摘要

| # | 决策 |
| --- | --- |
| 1 | Knowledge / Problem / Method / Error Mode = Static Mathematical Objects |
| 2 | Attempt = Learning Evidence |
| 3 | Mastery = Derived |
| 4 | Weakness = Derived |
| 5 | Success Rate = Derived |
| 6 | Mistake Count = Derived |
| 7 | Review Due = Derived |
| 8 | Personal Difficulty = Derived |
| 9 | Recommendation = Derived / Transient |
| 10 | Problem 不保存用户当前 result |
| 11 | 历史 Attempt 不因后续成功而覆盖 |
| 12 | Method ≠ 单题完整解答 |
| 13 | Error Mode ≠ 单次错误记录 |
| 14 | Attempt Schema 尚未设计 |
| 15 | （历史）当时 Problem Schema 尚未开始；**现已 Frozen**（Phase 3B） |
| 16 | （历史）下一阶段曾为 Phase 3B；**现已进入 Phase 3C-0** |

Phase 3A 只冻结职责边界与数据分层。不冻结 Attempt / Method / EM 字段、Attempt ID、mastery 算法、recommendation 算法。

---

Phase 3A 结束时下一阶段曾写为 Problem Schema Candidate；该阶段已完成。Attempt 细节见第 18 节 Phase 3C-0。

---

## 18. Phase 3C — Attempt / Learning Evidence Foundation

### 3C-0 Attempt Evidence Inventory + Boundary Review

**状态：** Architecture / Pre-Schema。不是 Candidate，不是 Frozen。本轮不引入 `schema_version`、不分配 A ID、不写 YAML 字段表。

标记约定：

* **DECIDED** — 3C-0 架构结论，后续 3C 不得无理由推翻
* **RECOMMENDED FOR 3C-1** — 进入 Identity + Evidence Contract 时优先采用，尚未冻结
* **DEFERRED** — 不进入 Attempt v1 概念核心
* **OPEN** — 必须在 3C-1 决策，本轮不定案

---

#### 18.1 Inventory Findings

当前仓库 **没有** 任何正式 Attempt 对象。已有表达多数是：静态对象、研究工作流、或人工维护的 legacy 学习状态。

| Concept | Current Location | Current Meaning | Layer | Attempt Relevance | Decision |
| --- | --- | --- | --- | --- | --- |
| Problem statement / 题面 | `P0001` / `P0002` Markdown | 数学任务本身 | Static / Problem | Target 的内容，不是 Attempt | 保持在 Problem |
| Problem.status | Problem YAML | `draft` / `reviewed` / `archived` | Static lifecycle | 不表达会不会做 | 与 Attempt 分离 **DECIDED** |
| 未解决 / 研究中 / 已解决 | 目录；`P0001` 正文；`已解决问题索引.md` | legacy research workflow | Workflow | 不是 result | 不迁入 Attempt |
| 研究日志 | `P0001` 文末 | 任务推进 / 归档记录 | Workflow | 不是一次作答证据 | 不回填为 Attempt |
| 候选思路 / 最终思路 / 详细推导 | `P0001` | 研究型题的数学内容与结论 | Problem body | 标准/研究解答 ≠ 用户这次怎么做 | 与 Attempt body 分离 **DECIDED** |
| 待审核数学陈述 | `P0002` | Content Review，非用户作答 | Problem / workflow | 无 | 不进入 Attempt |
| parts a/b/c | `P0002` YAML | Frozen local anchors | Problem | Attempt 只能引用已有 anchor | 适配 Frozen parts **DECIDED** |
| 易错点列表 | `P0001` 第十节 | 题目静态注意事项 | Problem | 可提示未来 EM，不是 occurrence | EM 定义 ≠ Attempt |
| EM001–EM005 | `错题与错误模式.md` | 可复用失败机制草稿 | Legacy ≈ future Error Mode | 定义层 | 不把频率当事实源 |
| 出现频率 | 同上总览表 | 人工待补充计数 | Legacy derived | 未来由 Attempt 聚合 | 不迁入 Attempt YAML |
| 当前薄弱点 | `当前学习状态.md` | 人工学习快照 | Legacy Derived / human state | 未来由 engine 计算 | 保留人工；不伪造 Attempt |
| 正在学习 / 需巩固 | 同上 | 学习主线 | Legacy human state | Recommendation 输入候选 | 非 Attempt |
| 个人目标与解释偏好 | `个人学习档案.md` | 长期偏好 | Governance | 不属 evidence 事件 | 排除 |
| 知识地图 | `数学知识地图.md` | 导航 / 汇总 | Derived / index | 无 | 排除 |
| P001 已解决索引 | `已解决问题索引.md` | workflow 导航；编号仍写 P001 | Legacy index | 非 Attempt | 不手工维护 reverse attempts |
| mastery / mistake_count / review_due 等 | 规范与模板中列为 **禁止写入 Problem/Knowledge YAML** | 明确排除的动态概念 | Derived | Excluded from Attempt | 已与 Frozen Schema 一致 |
| hint / confidence / duration | 仅架构讨论与模板排除列表 | 尚无真实填写实践 | 见 18.8 | 见分类 | 无真实数据，避免 field shopping |
| Chat / Agent 会话 | 不在仓库对象中 | 交互载体 | Carrier | **不等于** Attempt | **DECIDED** |

**冲突记录（本轮不迁移）：**

* `错题与错误模式.md` 总览表「出现频率」若被当作长期事实源，将与「mistake_count 必须 derived」冲突。现状多为「待补充」，视为 **Legacy Human-Maintained State**。
* `已解决问题索引.md` 仍用 `P001` 与「状态：已解决」，与 YAML `id: P0001` / `status: reviewed` 是不同层；属 workflow 索引漂移，不是 Attempt 字段冲突。
* Frozen Schema 与模板 **已经禁止** Problem/Knowledge 保存 mastery / result / mistake_count。未发现真实对象 YAML 写入这些字段。

**Backward compatibility（原则，非 migration）：** Legacy 学习状态文件保留。未来 Attempt 从正式启用日起成为 Evidence Source of Truth。无明确时间、结果、target 的旧记录 **不得伪造** 为历史 Attempt。高置信度迁移留待更后阶段。

---

#### 18.2 Attempt Candidate Definition

**3C-1 refined：** 见 **§19.2** 正式定义。下列 3C-0 表述保留为 inventory 摘要。

**DECIDED（3C-0 摘要；target 粒度已由 3C-1 锁定）：**

> Attempt 是用户针对一个已识别数学 Problem target（整题，或该题一个已 Frozen 的 part anchor）所发生的一次可区分的解题 / 推理尝试，留下的原始学习证据。它记录「这一次实际发生了什么」，不记录「用户现在会不会」。

一次 ChatGPT / Cursor conversation **不是** Attempt。聊天只是载体。研究型 Problem（如 `P0001`）可以产生 Attempt，但现有研究稿与研究日志 **不是** 自动 Attempt，也不得回填伪造。

Attempt **不是** 全部用户行为日志：必须有清晰数学任务 target。

---

#### 18.3 Evidence Truth Principle

**DECIDED：** Attempt = Learning Evidence Truth，含义是记录发生过的事实，不是当前 mastery。

Derived Learning State 必须可删除、可从 Attempt 重算。Recommendation（Next Problem、Review Due、priority、score）**不得**写回 Attempt。Attempt 不得因 derived 更新而被改写。

---

#### 18.4 Attempt Boundary

**3C-1 refined：** 旧候选曾倾向「一次连续 solving/reasoning episode = 一次 Attempt」。**3C-1 final review 修正为：** episode = 过程上下文；Attempt = target-scoped evidence unit（§19.3–19.5）。下列方案比较保留为 3C-0 历史记录。

什么构成一次 Attempt——比较：

| 方案 | 含义 | 评价 |
| --- | --- | --- |
| A | 一次可区分的独立解题 / 推理过程 | 语义稳定；兼容重做、multipart、research；与刷题「提交」不完全等同 |
| B | 一次聊天 / session | 过粗；一会话可混多题；载体 ≠ 数学事件 |
| C | 一次提交答案 | 对考试题可操作，但研究证明/卡住/放弃没有单一 submit |

**DECIDED（3C-0）：** 采用方案 A 的**过程语义**作为 episode 语境，但 **不再** 等同「一条 Attempt」。不采用 B。C 可作为普通题的常见实现形态，但不是 Attempt identity 定义。

「一次过程」的起止边界 → **3C-1 §19.4 Episode boundary**（解题意图 + 信息状态连续性；禁止固定时间阈值）。

---

#### 18.5 Reattempt vs Correction

**DECIDED：**

* **Reattempt：** 同一 Problem（或同一 part）上后来又发生一次真实作答 → **新建 Attempt**。Later success **不得**覆盖 earlier failure。
* **Correction：** 登记错误（例如当时其实做对，却误记为 incorrect）→ 修正该条记录的事实，**不是**新 Attempt。
* 第一次 incorrect、后来重做 correct → 两条 Attempt，不是 correction。

Attempt **append-oriented**：**DECIDED**。Correction 具体存储字段 → **§20**（3C-1 仅冻结语义：§19.14–19.15）。

覆盖旧 Attempt 表示「现在会了」——禁止。

---

#### 18.6 Problem / Part Targeting

**DECIDED：**

* Attempt 必须引用 Problem，权威方式是 **Problem YAML `id`**（`^P\d{4}$`）。禁止用路径或标题当 identity。
* Multipart 时，若只做某一小问，target 为 **Problem ID + 该题 Frozen `parts` 中的一个 anchor**。不得复制题面或 part title 作为 identity。
* `P0002` 只做 (b)：未来定位为 `P0002` + anchor `b`，以便 Error Mode / Mastery / Review 能落到 part b。
* `parts` 一旦被 Attempt 引用即 history-sensitive；与 Frozen Problem Contract 一致。Attempt 适配 Problem，**不得**要求改 Problem Schema。
* Problem **不**维护 `attempts: [...]`。Knowledge **不**维护 attempts / mistake_count / mastery。反向列表一律 derived。

**3C-1 CONFIRMED（§19.6–19.7）：** Target = 整题 **或** Problem ID + **恰好一个** Frozen part anchor。禁止一条 Attempt 同时绑定多个 part（如 `a+b`）；禁止 `part_results` / 复合 identity `P0002#b`。

一次连续 session 内完成 `P0002(a)(b)(c)` → **三条** Attempt（评价粒度优先），无需虚构三个 episode。同一份 Evidence 不得在 whole + parts 上重复登记（§19.7）。

---

#### 18.7 Result Semantics Inventory

**3C-1 CONFIRMED：** 六种 outcome 语义见 **§19.8**（`unknown` 已由 `unassessed` 取代）。下列 3C-0 表格为 inventory 历史。

| 概念 | 粗语义 | 诊断价值 |
| --- | --- | --- |
| correct | 给出正确完成 | 成功证据 |
| incorrect | 给出明确但错误的答案 / 证明 | 与「不会」不同 |
| partial | 部分小问或证明片段正确 | multipart / 长证明 |
| unsolved | 无法完成；没有可判定的完整错误答案 | 卡住、不会做 |
| abandoned | 主动放弃 | 与时间耗尽/不会可重叠 |
| unassessed | （3C-1）缺可靠 Assessment basis | 低质量 / 待判定 |

**3C-0 历史行：** ~~unknown~~ → 3C-1 使用 `unassessed`。

**DECIDED 方向（3C-0，3C-1 已展开）：** incorrect vs unsolved 区分；assistance 与 outcome 正交（§19.10）。

---

#### 18.8 Optional Evidence Review

| 概念 | 是原始事实？ | 谁产生 | 谁消费 | 没有它失去什么 | 能否以后推导 | 3C-0 |
| --- | --- | --- | --- | --- | --- | --- |
| 粗粒度 assistance（独立 vs 看过提示/答案） | 是，若当时记下 | 用户/记录者 | State engine（证据质量） | 无法区分独立掌握与复现 | 不能从 result 单独推出 | **PROMOTE CANDIDATE**（只保留极粗粒度，不展开 hint taxonomy） |
| AI 分步 / 教材 / 查看答案细分 | 部分是 | 高填写成本 | 细诊断 | 过细 | 部分可由笔记推断 | **DEFER** |
| confidence | 用户自评事实 | 用户 | 弱；不可靠 | 几乎无 | 否 | **DEFER** |
| duration / time_spent | 若机器自动记则是事实 | 机器或人工 | 次要统计 | 不大 | 否 | **DEFER**（不强制人工填） |
| 单一发生时间 | 是 | 记录时 | retention / 排序 | 无法做间隔复习 | 否 | **PROMOTE CANDIDATE**（一个时间足够） |
| started_at + completed_at | 过程事实 | 高成本 | duration | 可用 duration 代替 | — | **DEFER** |
| 作答 / 推理 / 证明草稿 | 是 | 用户 | 人工复查、未来 EM 标注 | 只剩空洞 result | 否 | **PROMOTE CANDIDATE**（Markdown body，不进 YAML） |
| Method ID 引用 | 待 Method Schema | — | Method mastery | 可后补标注 | 可后标 | **DEFER** |
| Error Mode ID 引用 | 待 EM Schema | — | recurrence | 可后补 | 可后标 | **DEFER** |
| 自由文本 method/error | 弱事实 | 用户 | 迁移债务高 | — | — | **DEFER**（避免制造无法对齐的字符串事实源） |

**DECIDED：** Method / Error Mode Schema **不阻塞** Attempt Foundation。Attempt v1 **不要**提前写 `methods: []` / `error_modes: []`。

---

#### 18.9 Ownership Matrix

每项只标 **主归属**。

| Concept | Owner |
| --- | --- |
| Problem statement | Problem |
| Problem lifecycle (`status`) | Problem |
| Frozen `parts` anchors | Problem |
| 标准解答 / 研究结论 / 题面数学 | Problem Markdown |
| 工作流目录 未解决/研究中/已解决 | Workflow |
| 用户这一次的答案 | Attempt Markdown |
| 这一次的推理 / 证明草稿 | Attempt Markdown |
| 这一次的 result | Attempt |
| hint / assistance 粗证据 | Attempt（若进入 v1） |
| duration | Attempt 若将来记录；否则不维护 |
| confidence | Attempt 若将来记录；否则不维护 |
| 可复用解题策略定义 | Method |
| 此次采用了何方法（证据） | Attempt（Method Frozen 后 optional reference） |
| 可复用失败机制定义 | Error Mode |
| 此次错误实际发生 | Attempt（occurrence）；不是 EM 对象本身 |
| mastery | Derived State |
| success rate | Derived State |
| mistake count / error recurrence | Derived State |
| review_due | Derived State |
| personal difficulty | Derived State |
| retention / weakness | Derived State |
| recommendation / next problem / priority | Recommendation |

---

#### 18.10 Candidate / Deferred / Excluded

以下为 **conceptual inventory**，不是 YAML Schema。

**Candidate for Attempt v1（概念）：**

1. 稳定 Attempt 身份（**推荐有独立 ID**；格式 **OPEN**）
2. Problem target（P ID）
3. 可选单一 part anchor
4. 粗粒度 result（枚举未冻）
5. 一次发生时间
6. 粗粒度 independence / assistance 证据
7. Markdown：此次作答与推理

每个 Candidate 均满足：记录原始事实；后续 engine 或人工复查需要；不能从静态对象推出。

**Deferred：** Method/EM 引用；细 hint/AI taxonomy；confidence；duration；双时间戳；correction 字段形状；Attempt 是否直接列 Knowledge ID；文件目录布局细节；数据库。

**Excluded from Attempt：** mastery；weakness；success_rate；review_due；retention；personal_difficulty；recommendation_score；mistake_count 作为存档总数；Problem/Knowledge 上的 reverse attempt 列表；聊天 session ID 作为 identity；把研究日志当 Attempt。

---

#### 18.11 Future Pilot Scenarios

**不创建**真实 A 对象。供 3C 后续使用：

1. **普通单题历史：** 某非 multipart Problem，第一次 incorrect，间隔后重做 independent correct。验证 append、later success 不覆盖 earlier failure。
2. **Multipart 局部：** 以 `P0002` 为场景，用户只做 part `b`。验证 P ID + Frozen anchor；engine 能定位 b。
3. **提示辅助：** 先 unsolved / 不会做，得到提示后完成。验证 result 与 assistance 边界，且聊天记录本身不是 Attempt。

`P0001` 可作为 research-type 后续扩展场景，但 **不要**把现有研究稿回填为 Attempt #1。

---

#### 18.12 Identity, files, database

**3C-1 CONFIRMED：** Attempt ID `^A\d{6}$`；sentinel `A000000`；首个真实 ID `A000001`（§19.13）。一 Attempt record = 一 Markdown 文件（§19.17）。Future root `11_学习证据/尝试记录/`（§19.18；**本轮未创建目录**）。不用数据库作为 v1 必需。

---

#### 18.13 Problem lifecycle vs Attempt

**DECIDED：** `draft` / `reviewed` / `archived` 与 Attempt evidence 独立。`reviewed` ≠ 做对；`未解决` ≠ 没有 Attempt。

**draft Problem 上的 Attempt：DECIDED 允许。** 研究中临时题与正式刷题都应能留下证据。正式统计可信度由未来 engine 降权 / 过滤（例如更信任 reviewed Problem 上的 Attempt），而不是禁止记录。细节 **OPEN FOR 3C-1**（是否 WARNING、是否计入默认 mastery）。

**archived Problem：** 历史 Attempt **继续有效**，不因 archive 删除或失效。**DECIDED。**

---

#### 18.14 Dependency Review

```text
Knowledge (Frozen)
Problem   (Frozen)
      │
      ▼
Attempt   (尚未 Schema)
      │
      ▼
Derived Learning State Engine   (未开始)

Method / Error Mode  (未 Frozen)
  → 未来 optional reference / 后标注
  → 不阻塞 Attempt Foundation
```

自动化顺序 **roadmap（非实现）：** Knowledge Validator → Problem Validator → 未来 Attempt Validator → Derived Learning State Engine。

Problem Indexer / Problem Workflow 保持 **Deferred**。待 Attempt pilots 后再选：Problem Index vs Evidence Index vs Learning Index。

---

#### 18.15 Resolved Decisions（3C-0）

1. Attempt = 针对已识别 Problem target 的一次可区分解题/推理尝试的原始证据。
2. Attempt 是 Evidence Truth，不是当前 mastery。
3. Chat session ≠ Attempt。
4. Attempt ≠ 全部行为日志。
5. 新学习事件 append；later success 不覆盖 earlier failure。
6. Correction ≠ Reattempt。
7. Target = Problem ID；multipart 局部 = P ID + Frozen part anchor。
8. 只做 `P0002`(b) 时定位 `P0002`+`b`。
9. Problem.status 与 Attempt 分离；archive 不抹掉历史 evidence。
10. 允许对 draft Problem 记录 Attempt。
11. Research Problem 可以产生 Attempt；现有研究稿不自动等于 Attempt。
12. incorrect 与 unsolved 概念上应区分。
13. 作答与推理放 Markdown body；不把完整答案塞 YAML。
14. Problem 标准解答 ≠ Attempt 此次作答。
15. Method / EM 不阻塞 Attempt；v1 不预留空数组引用字段。
16. Derived 可重算；Recommendation 不写回 Attempt。
17. 无 reverse attempts 人工列表；无 Knowledge 动态学习字段。
18. 不需要数据库。
19. 不伪造、不批量迁移 legacy 记录。
20. 粗 assistance 与单一发生时间为 v1 概念候选；confidence / duration 延后。
21. ~~推荐独立稳定 ID 与一文件一 Attempt；格式与目录 **不冻结**~~ → **3C-1 已确认**（§19.12–§19.17）。
22. Problem Indexer / Workflow 继续 Deferred。

---

#### 18.16 Open Questions for 3C-1

**已关闭（2026-08-19）。** 决议见 **§19**。剩余 Schema 形状问题见 **§19.21**。

---

#### 18.17 Non-Goals（3C-0 / 3C-1 禁止）

* Attempt YAML Schema / Candidate Freeze
* Attempt 对象、ID、模板、Validator、Indexer、Workflow、Creator、目录、数据库
* Learning State Engine / Review Queue / Recommendation Engine
* 修改 Frozen Problem / Knowledge Schema 或真实 P/K 对象
* 把 `attempts` 写入 Problem YAML
* 开始 Problem Indexer / Problem Workflow
* 本阶段实现 Method / Error Mode Schema

---

## 19. Phase 3C-1 — Attempt Identity + Evidence Contract

**状态：** CONFIRMED / DONE（2026-08-19）

**Semantic Contract Frozen** — 下列语义在 Phase 3C 内具有约束力，直至经正式 Schema Change Review 修订。

**Complete Attempt YAML Schema — historical Candidate（§20）→ Frozen v1：`元数据规范.md` §8** — Validator rule ID、文件命名模板 → **3C-6**。

> Phase 3C-1 freezes the semantic contract. Phase 3C-2 maps it to Candidate shape (§20). Schema Freeze → Phase 3C-5.

Attempt Schema v1 当前为 **CANDIDATE**，**不是** Frozen Schema。`元数据规范.md` 在本阶段 **不** 增加 Attempt 字段表（**NEEDS SCHEMA SOURCE-OF-TRUTH REVIEW**）。

### 19.1 与 3C-0 的关系

3C-0 完成 inventory 与边界审查。3C-0 曾倾向「一次连续 solving/reasoning episode ≈ 一次 Attempt」——**3C-1 final review refined this boundary**：

```text
Solving / Reasoning Episode     ← 过程上下文
        │
        │ may produce one or more
        ▼
Attempt Evidence Records        ← target-scoped 原始证据
        │
        ├─ Target: whole Problem
        ├─ Target: P + one part
        └─ ...
```

**Episode ≠ Attempt。** 一个 episode 可产生多条 Attempt；一条 Attempt **只能** 对应 **一个** evaluation target。

**Chat session ≠ Solving Episode ≠ Attempt。** 不用消息条数、提交次数、屏幕停留时间定义 Attempt identity。

### 19.2 Attempt 正式定义

> Attempt 是在一次 solving/reasoning episode 的上下文中，针对一个确定的数学评价 target 所形成的一条可独立评价的原始学习证据。

Attempt 记录「这一次针对该 target 实际发生了什么」，不记录「用户现在会不会」（mastery 属 Derived State）。

### 19.3 什么才算真实 Attempt

Attempt 必须对应真实发生的 **solving / reasoning / mathematical work**。

以下 **不** 自动形成 Attempt Evidence：

* 打开 / 看到 / 收藏题目
* 阅读题面但未真正开始尝试

不要把「接触过 Problem」误当成 Attempt。

### 19.4 Episode boundary（≠ Attempt boundary）

**禁止** 固定时间阈值（例如离开 20 分钟 ⇒ 新 Attempt）。

Episode continuity 判断标准（**DECIDED**）：

```text
解题意图连续性
+
信息状态连续性
```

时间仅作辅助，**不能**作决定性规则。

示例：

* 出去吃饭 30 分钟后回来 **继续原推导** → 可仍属 **同一 solving episode**。
* 昨天已明确失败并结束，今天 **从头重做** → **新 solving episode** / reattempt → **新 Attempt identity**（针对同一 target 时）。
* **完整阅读标准答案** → 强 **information-state boundary**；之后闭卷重新解决 → **new solving attempt → new Attempt identity**。

**Episode boundary ≠ Attempt boundary：** 同一 episode 内可对多个独立 target 各形成一条 Attempt。

### 19.5 何时产生新的 Attempt identity

**Reattempt → new Attempt identity（DECIDED）。**

核心判断：

> 此前针对 **该 evaluation target** 的 Attempt Evidence 已经闭合，用户后来重新开始了一次 **可区分** 的 solving attempt。

典型 **new Attempt**：

* 第一次失败 → 之后重新做
* 第一次做对 → 以后复习闭卷重做
* 完整看完答案 → 之后闭卷重新解决
* 明确结束原尝试 → 之后重新开始

以下 **默认不自动** 产生新 Attempt identity（可能仍更新 assistance、outcome 语义所依据的 body、或 Assessment）：

* 短暂停顿
* 修正一个算术错误（同一次推导内）
* 同一次推导中换一种方法
* 得到提示后 **在同一 solving attempt 内** 继续
* AI 检查中间步骤后继续
* 用户重新措辞自己的证明

### 19.6 Target granularity（evaluation target）

一条 Attempt 的 evaluation target **只能是**：

```text
整个 Problem（Problem YAML id）
```

或：

```text
Problem id + exactly one Frozen part anchor
```

概念示例：`P0002` 或 `P0002 + b`。

**禁止：**

* 一条 Attempt 同时对应 `a + b` 或 `a + b + c`
* 设计 `parts: [a, b]` 于单条 Attempt
* 设计 `part_results:` 或多 outcome 列表于单条 Attempt
* 新复合 identity（如 `P0002#b`）

Problem ID 与 part anchor 分别遵守既有 **Frozen Problem Identity Contract**（`元数据规范.md` §7.2）。Attempt reference **不** 创建第二套 Problem identity 规则。

### 19.7 Multipart 与 Evidence double-counting

**原则（DECIDED）：**

> Evidence granularity follows evaluation granularity, not physical study-session granularity.

记录条数 **不由**「用户坐了几次」决定，而由 **独立评价 target** 决定。

`P0002` 的 `parts: [a, b, c]` 若 a/b/c 均为稳定且需独立诊断的单元，即使用户 **一次连续** 完成全部小问，也 **允许** 形成三条 Attempt Evidence：

```text
Attempt → P0002 + a
Attempt → P0002 + b
Attempt → P0002 + c
```

**无需** 虚构三个 solving episodes。

**防重复计数（DECIDED）：**

> 同一份数学学习 Evidence 不得同时在相互重叠的 target granularity 上重复登记。

若已登记 `P0002+a`、`P0002+b`、`P0002+c`，则 **原则上不再** 用 **完全相同** 的数学 Evidence 额外登记 `Attempt → P0002`（whole），以免 success rate / mastery / weakness 等 **double counting**。

**禁止的不是** whole-Problem Attempt 本身；禁止的是 **同一份 Evidence 被 whole + parts 重复登记**。若 whole Problem 作为 **独立完整评价事件**（与 part 级 Evidence 不可互换），可形成 **另一次** whole-Problem Attempt。

### 19.8 Outcome 语义（六种）

Phase 3C-1 **正式保留** 下列六个 **outcome semantic values**（语义名，非 YAML 字段形状）：

```text
correct
incorrect
partial
unsolved
abandoned
unassessed
```

**不** 使用 `unknown`、`failed`、`success`、`wrong`、`incomplete` 等作为同层替代。

| Outcome | 语义 |
| --- | --- |
| **correct** | 当前 evaluation target 被 **完整、可靠地** 正确完成。 |
| **incorrect** | 用户形成了 **可评价的完整** 答案/证明/数学结论，但存在 **决定性数学错误**，target 未被正确完成。不是「任意一处笔误」。 |
| **partial** | target 未完整正确完成，但已有 **可确认的、实质性** 正确数学进展（如核心推导前半正确后卡住）。 |
| **unsolved** | 用户 **真实尝试** 了 target，但未形成足够有效正确成果，不足以达到 partial。 |
| **abandoned** | solving process 因主动停止、时间限制、优先级变化等 **中止** 而结束，且 **尚未** 形成上述数学闭合结果（complete incorrect/correct/partial/unsolved 的判定）。若已有完整可评价答案，勿仅因「之后不继续」而改标 abandoned。 |
| **unassessed** | Attempt **确实发生**，但 **无足够可靠依据** 判断数学结果。不用 `unknown` 代替。 |

Outcome **不是** 连续分数；**不是** Derived State。

### 19.9 Outcome 判定优先（防 Recorder 漂移）

Recorder 应大致按下列 **优先逻辑** 选择 outcome 语义（非算法代码）：

```text
完整可靠正确 → correct

完整可评价但决定性错误 → incorrect

未完整但有实质正确进展 → partial

真实尝试但有效成果不足 → unsolved

过程因中止结束且尚无上述闭合 → abandoned

缺少可靠 Assessment basis → unassessed
```

### 19.10 Assistance 语义

**三种状态（DECIDED）：**

```text
independent
assisted
omitted / not reliably recorded
```

**核心原则：**

> 外部帮助是否 **实质改变** 用户解决 **当前 evaluation target** 所需的数学搜索空间、关键思路或下一步行动。

若明显改变 → **assisted**。若未改变 → 可保持 **independent**。

**Independent 精确定义（DECIDED）：**

```text
independent ≠ 完全没有接触任何外部资源
```

Independent 表示：当前 **target-scoped** solving process 的认知自主性 **未被实质外部帮助改变**。查已知公式若未提供关键方向，勿机械判 assisted。

**示例：**

* 「你第一步写得对」且未改变后续搜索路径 → 常仍为 independent。
* 「考虑 A 与 B 的特征值差」若即关键突破 → assisted。
* 完整查看标准答案 → 实质 assisted；闭卷 **重做** → **new Attempt**，新 Attempt 的 assistance **独立** 判断。

**Target-scoped（DECIDED）：** 同一 episode 内 `P0002(a)` independent、`P0002(b)` assisted、`P0002(c)` independent **可以** 并存——除非 (b) 的提示 **实质** 改变了 (c) 的搜索空间。

**后验检查（DECIDED）：** 用户已 **独立形成完整解答** 后再请 AI 检查/验证/核对 → **原则上不** 反向把该 solving process 改为 assisted。区分「帮助 **解决**」与「解决 **后验证**」。

**不** 创建复杂 assistance taxonomy（本轮）。

Assistance 与 outcome **正交**（3C-0 方向，3C-1 **CONFIRMED**）。

### 19.11 Assessment Authority

**Recording ≠ Assessment（DECIDED）。**

Recorder **不得** 无依据猜测 correct / partial / incorrect。

**原则：**

> Outcome 必须来自具有 **可说明依据** 的 Assessment。

Assessment 来源 **可以包括**（3C-1 **不** 规定完全相同的可信等级）：

* 用户明确确认
* 标准答案比对
* 可机械验证
* 人工数学审核
* AI 基于 **充分数学依据** 的判断

3C-1 **不** 采用「仅人工可 Assessment」或「AI 无条件自动 Assessment」任一极端。

**冻结：**

```text
Assessment must have an explainable basis.
Recorder must not invent an outcome.
```

**本轮不字段化：** `assessed_by`、`assessment_method`、`assessment_basis`、`assessment_confidence` 等 — **3C-2 未加入**（§20.3.10）；是否进入 Schema → **3C-4**。

### 19.12 Attempt identity（A ID）

**DECIDED：**

```regex
^A\d{6}$
```

示例：`A000001`、`A000002`。

**永久 sentinel：** `A000000` — documentation / template placeholder only；**不得** 分配给真实 Attempt。

**首个真实 Attempt ID：** `A000001`。

Attempt ID = **global stable identity**。**禁止** 使用时间戳、文件名、路径、Problem ID + 本地序号、chat session ID 作为 Attempt identity。

### 19.13 attempted_at 语义

3C-1 **只冻结含义**；YAML 类型、精度、格式 → **§20 Candidate**；Validator 规则 → **3C-6**。

> attempted_at 表示该 Attempt 所对应的 **target-scoped Evidence 形成完成** 时刻的 **最佳可得** 时间记录。

因 **Episode ≠ Attempt**，attempted_at **不是** 整个大 episode 的结束时间。例如同一晚依次完成 `P0002(a)`、`P0002(b)`、`P0002(c)` 可对应 **三个不同** attempted_at。

**明确区分：**

```text
attempted_at ≠ Markdown 文件创建时间
attempted_at ≠ 文件最后修改时间
```

### 19.14 Reattempt vs Correction

| | Reattempt | Correction |
| --- | --- | --- |
| **身份** | **new A identity** | **same A identity** |
| **含义** | 用户后来又进行了一次 **新的** solving attempt | 实际 solving evidence **未变**，仅 **登记事实有误** |

Correction 示例：Problem / part 写错、时间记错、已确认 outcome 登记错。

Reattempt 示例：第一次 incorrect，第二天重做 correct → **两条** Attempt，**禁止** 覆盖第一条。

### 19.15 Correction 可审计性

**DECIDED：**

```text
Correction preserves Attempt identity.
Correction must retain auditable provenance.
```

**不** 在 3C-1 冻结 correction 具体字段 → **§20.3.8**（3C-2 Candidate）。

### 19.16 History-bearing Problem / part reference

**不** 新建「History-Bearing Problem Identity Contract」。**完全复用** Frozen **Problem Identity Contract**（`元数据规范.md` §7.2）。

Attempt 引用 Problem / part 的作用：让既有 identity 规则承担 **历史引用责任**——

> 修改以后，历史 Attempt 是否仍然是在回答 **同一个数学任务**？

* 措辞澄清、等价重述、排版、solution 改进、非语义说明 → **保留** 原 P ID（与 part anchor）。
* 改变关键假设、条件、目标、结论或任务本质 → **new P ID**；历史 Attempt **不** 自动迁移语义。

**Part anchor（DECIDED）：** 若 `Attempt → P0002 + b`，则 `b` 为 history-bearing anchor；已有 Attempt 引用后 **不得** 为排版将 `b` 改为 `ii` / `part2` / `B` 而破坏引用（与 Frozen Problem `parts` history-sensitive 一致）。

### 19.17 File granularity

**DECIDED：**

```text
one Attempt record = one Markdown file
```

不把多条 Attempt 嵌进 Problem Markdown；不把多条 Attempt 塞进同一 Event Log 文件。

**3C-1 不** 决定最终 **文件命名** 规则；**不** 创建任何真实文件。

### 19.18 Future Evidence storage boundary

**Confirmed future root（未创建）：**

```text
11_学习证据/
└─ 尝试记录/
```

Phase 3C-1 **禁止** 实际创建该目录。初期 **不** 按年份 / 学科 / correct-incorrect / Problem 再分子目录；分类由未来 Index / Derived View 承担。

### 19.19 Excluded from Attempt Evidence

继续 **排除**（属 Derived Learning State 或 Recommendation，**不得** 写入 Attempt Truth）：

`mastery`、`weakness`、`success_rate`、`review_due`、`retention`、`personal_difficulty`、`recommendation_score`、`mistake_count`（作为存档总数）

**禁止** Problem / Knowledge 手工维护 reverse Attempt 列表；reverse relation **derived**。

**Deferred（不阻塞 Attempt Foundation）：** `method` / `methods` / `error_mode` / `error_modes` 引用 → Phase 3D。3C-1 **不** 预留 `methods: []` / `error_modes: []`。

**Deferred / 非 v1 必需：** `duration`、`started_at`、`completed_at`、`confidence`；数据库；RAG；Multi-Agent（见 §16 Future Extensions）。

用户作答与推理 → **Attempt Markdown body**（概念）；**不** 把完整答案塞 YAML（3C-0 **DECIDED**，3C-1 **维持**）。

### 19.20 Resolved in 3C-1（摘要）

1. Episode vs Attempt 模型与正式定义  
2. Episode boundary（意图 + 信息状态；无固定时间阈值）  
3. 真实 Attempt vs 仅接触题目  
4. Target = whole Problem 或 P + exactly one part；禁止多 part 单条 Attempt  
5. Multipart 评价粒度；Evidence double-counting 禁止  
6. Reattempt = new A；默认不自动 new identity 的细粒度编辑行为  
7. 六种 outcome 语义 + 判定优先  
8. Assistance 三态 + target-scoped + independent 精确定义 + 后验验证边界  
9. Recording ≠ Assessment；可说明依据  
10. A ID `^A\d{6}$`；`A000000` sentinel；`A000001` 起  
11. attempted_at = target-scoped evidence completion 最佳可得时间  
12. Correction vs Reattempt；Correction 可审计（**字段形状 → §20**）  
13. Problem/part 历史引用复用 Frozen Problem Identity Contract  
14. 一 Attempt = 一 Markdown 文件；future root 路径确认但未建目录  
15. Method/EM/DB 不阻塞；Derived 不写回 Attempt  

### 19.21 Open for 3C-2（Schema shape only）

**已关闭（2026-08-19）。** Candidate Baseline 见 **§20**。下列留待 Pilot / Final Review / Validator 阶段：

1. **文件命名** 与 discovery / exclusion 规则（3C-3 / 3C-6）
2. draft Problem 上 Attempt 是否默认计入 derived 统计（3C-4 / 3E 策略）
3. Validator **rule ID** 与 dependency 编排（3C-6）
4. Assessment provenance **是否** 进入 Schema（3C-4；3C-2 **未** 加入）
5. `attempted_at` 事后补记策略与跨时区治理细节（Pilot 后）
6. Phase 3C-5 **Freeze** 前 Final Review 变更控制

### 19.22 Non-Goals（3C-1 已完成边界内仍禁止）

* 创建 `11_学习证据/`、Attempt Markdown、A000001 等真实对象  
* Attempt Validator / Indexer / Workflow / Creator  
* 修改 `元数据规范.md`、Frozen Knowledge / Problem Schema、P0001/P0002/K0001/K0002  
* Learning State Engine / Recommendation Engine  
* 在仓库中创建真实 Attempt 对象或 `11_学习证据/` 目录（Candidate 示例仅存在于本文档）

---

## 20. Phase 3C-2 — Attempt Schema v1 Candidate

> **Historical — superseded by Frozen Attempt Schema v1（`元数据规范.md` §8；Phase 3C-5 DONE §23）。** 本节保留 Candidate 设计过程；**不再**承担字段级 technical authority。

**Status:** **CANDIDATE**（historical）  
**Candidate Design Complete:** 2026-08-19  
**Schema Not Frozen** · **No Real Attempt Created** · **No Validator Implemented**

本节将 **§19 已冻结语义合同** 映射为 **Minimal Evidence Record v1 Candidate** YAML 形状。**不**重新定义 Attempt 语义；**不**修改 Frozen Knowledge / Problem Schema。

**后续仍须：** Phase 3C-3 Real Attempt Pilots → Phase 3C-4 Final Review → Phase 3C-5 Schema Freeze。

> **NEEDS SCHEMA SOURCE-OF-TRUTH REVIEW：** Attempt 仍为 Candidate，尚未迁入 `元数据规范.md`。正式 Freeze 前以本节为 Candidate Baseline。

### 20.1 Design Principles

| 原则 | 说明 |
|------|------|
| Minimal Evidence Record | 只持久化必须稳定的 **原始 Evidence facts** |
| No Static lifecycle copy | 不复制 Knowledge / Problem 的 `status` / `draft` / `reviewed` |
| No Derived State | 不保存 mastery、success_rate 等聚合指标 |
| No premature Method / EM | 不字段化尚未设计的 Method / Error Mode |
| Single Source of Truth | Problem 拥有 parts；Attempt 只引用，不复制 Problem 内容 |
| YAGNI | 不为对象对称性添加无必要字段 |

### 20.2 Candidate Schema Summary

**允许字段最大集合（共 9 个，不得扩展）：**

```text
schema_version
id
type
problem
part
outcome
assistance
attempted_at
corrections
```

| 类别 | 字段 |
|------|------|
| **Required** | `schema_version`, `id`, `type`, `problem`, `outcome`, `attempted_at` |
| **Optional** | `part`, `assistance`, `corrections` |

**Candidate 决策：NO `status` · NO lifecycle**

Attempt 是 **Historical Evidence Record**（记录发生过什么），不是 Static Mathematical Object（不表达当前处于什么 lifecycle 阶段）。

### 20.3 Field Specifications

#### 20.3.1 `schema_version`

```yaml
schema_version: 1
```

| 属性 | 值 |
|------|-----|
| Required | 是 |
| Type | integer |
| Candidate value | `1` |

这是 **Attempt Schema v1 Candidate**，**不是** Frozen Schema v1。`schema_version: 1` **不等于** 已 Freeze。

#### 20.3.2 `id`

```yaml
id: A000001
```

| 属性 | 值 |
|------|-----|
| Required | 是 |
| Pattern | `^A\d{6}$` |
| Sentinel | `A000000` — **不得**分配给真实 Attempt |
| Real allocation | 从 `A000001` 起 |

禁止：timestamp ID、Problem-local sequence、filename / path / session identity。

#### 20.3.3 `type`

```yaml
type: attempt
```

| 属性 | 值 |
|------|-----|
| Required | 是 |
| Value | literal `attempt` |

不引入 `learning_attempt`、`problem_attempt` 等额外 type。

#### 20.3.4 `problem` + `part`（Target）

**扁平结构** — 不使用 `target:` 对象、不使用 `P0002#b`、不使用 `parts:` 列表。

```yaml
problem: P0002
part: b          # optional
```

| 字段 | Required | 语义 |
|------|----------|------|
| `problem` | 是 | 必须引用合法 Problem YAML `id`（`^P\d{4}$`） |
| `part` | 否 | 若存在，必须引用目标 Problem 当前已定义的 **Frozen part anchor** |

| `problem` | `part` | Target 语义 |
|-----------|--------|-------------|
| present | absent | **Whole Problem** target |
| present | present | **Problem + exactly one part** target |

**Target Source of Truth：** Problem object 拥有 parts 定义；Attempt **不**复制 part 定义、part 标签列表、Problem title 或 Problem metadata。`part` 合法性未来由 Problem registry / Validator 验证。

#### 20.3.5 `outcome`

| 属性 | 值 |
|------|-----|
| Required | **是** |
| Candidate allowed values | `correct`, `incorrect`, `partial`, `unsolved`, `abandoned`, `unassessed` |

**语义区分（§19 映射）：**

| 情况 | 合法性 |
|------|--------|
| `outcome` **字段缺失** | **invalid / incomplete record** |
| `outcome: unassessed` | **valid Attempt** — Evidence 确实发生，但当前没有可靠 Assessment |

`unassessed` **不等于** draft、**不等于** lifecycle、**不代表** Attempt 尚未创建。

下列值 **Excluded**（不得作为 Candidate enum）：`unknown`, `pending`, `success`, `failed`, `wrong`, `incomplete`。

> 六种值为 **Candidate allowed values**，**不是** Frozen enum。正式 Freeze 在 Phase 3C-5。

#### 20.3.6 `assistance`

| 属性 | 值 |
|------|-----|
| Required | **否**（optional） |
| Allowed values（字段存在时） | `independent`, `assisted` |

**字段省略的正式语义：** assistance was **not reliably recorded**（§19：`omitted / not reliably recorded`）。

**禁止：** `assistance: omitted`、`assistance: unknown` 作为第三个枚举值。省略 = 未可靠记录，不是 Schema enum。

**与 `outcome` 正交：** `assisted + correct`、`independent + incorrect`、`assisted + partial` 均合法。禁止根据 assistance 自动推 outcome，或根据 outcome 自动推 assistance。

#### 20.3.7 `attempted_at`

```yaml
attempted_at: 2026-08-19T20:47+08:00
```

| 属性 | 值 |
|------|-----|
| Required | 是 |
| Candidate format | offset-aware ISO 8601，**分钟精度**：`YYYY-MM-DDTHH:MM±HH:MM` |
| 语义（§19） | 该 Attempt 对应 target-scoped Evidence **形成完成时刻**的最佳可得时间记录 |

**分钟精度 rationale：** 秒级精度对学习 Evidence 无实际分析价值；分钟级足够支持 ordering、recent performance、review interval、retention calculations；同时避免 false precision。此格式仍为 **3C-2 Candidate**；Pilot 后可在 3C-4 调整。

**Excluded 格式：** date-only（`2026-08-19`）、无 offset（`2026-08-19T20:47`）、纯时间（`20:47`）、Unix timestamp、filesystem ctime/mtime。

#### 20.3.8 `corrections`

```yaml
corrections:
  - at: 2026-08-20T09:12+08:00
    note: "Corrected target part from a to b; initial registration error."
```

| 属性 | 值 |
|------|-----|
| Required | **否**（optional） |
| Shape | list of entries |
| Entry fields | **`at`** + **`note`** only |

| Entry 字段 | 规则 |
|------------|------|
| `at` | 与 `attempted_at` 相同时间格式（offset-aware ISO 8601，分钟精度） |
| `note` | human-readable、non-empty；说明登记修正发生了什么 |

**Append-only 语义：** correction 保留原 Attempt ID，修正错误登记事实，**追加** correction entry。**不能**用 correction 表示「后来又重新做了一次」——后者必须 **new Attempt ID**（Reattempt，§19）。

**无 correction 时：** **省略** `corrections` 字段；**不要**强制 `corrections: []`。

**Excluded entry 字段：** `changed_by`, `old_value`, `new_value`, `diff`, `reason_code`, `revision_id`, `source`。

#### 20.3.9 Explicitly Excluded Fields

下列字段 **不得** 出现在 v1 Candidate 中：

```text
status, title, created, updated
knowledge, domain, aliases, tags
method, methods, error_mode, error_modes
mastery, weakness, review_due, retention, success_rate,
  personal_difficulty, mistake_count, recommendation_score,
  recent_performance
attempts, reverse_attempts
started_at, completed_at, duration
confidence, difficulty
assessed_by, assessment_method, assessment_basis,
  assessment_confidence, assessment_note
session_id, chat_id
```

#### 20.3.10 Explicitly Deferred

| 主题 | 状态 |
|------|------|
| Method / Error Mode 字段 | **Deferred** — Schema 尚未设计 |
| Assessment provenance 字段 | **Deferred** — Recording ≠ Assessment；basis 要求属 Recording / Assessment Contract，非 v1 YAML 字段 |
| Derived Learning State 消费规则 | **Deferred** — Phase 3E（如 unassessed 是否进 mastery、partial 权重、assisted 降权） |
| File naming / discovery / registry | **Deferred** — 3C-3 / 3C-6 |
| Filename convention | **Deferred** |

#### 20.3.11 No `created` / `updated`

**Excluded** 理由：

| 时间类型 | 归属 |
|----------|------|
| `attempted_at` | learning evidence time |
| file creation / modification time | infrastructure fact |
| correction `at` | correction audit fact |

Attempt **不**复制 Knowledge / Problem 的 `created` / `updated` 模式。

#### 20.3.12 No `knowledge`

关联路径：**Attempt → Problem → Knowledge**。Attempt 再维护 `knowledge:` 会产生 duplicate source of truth。Attempt-related Knowledge 若需表达，优先 **Derived**。

### 20.4 Markdown Body vs YAML Boundary

| YAML | 稳定结构化 facts |
|------|-----------------|
| Markdown body | 用户原始作答、推理过程、关键中间步骤、必要的 Assessment explanation、上下文说明 |

禁止：把完整 answer / reasoning / proof / scratch work 塞入 YAML。禁止：把 `outcome` / `problem` / `part` / `attempted_at` 只写在 body 而不进入结构化 Metadata。

### 20.5 Storage Semantics（Candidate，非 discovery 实现）

| 语义 | 值 |
|------|-----|
| 一 Attempt | 一 Markdown 文件 |
| 未来 root | `11_学习证据/尝试记录/`（3C-3 起在有真实 Evidence 时创建；**当前尚无真实 Attempt**） |

Filename convention、discovery code、duplicate detection → **Deferred**（3C-3 / 3C-6）。

### 20.6 Candidate Validation Semantics（Future Contract）

**本轮不实现 Validator，不设计 rule ID。** 下列为 future Validator 须能验证的高层 invariants：

1. `id` matches `^A\d{6}$`；real Attempt cannot use `A000000`
2. `type == attempt`
3. `schema_version` is valid Candidate integer（当前 `1`）
4. `problem` target exists in Problem registry
5. `part`, if present, exists in target Problem's Frozen part anchors
6. `outcome` is valid Candidate value（六种之一）
7. `outcome` **must be present**（missing → ERROR）
8. `assistance`, if present, is `independent` or `assisted`
9. `attempted_at` has valid offset-aware minute-precision format
10. `corrections`, if present, is a list; each entry has valid `at` + non-empty `note`
11. No forbidden / excluded fields present

Dependency：**Problem Validator** must pass before Attempt validation can be authoritative on `part` references.

### 20.7 Candidate Examples

> **Candidate Example — NOT a real Attempt.** 下列示例仅存在于本文档；**未**在仓库创建 `11_学习证据/` 或任何 A000001 等真实对象。  
> `A000000` 为 sentinel / template placeholder，**cannot be assigned to a real Attempt**。

#### 20.7.1 Multipart + assisted + partial

```yaml
---
schema_version: 1
id: A000001
type: attempt
problem: P0002
part: b
outcome: partial
assistance: assisted
attempted_at: 2026-08-19T20:47+08:00
---
```

`A000001` 此处仅为 Candidate 示例 ID；**并未**创建真实 Attempt object。

#### 20.7.2 Whole Problem — part omitted, assistance omitted

```yaml
---
schema_version: 1
id: A000000
type: attempt
problem: P0001
outcome: correct
attempted_at: 2026-08-20T14:25+08:00
---
```

- `part` **omitted** → whole Problem target  
- `assistance` **omitted** → not reliably recorded

#### 20.7.3 Unassessed — valid Evidence, no reliable Assessment

```yaml
---
schema_version: 1
id: A000000
type: attempt
problem: P0002
part: c
outcome: unassessed
attempted_at: 2026-08-20T15:10+08:00
---
```

合法 Evidence record；outcome 尚无可靠 Assessment。**不是** draft lifecycle。

#### 20.7.4 Correction — same Attempt identity

```yaml
---
schema_version: 1
id: A000000
type: attempt
problem: P0002
part: b
outcome: unassessed
attempted_at: 2026-08-20T15:10+08:00
corrections:
  - at: 2026-08-20T18:20+08:00
    note: "Corrected target part from c to b; initial registration error."
---
```

**同一 Attempt identity**（same `id`），不是 new Attempt / Reattempt。

### 20.8 Semantic Contract Alignment

§19 语义 → §20 Candidate 映射检查：

| §19 语义 | §20 表达 | 状态 |
|----------|----------|------|
| Episode ≠ Attempt | 不在 Schema 中建模 Episode | OK |
| 1 Attempt = 1 evaluation target | `problem` + optional `part` | OK |
| Outcome 六种语义 | `outcome` required enum Candidate | OK |
| Assistance independent / assisted / omitted | optional；省略 = not reliably recorded | OK |
| Recording ≠ Assessment | Assessment 字段不进入 Candidate | OK |
| Reattempt = new A ID | 不在 corrections 中表达 | OK |
| Correction = same A + provenance | optional `corrections` list | OK |
| attempted_at 语义 | required，Candidate 格式 | OK |
| No lifecycle on Attempt | NO `status` | OK |

**Semantic Contract 与 Candidate Schema 冲突：** **否**

### 20.9 Non-Goals（3C-2 边界）

* 创建真实 Attempt 或 `11_学习证据/`  
* Attempt Validator / Indexer / Workflow / Creator / Template  
* 修改 `元数据规范.md`、Frozen Schema、P0001/P0002  
* Phase 3C-3 Real Attempt Pilots（**IN PROGRESS**，§21）  
* 决定 Derived State 如何消费 `unassessed` / `partial` / `assisted` / `abandoned`  
* Git commit / push

### 20.10 Open for Pilot / Final Review

下列须 **3C-3 Pilot** 或 **3C-4 Final Review** 才能定案：

1. Candidate 示例在真实 Markdown 文件中的作者体验与 body 结构  
2. `attempted_at` 跨时区与事后补记治理  
3. Assessment explanation 在 body 中的最小必要结构  
4. Filename convention 与 discovery exclusion  
5. Assessment provenance 是否需在 3C-4 升格为 Schema 字段  
6. 分钟精度是否足够（或需调整）

**3C-2 输出 = Candidate Baseline。** Schema Freeze 仅在 **Phase 3C-5**。Pilot Reality Check → **§21**。

---

## 21. Phase 3C-3 — Real Attempt Pilots

**Status:** **DONE**（completion candidate met 2026-08-19；**Schema NOT Frozen**）  
**Started:** 2026-08-19  
**Completed:** 2026-08-19（2 条真实 Pilot；await explicit 3C-4 authorization）  
**Attempt Schema:** **CANDIDATE** · **NOT Frozen** · **NOT modified during Pilot**  
**No Fabricated Pilot** · **First Real Attempt:** `A000001` · **Second:** `A000002`（2026-08-19）

### 21.1 Purpose

使用 **真实发生的数学学习 Evidence**，对 §20 Candidate 做 **Reality Check**——验证其能否自然、无歧义、低摩擦地记录真实 Attempt，并为 Phase 3C-4 Final Review 提供 **Evidence-based KEEP / CHANGE / DEFER** 输入。

```text
Real Pilot = 真实学习事件

Real Pilot ≠ 为了测试 Schema 而伪造的数据
```

**本轮不：** Freeze Schema · 开发 Attempt Validator · 创建 Attempt Template · 修改 3C-2 Candidate · 建立 Pilot Infrastructure（除最小物理存储外）。

### 21.2 Pilot Principles

| 原则 | 说明 |
|------|------|
| Reality first, Coverage second | 真实事件决定 Pilot 数量；不人为覆盖 edge case |
| Candidate must not mutate | Pilot 暴露问题 → 记录 **CHANGE candidate** → 3C-4 统一决策 |
| No fabricated edge cases | 禁止故意写错 part 测 correction、故意 unassessed、无重新做题却建 Reattempt |
| Historical Fidelity | body 保留当时真实过程，不后验整理成完美讲义 |
| Lightweight recording | 记录不应比做题本身更繁琐 |
| Unknown fields → omit / unassessed | 不知道的 assistance 省略；无可靠 Assessment → `unassessed`；不知道精确分钟 → 不造假精度 |

### 21.3 Minimal Storage（3C-3）

**仅允许**最小物理结构：

```text
11_学习证据/
└─ 尝试记录/
    └─ A000001.md   # 仅当有真实 Evidence 时创建
```

**禁止**创建：`按年份/`、`按学科/`、`draft/`、`reviewed/`、`index/`、`registry/`、`templates/`、`pilot/`、`fixtures/`、`test-data/`、`sample-attempts/`。

**当前状态（2026-08-19）：** `11_学习证据/尝试记录/` 已创建；**2** 条真实 Attempt（`A000001` → P0002(b)；`A000002` → P0002(a)）。

### 21.4 Provisional Filename Convention（NOT Frozen）

3C-3 创建真实 Attempt 时采用 **临时最小约定**：

```text
<Attempt YAML id>.md
```

示例：`A000001.md`、`A000002.md`。

| 属性 | 说明 |
|------|------|
| Identity authority | **永远是 YAML `id`**，不是 filename |
| Convention status | **provisional** · **NOT Frozen** · **NOT Attempt identity** |
| 禁止 | Problem title、Problem filename、日期、slug、聊天 session 作为 identity |

**ID 分配：** 从当前最小未使用合法 ID 起（尚无真实 Attempt 时 → `A000001`）。`A000000` 为 sentinel，**不得**用于真实 Attempt。分配前扫描 `11_学习证据/尝试记录/` 与已知 Attempt 文件的 YAML `id`，取最大已用 ID 的下一个。禁止复用、禁止根据 filename 猜 identity。

### 21.5 Attempt Markdown Body（Observational）

**不**创建 Attempt Template；**不**强制 body headings checklist。

YAML → stable structured facts；body → 用户作答 / reasoning / 必要说明。可按真实需要选用 `## 作答 / 推理`、`## Assessment`（**非 mandatory**）。Assessment explanation 可放 body，**不**进入 YAML（§20.4）。

### 21.6 Six-Dimension Reality Check（每条真实 Attempt 后执行）

每出现一条有价值的真实 Attempt，检查：

1. **Expressiveness** — Candidate 能否准确表达？有无重要 Evidence 无处记录？
2. **Ambiguity** — 哪些字段填写时犹豫？（重点：`outcome`、`assistance`、`attempted_at`、`part`）
3. **Redundancy** — 有无实际无价值、仅为 Schema 完整性存在的字段？
4. **Recording Friction** — 记录是否明显比做题更繁琐？
5. **Historical Fidelity** — 是否忠实保留当时学习过程？
6. **Future Derived Usability** — 未来只读结构化 Evidence，是否明显缺少计算学习状态所需基础事实？（本轮不计算 mastery 等）

结论记入 §21.8 Pilot Log 与 §21.9 Candidate Review；**不在 3C-3 执行 CHANGE**。

### 21.7 Priority Observation Scenarios

| 优先级 | 场景 | 验证目标 |
|--------|------|----------|
| 1 | Reattempt / History Preservation | 同 target 失败后真正重做 → new A ID；历史不覆盖 |
| 2 | Multipart Target | `problem` + `part` 表达独立评价 target |
| 3 | Assistance | 自然出现 `independent` / `assisted` / 省略 |
| 4 | unassessed | 真实发生但无可靠 Assessment basis |
| 5 | Correction | 真实登记错误 → append-only `corrections` |

**NOT OBSERVED 不阻塞 3C-3**（correction、unassessed、跨时区等可 **DEFER**）。

### 21.8 Pilot Log

> 不复制完整数学正文；Attempt body 留在真实 Attempt Markdown 文件（`11_学习证据/尝试记录/`）。

#### Pilot 1 — A000001

| 项 | 内容 |
|----|------|
| **Attempt** | `A000001` |
| **Target** | `P0002(b)` |
| **Scenario** | Multipart + Assisted Real Attempt |
| **Observed** | multipart **OBSERVED** · assisted **OBSERVED** |
| **NOT OBSERVED** | whole Problem · reattempt · unassessed · correction |
| **Note** | 同一 Attempt 内存在独立进展 → 实质帮助 → 最终完成；Attempt-level `assisted` + `correct`；body 保留两阶段 fidelity |

**六项 Reality Check（A000001）：**

| 维度 | 结论 | 摘要 |
|------|------|------|
| **Expressiveness** | **KEEP** | 9 字段 Candidate 准确表达 multipart + assisted + correct；独立/帮助两阶段由 body 承载，YAML 无需额外字段 |
| **Ambiguity** | 低 | `part: b` 无犹豫；`outcome` / `assistance` 用户已明确；`attempted_at` 用户给出精确分钟 |
| **Redundancy** | **KEEP** | 本次无实际冗余字段；9 字段均有用途或正确省略 |
| **Recording Friction** | **KEEP** | YAML 轻量；body 自然分段即可，无明显额外负担 |
| **Historical Fidelity** | **KEEP** | body 明确区分独立阶段与 ChatGPT 补全阶段，未后验美化 |
| **Future Derived Usability** | **DEFER** | 结构化字段足够表达 target/outcome/assistance/time；独立进展粒度仅 body 有，Derived 是否需结构化 **DEFER** 至 3E |

**Pilot Finding（§19 一致性）：** 同一 Attempt 内「独立正确进展 → 获得实质帮助 → 最终正确完成」**不**要求拆成两条 Attempt；Attempt-level `assistance: assisted` 与 `outcome: correct` 正交合法，与 §19 Semantic Contract **一致**。

| Attempt ID | Target | 真实场景 | Candidate 使用 | 观察摘要 | KEEP/CHANGE/DEFER |
|------------|--------|----------|----------------|----------|-------------------|
| A000001 | P0002(b) | Multipart + Assisted | 全 required + `part` + `assistance`；无 `corrections` | 首条真实 Pilot；高价值 multipart | 见 §21.9 |
| A000002 | P0002(a) | Multipart + Independent + Partial | 全 required + `part` + `assistance`；无 `corrections` | 第二条真实 Pilot；partial/independent | 见 §21.9 |

#### Pilot 2 — A000002

| 项 | 内容 |
|----|------|
| **Attempt** | `A000002` |
| **Target** | `P0002(a)` |
| **Scenario** | Multipart + Independent + Partial Real Attempt |
| **Observed** | multipart **OBSERVED** · independent **OBSERVED** · partial **OBSERVED** |
| **NOT OBSERVED** | whole Problem · reattempt · unassessed · correction · assisted（本次） |

**六项 Reality Check（A000002）：**

| 维度 | 结论 | 摘要 |
|------|------|------|
| **Expressiveness** | **KEEP** | `partial` + `independent` + `part: a` 自然表达；进展细节由 body 承载 |
| **Ambiguity** | 低 | `partial` vs `unsolved` 有明确 Assessment basis；`assisted` vs 轻微提示 → `independent` 清晰 |
| **Redundancy** | **KEEP** | 无额外字段需求 |
| **Recording Friction** | **KEEP** | 与 A000001 相当；YAML 仍轻量 |
| **Historical Fidelity** | **KEEP** | 保留「做到 $v^T B = \lambda_2 v^T$ 后停止」的真实过程，未补写完整证明 |
| **Future Derived Usability** | **DEFER** | `partial` 结构化可表达；Derived 如何消费 partial 权重留 3E |

**Pilot Finding（partial 语义）：** 用户已形成可确认的实质性正确进展（$B^T v = \lambda_2 v$、$v^T B = \lambda_2 v^T$），但未构造 $\varphi$ 特征向量、未完成证明 → **`partial` ≠ `unsolved`**。支持 §19 partial/unsolved 语义区分。

**A000001 vs A000002 对比：**

| | A000001 | A000002 |
|---|---------|---------|
| Target | P0002(b) | P0002(a) |
| outcome | correct | partial |
| assistance | assisted | independent |
| 结论 | Candidate 自然支持不同 outcome/assistance 组合；同一 Problem 不同 part 独立记录 |

### 21.9 Candidate Decisions Review（基于 A000001 + A000002）

| 设计点 | 决策 | 依据 |
|--------|------|------|
| 9 字段最大集合 | **KEEP** | 两条 Pilot 均无缺失 fact 需额外字段 |
| `schema_version` | **KEEP** | `1` 自然 |
| `id` | **KEEP** | A000001/A000002 顺序分配无冲突 |
| `type: attempt` | **KEEP** | 无歧义 |
| `problem` + optional `part` | **KEEP** | (a)/(b) 独立 target 自然；同 Problem 多 Attempt 清晰 |
| required `outcome` | **KEEP** | `correct` / `partial` 均有可靠 Assessment；`partial` 真实价值 **确认** |
| `unassessed` 真实价值 | **DEFER** | **NOT OBSERVED** |
| optional `assistance` / omission | **KEEP** | `assisted` / `independent` 均自然；轻微提示 ≠ assisted 边界清晰 |
| `attempted_at` 分钟精度 | **KEEP** | 两条均精确分钟自然；事后补记假精度 **DEFER** |
| NO `status` | **KEEP** | Evidence 无需 lifecycle |
| NO `created` / `updated` | **KEEP** | `attempted_at` 足够 |
| NO assessment provenance | **KEEP**（excluded） | basis 放 body 可行；升格 Schema **DEFER** 至 3C-4 |
| `corrections` minimal shape | **DEFER** | **NOT OBSERVED** |
| one Attempt = one Markdown file | **KEEP** | 自然 |
| filename = `Axxxxxx.md` | **KEEP**（provisional） | 简单；YAML `id` authoritative |

**需 3C-4 决定（有 Pilot Evidence）：** Assessment provenance 是否升格 Schema；`partial` Derived 权重；独立进展粒度是否结构化；`attempted_at` 事后补记治理。

**尚无真实依据（留 3C-4/Pilot 或 DEFER）：** reattempt 历史保留、`corrections` 形状、`unassessed` 价值、assistance 省略语义（两条均填写了 assistance）

### 21.10 Scenario Coverage（Current）

| 场景 | 状态 |
|------|------|
| whole Problem | **NOT OBSERVED** |
| multipart | **OBSERVED**（A000001, A000002） |
| reattempt | **NOT OBSERVED** |
| assisted | **OBSERVED**（A000001） |
| independent | **OBSERVED**（A000002） |
| correct | **OBSERVED**（A000001） |
| partial | **OBSERVED**（A000002） |
| unsolved | **NOT OBSERVED** |
| unassessed | **NOT OBSERVED** |
| correction | **NOT OBSERVED** |

### 21.11 Minimum Completion Criteria（3C-3 → DONE）

**当前进度（2026-08-19，A000002 后）：**

| 条件 | 状态 |
|------|------|
| 至少一个真实 Attempt | **满足**（2 条） |
| 高价值 edge case（Reattempt 或 Multipart） | **满足**（Multipart ×2） |
| 主要 Candidate 决策有 KEEP/CHANGE/DEFER 证据 | **满足**（outcome/assistance/part/time/no-status 等均有双 Pilot 证据） |
| Reattempt 历史保留验证 | **未观察**（**DEFER** 至 3C-4；不阻塞关闭） |

**Phase 3C-3 completion candidate: YES**

两条互补真实 Pilot（correct/assisted + partial/independent；P0002(b) + P0002(a)）已足以评价主要 Candidate 决策。**未**因缺少 reattempt/correction/unassessed 机械延长 3C-3。

**判定：** **3C-3 可关闭**（completion criteria met）。**不**自动进入 Phase 3C-4；await 下一条明确授权。

**Reattempt / correction / unassessed：** 留待真实发生或 3C-4 Final Review 决策；非 3C-3 阻塞项。

### 21.12 Non-Goals（3C-3 边界）

* 伪造学习 Evidence 或 Pilot  
* 修改 §19 Semantic Contract 或 §20 Candidate Schema  
* 修改 Frozen Knowledge / Problem Schema、P0001/P0002/K0001/K0002（默认）  
* Attempt Validator / Indexer / Workflow / Creator / Template  
* Derived State Engine / Recommendation Engine  
* Schema Freeze（3C-5）或迁入 `元数据规范.md`（3C-4/3C-5）  
* Git commit / push

### 21.13 Open for Next Real Attempt

下一次 **真实数学学习事件** 发生时：

1. 若 `11_学习证据/尝试记录/` 不存在，创建最小目录结构  
2. 分配下一个合法 A ID（当前下一预期 ID：`A000003`）  
3. 按 §20 Candidate 填写 YAML；body 保留真实过程  
4. 执行 §21.6 六项 Reality Check  
5. 更新 §21.8 Pilot Log 与 §21.9 Candidate Review  
6. 评估是否满足 §21.11 完成条件

---

## 22. Phase 3C-4 — Attempt Schema v1 Final Candidate & Freeze Readiness Review

> **Historical — Final Candidate 已于 Phase 3C-5 迁入 Frozen Schema（`元数据规范.md` §8）。** 本节保留 Final Review 过程与决策历史。

**Status:** **DONE**（historical Final Candidate input to 3C-5）  
**Review Date:** 2026-08-19  
**Final Candidate ≠ Frozen Schema** · **No production tooling** · **No `元数据规范.md` mutation**

本节基于 **§19 Semantic Contract**、**§20 Candidate Baseline**、**§21 两条真实 Pilot** 做最后一次一致性审计，输出 **Attempt Schema v1 Final Candidate**，并判定 **Phase 3C-5 Freeze Readiness**。

> **Authority chain：** §19 → §20 → §21 Pilot Evidence → **§22 Final Candidate** →（3C-5）`元数据规范.md` Freeze。

### 22.1 Real Pilot Evidence（仅两条；不得 extrapolate）

| Attempt | Target | outcome | assistance | attempted_at |
|---------|--------|---------|------------|--------------|
| A000001 | P0002(b) | correct | assisted | `2026-08-19T21:36+08:00` |
| A000002 | P0002(a) | partial | independent | `2026-08-19T22:05+08:00` |

**Pilot OBSERVED：** multipart · correct · partial · assisted · independent

**NOT OBSERVED（不得写 “Pilot validated”）：** whole Problem · incorrect · unsolved · abandoned · unassessed · assistance omitted · reattempt · correction · delayed/approximate `attempted_at`

### 22.2 Semantic Contract → Candidate 一致性审计

| §19 约束 | Final Candidate 表达 | 结果 |
|----------|------------------------|------|
| Episode ≠ Attempt | 不建模 Episode | **OK** |
| 1 Attempt = 1 target-scoped Evidence | `problem` + optional `part` | **OK** |
| Target = whole Problem OR Problem + one part | `part` omitted / present | **OK** |
| Reattempt → new A ID | 新 `id`；不覆盖旧记录 | **OK**（Contract Review；NOT OBSERVED） |
| Correction → same A ID + auditable provenance | optional `corrections` append-only | **OK**（Contract Review；NOT OBSERVED） |
| Recording ≠ Assessment | outcome required；basis 可放 body | **OK** |
| Recorder cannot invent outcome | process rule + `unassessed` 语义 | **OK** |
| Historical Evidence；no lifecycle | NO `status` | **OK** |
| Later success ≠ overwrite earlier failure | 独立 Attempt 记录 | **OK**（Contract Review） |

**Semantic Contract 冲突：** **NONE** · **BLOCKER：** **NONE**

### 22.3 Attempt Schema v1 Final Candidate

**Final Candidate ≠ Frozen Schema。** 3C-5 将把本节（含 `attempted_at` CHANGE）迁入 `元数据规范.md`。

#### 允许字段最大集合（仍为 9 个）

```text
schema_version
id
type
problem
part
outcome
assistance
attempted_at
corrections
```

| 类别 | 字段 |
|------|------|
| **Required** | `schema_version`, `id`, `type`, `problem`, `outcome`, `attempted_at` |
| **Optional** | `part`, `assistance`, `corrections` |

#### Semantic enums（Final Candidate）

**outcome（required）：** `correct` · `incorrect` · `partial` · `unsolved` · `abandoned` · `unassessed`

**assistance（optional，存在时）：** `independent` · `assisted`  
**assistance 省略：** not reliably recorded（**不是** enum）

#### File & body conventions（非 Schema required）

| 项 | Final Candidate |
|----|-----------------|
| 一 Attempt = 一 Markdown 文件 | **YES** |
| Filename convention | `<A-ID>.md`（provisional v1 convention） |
| Identity authority | **YAML `id`**；filename 不得定义 identity |
| Body | **natural**；建议轻量 convention：`## 作答 / 推理`、`## Assessment`（optional headings） |

### 22.4 Field Decisions（摘要）

详见 §22.6 Decision Table。

**唯一 CHANGE：** `attempted_at`（自 §20 minute-only → **dual-format best-available record**）

**KEEP：** 其余 8 个字段设计 · NO `status` · NO `created`/`updated` · NO `knowledge` · NO Assessment provenance fields

**DROP：** 无（相对 §20 无字段删除）

**DEFER：** Method / Error Mode → 3D · Derived Learning State → 3E · Attempt production tooling → 3C-6+

### 22.5 `attempted_at` — Freeze Gate Review（最高优先级）

#### Pilot 能证明什么

A000001/A000002 仅证明：**分钟级 offset-aware 时间在实时、可靠已知时自然**。

#### Scenario Analysis（非 Pilot）

| 场景 | 3C-2 minute-only 风险 |
|------|----------------------|
| 「昨晚做的，不记得几点」 | **强迫假精度** 若必填 `T21:xx` |
| 「大约晚上九点」 | **强迫假精度** |
| 「前几天做过」 | **强迫假精度** |
| 「知道日期，不知道分钟」 | **无法诚实表达** |

**结论：** 3C-2 的 **minute-only required** 在 delayed/approximate recording 下 **YES → 制造假精度**，违反 §19 **best available record** 原则。**这是 3C-4 必须解决的 Freeze Gate。**

#### 选项比较

| Option | 描述 | 评价 |
|--------|------|------|
| **A** | 保持单一 minute-precision | **拒绝** — 无法诚实表示不精确时间 |
| **B** | dual-format `attempted_at` | **采用** — 最小 CHANGE |
| **C** | 新增 `attempted_at_precision` | **拒绝** — YAGNI；第二字段 |

#### Final Candidate — `attempted_at`（CHANGE）

**语义（§19 不变）：** target-scoped Evidence 形成完成时刻的 **best available** 时间记录。

**允许两种 honest representation（Final Candidate）：**

1. **Minute precision（已知时）：** `YYYY-MM-DDTHH:MM±HH:MM`  
   例：`2026-08-19T21:36+08:00`

2. **Date-only（仅日历日可靠已知时）：** `YYYY-MM-DD`  
   例：`2026-08-19`

**Process rules（Freeze 时一并固化）：**

- 分钟 **可靠已知** → 使用格式 1；**不得**降级为 date-only。
- 仅日历日可靠已知 → 使用格式 2；**禁止** 编造 `:21:00` 等假分钟。
- 「大约九点」**不足以** 使用格式 1；若无可靠日期则不得伪造 `attempted_at`（应推迟记录或 body 说明 uncertainty，不在 Schema 新增字段）。
- `corrections[].at` **遵循相同 dual-format 规则**。

**false-precision risk resolved:** **YES**（设计层面；3C-5 Freeze 时写入 Frozen Schema）

**现有 Pilot 兼容：** A000001/A000002 继续使用格式 1 — **valid**。

### 22.6 Decision Table

| Design Point | Decision | Basis | Pilot Status | Freeze Blocking? |
|--------------|----------|-------|--------------|----------------|
| `schema_version: 1` | **KEEP** | Convention Consistency（对齐 K/P） | N/A | No |
| `id` `^A\d{6}$` | **KEEP** | Contract + Pilot | OBSERVED | No |
| `A000000` sentinel | **KEEP** | Contract | N/A | No |
| `type: attempt` | **KEEP** | Convention Consistency（K/P 均有 `type`） | OBSERVED | No |
| `problem` + optional `part` | **KEEP** | Pilot + Contract | OBSERVED (part) | No |
| whole Problem (part omitted) | **KEEP** | Contract Review | NOT OBSERVED | No |
| `outcome` required | **KEEP** | Pilot + Contract | OBSERVED | No |
| outcome enum (6 values) | **KEEP** | Contract + Distinctness Review | partial/correct OBSERVED | No |
| `unassessed` | **KEEP** | Contract Reasoning | NOT OBSERVED | No |
| `assistance` optional | **KEEP** | Pilot | OBSERVED | No |
| assistance omission | **KEEP** | Contract | NOT OBSERVED | No |
| `attempted_at` | **CHANGE** | Scenario Analysis + §19 best-available | minute OBSERVED; date-only NOT OBSERVED | **Was blocker; resolved in Final Candidate** |
| `corrections` | **KEEP** | Contract（auditable provenance） | NOT OBSERVED | No |
| corrections shape `at`+`note` | **KEEP** | YAGNI + Contract | NOT OBSERVED | No |
| Assessment provenance fields | **KEEP EXCLUDED** | YAGNI + Pilot（body 足够） | OBSERVED (body) | No |
| NO `status` | **KEEP** | Contract | OBSERVED | No |
| NO `created`/`updated` | **KEEP** | YAGNI + Attempt semantics | OBSERVED | No |
| NO `knowledge` | **KEEP EXCLUDED** | YAGNI + SoT path | N/A | No |
| Method / Error Mode | **DEFER** | Phase 3D | N/A | No |
| Derived Learning State | **DEFER** | Phase 3E | N/A | No |
| one file = one Attempt | **KEEP** | Pilot + Contract | OBSERVED | No |
| filename `<A-ID>.md` | **KEEP** | Pilot + Convention | OBSERVED | No |
| body headings | **convention only** | Pilot | OBSERVED | No |
| Reattempt | **KEEP** (structural) | Contract Review | NOT OBSERVED | No |

### 22.7 Outcome & Assistance — Final Semantics

#### outcome

| 值 | 角色 | Pilot |
|----|------|-------|
| `correct` | target 完整可靠正确完成 | **OBSERVED** (A000001) |
| `partial` | 实质性正确进展；未完成 target | **OBSERVED** (A000002) |
| `incorrect` | 完整可评价答案；决定性错误 | NOT OBSERVED |
| `unsolved` | 真实尝试；未达 partial 有效成果 | NOT OBSERVED |
| `abandoned` | 主动中止/时间/外部原因结束 | NOT OBSERVED |
| `unassessed` | Attempt 已发生；无可靠 Assessment basis | NOT OBSERVED |

**`partial` ≠ `unsolved`：** A000002 **Pilot 确认** — 有实质性正确进展但未完成证明。

**`unassessed`：** Assessment **availability state**，不是数学 outcome；不是 draft/unknown/pending。与 Recording ≠ Assessment **一致**。

#### assistance

| 表达 | 语义 |
|------|------|
| `independent` | 无实质改变 search space / key insight / next action 的外部帮助 |
| `assisted` | 外部帮助实质改变了 solving process |
| **字段省略** | not reliably recorded |

A000001/A000002 **Pilot 确认** outcome ⊥ assistance。

### 22.8 Assessment Provenance — Final Decision

**Decision: KEEP EXCLUDED**

- 两条 Pilot 的 Assessment basis 均可放 body `## Assessment`。
- Process rules（§19）+ required `outcome` + body 足以满足 Recording ≠ Assessment。
- **YAGNI：** 未来 Derived/auditing 可能有用 ≠ v1 字段。
- **Re-evaluate：** Phase 3E Derived 设计或 Attempt Validator v2+ 若证明不足。

### 22.9 Corrections — Final Decision

**Decision: KEEP**（minimal `corrections: [{at, note}]`）

- §19 要求 Correction 保留 same A ID + **auditable provenance**。
- 纯 body 修正不足以 **稳定识别** correction history。
- **NOT YET PILOT-OBSERVED** — 但 **Contract-supported**；不阻塞 Freeze。
- `at` 遵循 §22.5 dual-format（与 `attempted_at` 统一）。

### 22.10 NOT OBSERVED — Freeze Impact

| 场景 | 阻塞 Freeze？ |
|------|---------------|
| whole Problem | **No** — Contract Review 支持 |
| incorrect / unsolved / abandoned | **No** — enum 语义清晰 |
| unassessed | **No** — Contract Review |
| assistance omitted | **No** — Contract Review |
| reattempt | **No** — structurally supported |
| correction | **No** — Contract-supported KEEP |
| delayed `attempted_at` | **Resolved** — CHANGE in Final Candidate |

### 22.11 Classification Summary

**A. Freeze-ready：** 9 字段 Final Candidate（含 `attempted_at` CHANGE）· enums · conventions · exclusions

**B. Deferred non-blocking：** Method/Error Mode (3D) · Derived State (3E) · Assessment provenance re-eval (3E/Validator v2) · Attempt Validator/Indexer (3C-6+)

**C. Freeze blockers：** **NONE**（`attempted_at` 已在 Final Candidate 中 CHANGE 解决）

### 22.12 Freeze Readiness Gate

```text
Is Attempt Schema v1 ready for Phase 3C-5 Freeze?
→ YES
```

**条件：**

1. Semantic Contract ↔ Final Candidate：**一致**
2. 两条真实 Pilot：**无 contradictions**
3. `attempted_at` false-precision：**已在 Final Candidate 解决**
4. 无未决 CHANGE 阻塞 Freeze

**3C-5 执行时须：** 将 §22 Final Candidate（含 `attempted_at` dual-format）写入 `元数据规范.md`；**不**修改 Frozen K/P Schema contract；A000001/A000002 **不必** retroactive 修改（仍 valid）。

### 22.13 Non-Goals（3C-4 边界）

* 修改 A000001/A000002/P0002/K/P objects  
* 开发 Attempt Validator / Indexer / Workflow  
* 执行 Schema Freeze（3C-5）  
* 修改 `元数据规范.md` Frozen K/P contract  
* Git commit / push

---

## 23. Phase 3C-5 — Attempt Schema v1 Freeze + Authority Transition

**Status：** **DONE**（2026-08-19）

### 23.1 Freeze Outcome

Attempt Schema v1 已正式冻结。

| Item | Value |
| --- | --- |
| **STATUS** | **FROZEN** |
| **Formal technical authority** | `元数据规范.md` §8 |
| **Design history / rationale** | 本文件 §19–§22（historical；superseded by Frozen v1 for field-level authority） |
| **Required fields** | 6 |
| **Optional fields** | 3 |
| **Allowed top-level fields** | exactly 9 |
| **Real Pilots** | A000001, A000002（conforming；canonical quoting normalization only） |

### 23.2 Authority Transition

**Before Freeze：** Attempt design / Candidate authority → 本文件（§19–§22）

**After Freeze：** Attempt Schema v1 formal technical authority → `元数据规范.md` §8

本文件从此 **不得** 覆盖 Frozen technical contract；仅保留 design history、semantic rationale、Candidate / Pilot / Final Review / Freeze 历史。

### 23.3 Key Frozen Decisions（pointer only）

完整 contract 见 `元数据规范.md` §8。摘要：

* `attempted_at` dual-format：minute + explicit offset **OR** date-only；quoted temporal scalar；no fabricated precision
* `outcome` 六枚举；`assistance` 两枚举 + omission semantics
* no `status` / `created` / `updated` / `knowledge` / assessment provenance / derived state
* Correction → same A ID + append；Reattempt → new A ID
* Unknown top-level field → Schema violation

### 23.4 Phase 3C-6 Handoff — Attempt Validator v1

**Phase 3C-6：** **READY**（await 明确授权；**不得**自动开始）

Attempt Validator v1 **must enforce**（机械层，见 `元数据规范.md` §8.18A）：

* Markdown / frontmatter discovery
* YAML parse
* allowed top-level fields（exactly 9）
* required fields
* `schema_version == 1`
* `id` lexical validity + sentinel exclusion
* duplicate Attempt ID
* `type == attempt`
* referenced `problem` existence
* `part` existence when present
* whole target via `part` omission
* `outcome` enum
* `assistance` enum / omission
* `attempted_at` Frozen Temporal Contract
* `corrections` list shape、allowed keys、`at` temporal validation、`note` non-empty
* one Attempt record per Markdown file

Validator **must NOT assess**（语义 / 过程层，§8.18B）：

* 数学证明正确性
* outcome 是否数学真实
* assistance 是否真实
* Assessment 是否充分
* correction 是否历史上真正 append-only
* reattempt 是否真的来自新 solving episode

**Automation order：** Freeze（3C-5 DONE）→ Validator（3C-6）→ Validated Attempt Registry → Indexer integration → Derived State

**Non-Goals（3C-5）：** 未创建 `tools/attempt_validator/`；Workspace Maintenance v1 仍不解析 Attempt；未生成 Evidence index / statistics。

---

## 24. Phase 3C-6 — Attempt Validator v1

**Status：** **DONE**（2026-08-19）

### 24.1 Outcome

| Item | Value |
| --- | --- |
| **Validator** | `tools/attempt_validator/` v1 |
| **Schema authority** | `元数据规范.md` §8（**unchanged**） |
| **Mode** | read-only |
| **Dependency** | Knowledge Validator → Problem Validator → Attempt Validator |
| **Registry** | in-memory `dict[str, AttemptDocument]` keyed by YAML `id`（only when project validation PASS） |
| **CLI** | `python -m tools.attempt_validator check` · `check-file` |

### 24.2 Validator Boundaries

**Enforces（§8.18A）：** discovery、parse、9 fields、required、schema_version、ID/sentinel/duplicate、type、problem/part relation、outcome、assistance/omission、temporal dual-format、corrections shape、one record per file。

**Does NOT assess（§8.18B）：** 数学正确性、outcome/assistance 真实性、Assessment 充分性、historical append-only、reattempt 事件真实性。

### 24.3 Non-Goals（3C-6）

* 修改 Frozen Schema
* Workspace Indexer / Workspace Check Attempt integration
* Evidence Index / statistics
* Attempt Recorder / Workflow / ID allocator

### 24.4 Next

**Attempt validated-registry integration review**（是否 / 何时让 Workspace Maintenance 消费 Validated Attempt Registry）— **await 明确授权**；不自动进入 Phase 3D。
