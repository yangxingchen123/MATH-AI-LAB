# GUI + repair runtime
PySide6>=6.6
pypdf>=4.0
pymupdf>=1.24
Pillow>=10.0
numpy>=1.24
opencv-python-headless>=4.8
python-dotenv>=1.0

# EPUB uses the stdlib zipfile; no extra package.

# Vision / UI template matching (install when using 高保真视觉)
# playwright>=1.40
# then: playwright install chromium

# Optional formula OCR for FormulaRecoveryManager
# Default: UniMERNet via MinerU MFR (GPU). Pix2Tex remains optional fallback:
#   pip install "pix2tex>=0.1.2"

# Engines (install explicitly; versions move quickly)
# docling
# mineru

# Common runtime deps often pulled by engines
# torch
# transformers
# huggingface_hub

# Tests / CI (also: pip install -e ".[dev]")
# pytest>=7.0
