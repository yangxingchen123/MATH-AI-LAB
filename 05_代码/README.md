# 代码与实验

| 用途 | 放哪里 |
| --- | --- |
| 单次数值抽查 | 可写在对应 Problem / 项目的临时脚本；不要污染根环境 |
| 可复现建模实验 | `05_代码/<项目>/`（从 `_模板/建模实验_v1/` 复制） |
| MATLAB | 同一项目下的 `matlab/`，不进根 `requirements.txt` |

计算建模（v1.4）与竞赛数学建模共用这种工程结构；论文叙事在 `07_项目/`，排版在 `04_LATEX/数学建模/`。
